---
title: Joint Learning of Experiential Rules and Policies for Large Language Model Agents
title_zh: 面向大语言模型智能体的经验规则与策略联合学习
authors: "Shicheng Ye, Chao Yu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27136v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: LLM智能体的经验规则与策略联合学习
tldr: 大语言模型（LLM）智能体在多步交互任务中亟需高效利用累积经验，但现有方法将外部规则提示与策略参数更新分离，前者易与演化策略脱节，后者在稀疏奖励下局部纠错不足。JERP从相同交互轨迹联合学习经验规则与策略，决策时检索规则并与历史共同条件化智能体，事后通过比较当前轨迹与成功示例修订规则池并优化策略，实现规则与策略的紧耦合迭代。在AlfWorld和WebShop上的实验结果表明，JERP持续获得决策性能提升，其框架使稳定行为可逐步吸收至模型内部，同时保持规则库的活性和可解释性，为LLM智能体的渐进式经验内化提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27136v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1821, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 727, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27136v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1866, \"height\": 917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27136v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27136v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有LLM智能体经验利用方法将规则提示与策略更新分离，导致规则与演化策略不同步或局部错误纠正受限，尤其在稀疏奖励任务中表现不佳。
method: 提出JERP框架，从同一交互轨迹联合更新经验规则池和策略，决策时检索规则与历史共同条件化，事后通过对比成功轨迹修订规则池并优化策略。
result: 在AlfWorld和WebShop复杂交互任务上，JERP一致地取得决策性能增益，验证了联合学习框架的有效性。
conclusion: JERP通过紧耦合规则维护与策略优化，使稳定行为逐步内化，同时保持规则库时效性，为构建可解释且自适应LLM智能体提供了新途径。
---

## 摘要
对于在多步交互环境中的大语言模型智能体，一个关键挑战是如何有效利用累积的交互经验。现有工作通常将此类经验的两种用途分开：将其保留在模型外部作为自然语言规则，用于后续提示；或者利用轨迹和反馈来更新模型参数。前者易于解释，但可能与不断演化的策略不同步；后者更广泛地改进了策略，但在稀疏奖励设置中仅提供对局部错误的有限纠正。我们提出了面向大语言模型智能体的经验规则与策略联合学习（JERP），从相同的交互轨迹中更新长期经验规则库和策略。在决策时，JERP检索与任务相关的规则，并将其与交互历史一同作为智能体的条件。每个回合结束后，它利用收集到的轨迹来优化策略，并通过将当前推演与参考成功轨迹进行比较来修订规则库。这种耦合使规则库与演化的策略保持一致，同时允许稳定有效的行为逐渐被吸收到模型本身中。在AlfWorld和WebShop上的实验表明，JERP在复杂交互任务中带来了决策性能的持续提升。

## Abstract
For LLM agents in multi-step interactive environments, a key challenge is to make effective use of accumulated interaction experience. Existing work has typically separated two uses of such experience: keeping it outside the model as natural-language rules for later prompting, or using trajectories and feedback to update the model parameters. The former is easy to interpret but can fall out of sync with the evolving policy; the latter improves the policy more broadly but provides only limited correction for local mistakes in sparse-reward settings. We present Joint Learning of Experiential Rules and Policies for LLM Agents (JERP), which updates a long-term experiential-rule pool and the policy from the same interaction trajectories. At decision time, JERP retrieves task-relevant rules and conditions the agent on them together with the interaction history. After each episode, it uses the collected trajectories both to optimize the policy and to revise the rule pool by comparing current rollouts with reference successful trajectories. This coupling keeps the rule pool aligned with the evolving policy while allowing stable and effective behaviors to be gradually absorbed into the model itself. Experiments on AlfWorld and WebShop show that JERP yields consistent gains in decision performance for complex interactive tasks.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文，以下是以 Markdown 形式生成的结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：当前，基于大语言模型的智能体在多步交互式任务中面临一个核心挑战：如何有效利用累积的交互经验来提升未来决策能力。
*   **现有范式与局限**：现有工作主要将经验利用分为两种割裂的范式，各有优劣：
    *   **外部规则提示范式**：将经验提炼为模型外部的自然语言规则，通过提示工程注入。虽然可解释，但随着模型策略在训练中演化，这些静态规则容易过时，与策略不匹配。
    *   **策略参数更新范式**：利用交互轨迹和反馈直接微调模型参数。能全局优化策略，但在反馈稀疏的复杂任务中，难以对局部、具体的错误进行及时且有针对性的纠正。
*   **核心问题**：论文旨在探索能否从**同一批交互数据**中同时支持显式的经验规则更新和隐式的策略参数优化，使二者相辅相成。
*   **整体含义**：提出一个名为 JERP（Joint Learning of Experiential Rules and Policies for LLM Agents）的联合学习框架，让规则能够随策略共同演化，逐步将稳定有效的行为内化到模型参数中，实现可解释性与自适应能力的统一。

### 2. 论文提出的方法论

JERP 的核心思想是在同一个训练循环中，交替进行**策略优化**和**经验规则池的更新**，两者共享同一批交互轨迹。

*   **核心框架与流程**：
    1.  **规则检索与决策**：对于每个任务实例 `d`，维护一个长期经验规则池 `K(d)`。每轮开始前，根据效用分数从高到低选取 Top-k 条规则作为工作规则集 `K̃(d)`。大语言模型策略 `πθ` 在决策时，其输入提示 `xt` 由任务描述、当前交互历史 `ht` 和该工作规则集 `K̃(d)` 共同构成。
    2.  **轨迹采样**：基于当前策略和规则，采样一组共 `N` 条完整轨迹 `Td = {τi}`。
    3.  **阶段 I：模型参数更新**：
        *   采用**组相对策略优化**方法，借鉴 GRPO（组相对策略优化），依据轨迹组内的相对表现来更新策略参数 `θ`。
        *   对同一任务实例采样的 `N` 条轨迹，计算每条轨迹的最终回报 `R(τi)`。
        *   对组内回报进行标准化，得到每条轨迹的组相对优势 `Ai`（公式18）。同一轨迹内的所有时间步共享该优势值。
        *   优化目标 `L_GRPO` （公式20）类似于 PPO，通过最大化优势并引入 `clip` 操作和 KL 散度正则项来限制策略更新幅度。
    4.  **阶段 II：经验规则库更新**：
        *   目标是维护而非仅仅扩充规则库，包括**添加、修订、强化、削弱、合并**规则，并控制容量。
        *   采用基于**对比反思**的结构化更新方法。输入为当前规则池 `K(d)`、当前采样的轨迹组 `Td` 和**参考成功轨迹集** `T+(d)`，通过提示一个冻结参数的大语言模型，生成一系列规则级编辑操作 `O(d)`。
        *   系统定义了五种编辑操作：`ADD`（添加新规则）、`EDIT`（修订现有规则）、`UPVOTE`/`DOWNVOTE`（增加/减少规则效用分数）、`MERGE`（合并语义相近或功能重叠的规则）。
        *   通过解析并执行这些操作，以及根据固定步长更新效用分数，实现对规则池的动态维护。

*   **算法流程**（Algorithm 1 摘要）：
    1.  初始化各任务 `d` 的规则池 `K(d)` 和成功轨迹集 `T+(d)` 为空。
    2.  每轮训练循环：采样任务实例 `d`。
    3.  检索工作规则集，采样 `N` 条轨迹。
    4.  基于轨迹组计算优势值，更新策略参数 `θ`。
    5.  调用 `ReflectAndEdit` 操作，对 `K(d)` 进行修订。
    6.  更新规则分数并修剪规则池。
    7.  将本轮成功的轨迹加入 `T+(d)`。

### 3. 实验设计

*   **数据集/场景**：在两个经典的交互式基准上进行评估：
    *   **AlfWorld**：一个基于文本的家居操作环境，包含 6 种任务类型（如拾取与放置、加热后放置、查看物体等），具有部分可观测和需要多步交互的特点。评估指标为**任务成功率（%）**。
    *   **WebShop**：一个在线购物交互环境，要求智能体执行搜索、浏览、比较和购买等操作。评估指标为**平均得分**和**任务成功率（%）**。
*   **基准对比方法**：对比了五类代表性方法，以区分不同经验利用方式的效果：
    *   **Vanilla LLM**：直接提示基础模型，无任何经验复用。
    *   **ReAct**：仅依靠回合内推理与行动，无跨回合记忆或参数更新。
    *   **Reflexion**：将失败经验提炼为自然语言反思，存入外部记忆，但**不更新参数**。
    *   **RLOO**：基于强化学习的参数更新方法，采用留一法基线。
    *   **GRPO**：与 JERP 参数更新部分一致的组相对策略优化方法，但**无经验规则池**。

### 4. 资源与算力

论文明确说明了实验的硬件配置：
*   **服务器系统**：Ubuntu 22.04 LTS。
*   **中央处理器/内存**：2 颗 Intel Xeon Gold 6226R 处理器，约 512 GB 内存。
*   **图形处理器**：4 块 NVIDIA A30  GPU。
*   **训练细节**：使用 LoRA（低秩适配）进行参数高效微调。详细的超参数如表 1 所示，包括学习率（3e-6）、采样组大小（8）、训练轮次（AlfWorld 200， WebShop 300）等。论文未明确给出总训练时长。

### 5. 实验数量与充分性

*   **实验组数**：
    *   **主实验**：在2个环境（AlfWorld 和 WebShop）上与 5 种基准方法进行全面对比。
    *   **细粒度对比**：在 AlfWorld 的 6 个子任务上分别绘制了训练曲线（图 4），展示各方法在不同阶段的表现。
    *   **消融实验**：针对核心机制（动态规则池更新）进行了消融研究，对比了 JERP 与其“冻结规则池”的变体（图 3）。
*   **实验充分性与公平性**：
    *   **客观性**：所有对比方法均在同一硬件平台上运行，关键超参数公开，结果汇报基于三次独立运行的平均值，保证了结果的可复现性和客观性。
    *   **公平性**：对比基准覆盖了从无经验复用到仅外部记忆、再到仅参数优化的多种范式，能清晰凸显 JERP 联合学习范式的增益。消融实验也直接验证了提出的动态规则更新机制的有效性。
    *   **总体评价**：实验设计较为严谨，从整体性能到任务细分，再到核心机制验证，论证链条完整，实验数量充足。

### 6. 论文的主要结论与发现

*   **整体性能提升**：JERP 在两个基准测试中均取得了最优的整体性能。在 AlfWorld 上成功率达 61.5%，在 WebShop 上得分 79.0，成功率 64.1%，均显著优于纯策略优化方法（GRPO， RLOO）。
*   **长序列与高约束任务受益更多**：增益在步骤更长、约束更多的任务上尤为明显（如 AlfWorld 的清洁、加热、冷却、拾取两个物体任务），这表明可复用的规则有助于保留在早期遇到的有效操作模式。
*   **规则池动态更新的必要性**：消融实验证明，初始阶段形成的固定规则池会随着策略变化而过时。持续的规则池更新使得模型能吸收后期交互中暴露出的新错误和有效行为模式，是实现性能持续增长的关键。

### 7. 优点

*   **方法设计亮点**：
    *   **范式创新**：创新性地将外部经验规则化与内部策略参数化两种利用范式紧密结合，实现了“知行合一”的迭代演化。
    *   **紧耦合状态维护**：规则与策略的更新共享同一批数据，且规则随策略而变，有效解决了传统方法中规则与策略不同步的痛点。
    *   **可解释性与自适应性的平衡**：既保留了自然语言规则的可读、可编辑性，又通过参数更新吸收了稳定行为，实现两者优势互补。
*   **实验设计亮点**：
    *   **基准选择具有代表性**：AlfWorld 和 WebShop 是评估大语言模型智能体的主流复杂交互环境。
    *   **对比方法层次分明**：实验基线清晰地划分了不同经验利用范式的边界，使得 JERP 的贡献易于被客观衡量。

### 8. 不足与局限

*   **实验覆盖范围**：目前仅在两个单智能体的文本交互环境中进行了验证，其在多模态环境、多智能体协作场景下的表现尚不明确。
*   **方法与实现细节限制**：
    *   **规则检索策略简单**：工作规则集的选择仅基于全局效用分数进行 Top-k 排序，未采用更精细的、面向特定任务实例的语义相关性检索，这可能在复杂任务下引入噪声。
    *   **规则更新依赖大语言模型**：规则修订操作完全依赖另一个大语言模型进行对比反思生成，这可能引入额外的推理成本、不稳定性和潜在偏差。规则的生成质量和合并操作的准确性高度依赖于该大语言模型的能力。
    *   **冷启动问题**：训练初期，若无任何成功轨迹（`T+(d)` 为空）用于对比反思，规则池的更新效率可能会受到影响。

（完）
