---
title: "SPIRAL: Learning to Search and Aggregate"
title_zh: SPIRAL：学习搜索与聚合
authors: "Jubayer Ibn Hamid, Ifdita Hasan Orney, Michael Y. Li, Omar Shaikh, Yoonho Lee, Dorsa Sadigh, Chelsea Finn, Noah Goodman"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23595v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: SPIRAL通过强化学习训练语言模型进行并行搜索与聚合以提升推理能力
tldr: "现有语言模型的后训练仅优化单轨迹顺序推理，难以充分利用测试时推理计算扩展中的并行采样与多轨迹聚合潜力。SPIRAL框架引入顺序-并行-聚合强化学习，模型并行采样多条独立思维链轨迹，再通过聚合轨迹生成最终响应，并以聚合奖励端到端优化全部组件。在推理任务上，SPIRAL的推理计算扩展效率较GRPO提升最高11倍，性能提高15%。该工作首次实现三种推理原语的统一训练，为可扩展的推理计算优化提供了新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23595v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1541, \"height\": 890, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1370, \"height\": 913, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1524, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1554, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 779, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1521, \"height\": 623, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1514, \"height\": 636, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23595v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1398, \"height\": 588, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23595v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 710, \"label\": \"Table\"}]"
motivation: 当前模型仅针对单轨迹顺序推理优化，未能利用并行采样与多轨迹聚合扩展测试时计算。
method: SPIRAL框架使用集合强化学习训练模型并行生成多条思维链，再用标准强化学习训练聚合模型，端到端优化最终响应奖励。
result: "在推理任务上，SPIRAL推理计算扩展效率比GRPO最高提升11倍，性能提高15%。"
conclusion: SPIRAL首次统一训练顺序推理、并行采样和聚合，为语言模型推理能力的高效扩展提供了新范式。
---

## 摘要
通过在测试时扩展推理计算，语言模型的推理能力可得到显著提升，这涉及不同的计算原语——单条轨迹内的顺序推理、独立采样的并行轨迹，以及将多条推理轨迹聚合成最终响应。然而，在训练后阶段，语言模型通常仅针对单条轨迹的顺序推理进行优化。我们提出顺序-并行-聚合强化学习（SPIRAL）框架，该框架训练语言模型使用所有三种原语，作为统一推理计算流程的一部分。具体而言，语言模型首先并行采样一组独立轨迹，每条轨迹通过顺序的思维链推理产生，然后基于这些轨迹生成最终的聚合轨迹；所有组件都针对最终聚合响应的奖励进行端到端优化。为训练此系统，SPIRAL使用集合强化学习教会模型产生一组对聚合器集体有用的轨迹，并使用标准强化学习教会模型将该集合聚合成更优的最终响应。我们在推理任务上的实验表明，SPIRAL能有效随推理计算扩展，当所有三种计算原语都被扩展时，其扩展效率比GRPO高出11倍，性能提升15%。

## Abstract
Language model reasoning can be substantially improved at test time via scaffolds that scale inference compute across different primitives -- sequential reasoning within a trace, independently sampled parallel traces, and aggregation of multiple reasoning traces into a final response. During post-training, however, language models are optimized only for sequential reasoning within a single trace. We introduce Sequential-Parallel-Aggregative Reinforcement Learning (SPIRAL), a framework in which a language model is trained to use all three primitives, as part of a unified inference compute pipeline. Concretely, the language model first samples a set of independent traces in parallel, each produced through sequential chain-of-thought reasoning, and then generates a final aggregation trace conditioned on those traces; all components are optimized end-to-end against the reward of the final aggregated response. To train this system, SPIRAL uses set reinforcement learning to teach models to produce a set of traces that are collectively useful for an aggregator and standard reinforcement learning to teach models to aggregate the set into improved final responses. Our experiments on reasoning tasks show that SPIRAL effectively scales with inference compute, outperforming GRPO by up to 11$\times$ scaling efficiency and 15% higher performance when all three compute primitives are scaled.

---

## 论文详细总结（自动生成）

好的，以下是对论文《SPIRAL: Learning to Search and Aggregate》的结构化深入总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：当前的语言模型通常在训练后阶段（post-training）仅针对单条轨迹内的顺序推理进行优化（如通过GRPO等方法）。然而，在测试时，为了提升性能，业界普遍采用多种推理计算扩展策略，包括：
    *   **顺序计算**：在单条思维链中投入更多思考token。
    *   **并行计算**：独立采样多条推理轨迹。
    *   **聚合计算**：将多条候选轨迹合成一个更优的最终答案。
    语言模型在训练期间并未接触过并行和聚合原语，导致其无法学会主动协调这些计算，限制了测试时推理计算扩展的效能。模型常常在顺序推理中错配计算资源，在并行采样中缺乏探索多样性，且在聚合时无法有效验证、筛选和融合不同思路。
*   **整体含义**：本文旨在弥合训练与测试之间的差距，提出一个统一框架，让语言模型在训练阶段就学会如何协同使用顺序、并行和聚合三种推理计算原语，从而在测试时能更有效地将原始计算资源转化为更优的搜索和推理策略。

### 2. 论文提出的方法论

*   **核心思想**：提出**SPIRAL（Sequential-Parallel-Aggregative Reinforcement Learning）** 框架，将整个推理流程视为一个端到端的强化学习问题。模型不再是孤立地优化单条轨迹，而是优化从并行采样到聚合的完整管道，仅以最终聚合响应的奖励作为唯一的优化信号。
*   **关键技术细节与算法流程**：
    1.  **流程分解**：SPIRAL将推理过程分解为两个阶段：**搜索（Search）** 和**聚合（Aggregation）**。
        *   **搜索阶段**：模型基于问题 `x`，并行、独立地采样 `N1` 条搜索轨迹 `y_1, ..., y_{N1}`，每条轨迹都是一次顺序推理。
        *   **聚合阶段**：从这 `N1` 条轨迹中随机采样 `K` 个大小为 `n` 的集合。对于每个集合 `G_i`，模型生成 `N2` 条聚合轨迹 `y^{G_i}_j`，这些轨迹基于问题和当前集合中的所有搜索轨迹进行综合推理，最终产出答案。
    2.  **统一优化**：优化目标被定义为最大化最终聚合响应的期望奖励：
        `max_{θ} E_{y_{1:n}~π_θ(·|x)} [E_{y^*~π_θ(·|x, y_{1:n})} [r(x, y^*)]]`
    3.  **双梯度更新**：将上述目标的梯度分解为两项，分别对应两种不同的学习信号：
        *   **集合强化学习梯度（Set RL Gradient）**：用于优化搜索轨迹。它为每条搜索轨迹分配一个**边际集合优势（marginal set advantage）**。该优势的计算方式为：首先计算包含该轨迹的所有集合的集合级优势（集合的平均奖励减去基线），然后取平均。这意味着，一条搜索轨迹即使本身不正确，但只要它能有效帮助聚合模型在结合其他轨迹时推导出正确答案，就会获得正向奖励，从而鼓励模型进行多样化和互补性的探索。
        *   **标准强化学习梯度（Standard RL Gradient）**：用于优化聚合轨迹。它使用标准的组内优势函数，鼓励模型在同一批搜索轨迹的条件下，生成比平均水平更好的聚合响应，从而学会有效的综合、验证和纠错能力。
    4.  **目标函数**：搜索轨迹的集合级目标函数 `f_spiral(x, y_{1:n})` 被定义为给定一组搜索轨迹时，聚合模型所能获得的期望最高奖励，从而将搜索和聚合的性能耦合在一起。

### 3. 实验设计

*   **数据集与场景**：
    *   **训练数据集**：使用**POLARIS-53k**（一个数学推理问题数据集）的过滤子集。
    *   **评测场景**：**数学推理**任务。
*   **基础模型**：**Qwen3-4b-Instruct-2507**。
*   **对比方法**：
    *   **主要基线**：**GRPO（Group Relative Policy Optimization）**，这是当前主流的、仅优化单轨迹顺序推理的强化学习方法。
    *   **测试时扩展策略**：
        *   `pass@k`：独立采样k条轨迹，只要有任意一条正确即视为正确（模拟有最佳答案验证器的场景）。
        *   多数投票（Majority@k）。
        *   递归自聚合（Recursive Self-Aggregation, RSA）。
        *   顺序计算扩展（生成长度更长的单条思维链）。
*   **训练预算公平性**：确保SPIRAL和GRPO在训练时每个问题使用**完全相同的推理计算总token预算**（98304 tokens）。SPIRAL将此预算分配在8条搜索轨迹和16条聚合轨迹上，而GRPO则用于生成12条更长的独立轨迹。

### 4. 资源与算力

*   论文明确指出，其实验规模相对较小，并计划未来在至少**80亿参数**的模型上进行训练。
*   关于具体的GPU型号、数量或总训练时长，**文中未提供详细说明**。
*   论文提到使用了**Tinker**平台进行强化学习训练，并获得了Google DeepMind的**TPU资助**。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了多组实验，从不同维度评估了模型的推理计算扩展能力，主要包括：
    1.  **并行计算扩展**（`pass@k`）评估。
    2.  **并行+聚合计算扩展**（递归自聚合）评估。
    3.  **纯顺序计算扩展**评估。
    4.  **对比测试时推理方法**（自聚合 vs. 多数投票）的效率。
    5.  **效率分析**：基于固定Token预算比较不同方法的性能。
    6.  **过程指标分析**：如Token级熵值的变化。
*   **充分性与公平性**：
    *   **公平性较强**：通过控制训练时的总Token预算保持一致，确保了与GRPO基线对比的公平性。
    *   **评估维度全面**：覆盖了顺序、并行、聚合以及混合策略（如递归自聚合）等多种测试时扩展场景，能全面反映模型在不同原语上的能力提升。
    *   **局限性**：实验目前仅在**一个模型（4B参数）** 和**一个任务类型（数学推理）** 上进行，需要更大规模和更广泛任务上的验证才能证明其普适性。

### 6. 论文的主要结论与发现

*   **统一的端到端训练优于分离设计**：通过SPIRAL框架，联合优化搜索和聚合过程，使模型能够学会超越人工设计启发式规则的动态搜索与综合策略。
*   **更高的探索效率与多样性**：SPIRAL训练的模型生成的搜索轨迹具有更高的多样性（Token级熵值下降更慢），使其在`pass@k`评估中表现出色，**扩展效率最高达到GRPO的11倍**。
*   **更强大的聚合能力**：SPIRAL学会了如何有效利用并行计算。在递归自聚合测试下，其性能**最高比GRPO提升15%**，表明模型学会了生成对下游聚合有用的轨迹，并且聚合模型本身也更强。
*   **模型自聚合优于规则聚合**：SPIRAL在递归自聚合下显著优于多数投票，而GRPO模型在这两种方法下表现相近。这说明SPIRAL的训练赋予了模型更强大的基于模型的聚合能力。
*   **顺序计算扩展瓶颈**：尽管SPIRAL在并行和聚合原语上优势明显，但在纯顺序计算扩展（生成长链）上，其性能与GRPO无明显差异，且所有模型在此原语下都会因上下文长度限制而遇到性能瓶颈。

### 7. 优点

*   **问题新颖且重要**：首次提出在训练阶段统一优化顺序、并行和聚合三种推理计算原语，直击当前推理模型训练与测试脱节的关键问题。
*   **方法论扎实**：巧妙地将集合强化学习与标准强化学习结合，解决了对并行搜索轨迹进行联合信用分配的难题，理论推导清晰。
*   **实验设计合理**：严格控制训练Token预算，实现了与强大基线（GRPO）的公平对比，并从多个维度（效率、多样性、不同测试策略）提供了有力证据。
*   **性能提升显著**：在关键指标，特别是测试时计算扩展效率上，取得了高达11倍的显著提升，证明了方法有效性。

### 8. 不足与局限

*   **实验规模有限**：目前仅在4B参数的较小模型和数学推理任务上进行了验证，结论向更大模型、更复杂领域（如代码、科学）的泛化能力尚待证实。
*   **训练成本**：尽管在token预算上做了公平对比，但SPIRAL的训练流程涉及构建多个集合并进行聚合采样，其实现复杂度和潜在的硬件利用效率可能与标准RL不同，文中未详细讨论。
*   **对称性假设**：集合RL梯度估计器在实现中假设了目标函数对输入轨迹顺序的对称性，而语言模型存在顺序偏见，虽然论文在理论上进行了补救（3.3节），但实际影响未深入探讨。
*   **超参数敏感性**：方法引入了多个新超参数（如搜索轨迹数、集合大小、聚合轨迹数），可能增加了调参的复杂性。
*   **递归扩展中的窗口固定问题**：训练时聚合窗口大小（n=4）固定，而测试时虽然能递归扩展，但其尺度能力与训练设置间的关系未被深入分析。

（完）
