---
title: "SPAR: Semantic-Pixel Self-Alignment and Adaptive Routing for Unified Multimodal Models"
title_zh: SPAR：统一多模态模型的语义-像素自对齐与自适应路由
authors: "Hongxiang Li, Hongxu Chen, Chenyang Zhu, Xiaoshuang Huang, Jiayin Cai, Xiaolong Jiang, Yao Hu, Long Chen"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23041v1"
tags: ["query:rl-mm-llm-ag"]
score: 10.0
evidence: 统一多模态框架，通过自对齐和路由整合视觉理解与生成
tldr: 多模态大语言模型在视觉理解上表现卓越，但在视觉生成任务中，因语义感知与像素级重建之间的根本特征差异而受到严重制约。现有方法要么依赖外部教师对齐，要么难以在统一框架内兼顾高保真重建。为解决这一问题，SPAR框架创新性地引入语义-像素自对齐与自适应路由机制。其核心组件包括：非对称双流统一分词器，利用轻量语义流保持判别特征，同时通过Transformer增强像素流恢复精细视觉细节到紧凑潜空间；自对齐生成范式，直接使用优化后的分词器作为内部对齐教师训练扩散模型，无需外部依赖；以及动态令牌路由，使每个令牌根据语义需求自适应聚合多层级MLLM特征。大量实验证明，SPAR在统一架构中取得了当前最优的生成和重建质量，并且完全保持了基础视觉理解能力，为构建真正统一的多模态模型开辟了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1241, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23041v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 1247, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 815, \"height\": 751, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 827, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1222, \"height\": 848, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1221, \"height\": 590, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1198, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23041v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 957, \"height\": 221, \"label\": \"Table\"}]"
motivation: 克服多模态大语言模型在视觉生成中因语义与像素特征差异导致的性能瓶颈，使其兼顾理解与生成。
method: 提出SPAR框架，包含非对称双流分词器实现语义-像素自对齐，自对齐生成范式内部训练扩散模型，及动态令牌路由自适应聚合特征。
result: SPAR在统一架构上达到生成与重建的SOTA，同时完全保持视觉理解性能。
conclusion: SPAR有效弥合语义理解与像素生成的鸿沟，为高效统一多模态模型提供新方案。
---

## 摘要
多模态大语言模型在视觉理解方面取得了显著成功，但由于语义感知与像素级重建之间存在根本性的特征差异，在视觉生成方面仍受限。弥合这一差距需要克服两个核心挑战：赋予语义编码器高保真重建能力，以及在不依赖外部教师的情况下有效对齐生成模型与语义空间。为此，我们提出了一种新颖的统一多模态框架，称为语义-像素自对齐与自适应路由（SPAR）。首先，为了协调语义感知与像素级重建，我们引入了一个非对称双流统一分词器。轻量级的语义流锚定判别特征，而增强型Transformer像素流则将细粒度视觉细节恢复到统一的紧凑潜在空间中。其次，为了消除外部依赖，我们提出了一种自对齐生成范式，该范式天然地利用此优化后的分词器作为扩散模型的内部对齐教师。此外，为了实现该统一空间内灵活的多模态交互，我们引入了动态令牌路由，使每个令牌能够根据其独特的语义需求自适应地聚合多层多模态大语言模型特征。大量实验表明，SPAR在统一架构中达到了最先进的水平，在保持基础视觉理解能力的同时，实现了卓越的生成和重建质量。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable success in visual understanding but remain constrained in visual generation due to the fundamental feature discrepancy between semantic perception and pixel-level reconstruction. Bridging this gap requires overcoming two core challenges: endowing semantic encoders with high-fidelity reconstruction capabilities, and effectively aligning generative models with semantic spaces without relying on external teachers. To this end, we propose a novel unified multimodal framework featuring \textbf{S}emantic-\textbf{P}ixel self-alignment and \textbf{A}daptive \textbf{R}outing (\textbf{SPAR}). First, to reconcile semantic perception with pixel-level reconstruction, we introduce an asymmetric dual-stream unified tokenizer. A lightweight semantic stream anchors discriminative features, while a Transformer-augmented pixel stream recovers fine-grained visual details into a unified compact latent space. Second, to eliminate external dependencies, we propose a self-aligned generation paradigm that natively leverages this optimized tokenizer as an internal alignment teacher for the diffusion model. Furthermore, to facilitate flexible multimodal interaction within this unified space, we introduce Dynamic Token Routing, which enables each token to adaptively aggregate multi-layer MLLM features based on its distinct semantic demands. Extensive experiments demonstrate that SPAR establishes the state-of-the-art for unified architectures, achieving exceptional generation and reconstruction quality while preserving foundational visual understanding capabilities.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：当前多模态大语言模型（MLLM）在视觉理解上表现优异，但在视觉生成任务中严重受阻，根源在于语义感知（侧重判别特征）与像素级重建（需保留高频细节）之间存在本征特征鸿沟。
- **核心挑战**：
  1. 如何让语义编码器同时具备高保真像素重建能力，且不破坏其原有的理解能力（防止灾难性遗忘）。
  2. 如何在不依赖外部教师模型的情况下，将生成模型（扩散模型）有效对齐到统一的语义空间。
- **整体含义**：SPAR 试图在单一框架内彻底弥合“理解”与“生成”的断裂，使 MLLM 无需切换表征空间即可同时胜任两种任务，从而实现真正意义上的统一多模态模型。

### 2. 论文提出的方法论
#### 2.1 核心思想
提出一种**语义‑像素自对齐与自适应路由**框架（SPAR），由三大组件构成：非对称双流统一分词器、自对齐生成范式、动态令牌路由。

#### 2.2 关键技术细节
##### (1) 非对称双流统一分词器（Semantic-Pixel Self-Aligned Tokenizer）
- **语义流**：轻量级投影器（残差块 + MLP），以最小变换将冻结编码器的输出映射到紧凑潜空间，锚定原始判别特征，防止语义遗忘。
- **像素流**：配备独立的 Transformer 编码器，建模长程空间依赖，将高维语义特征迁移到像素解码器原生潜空间，恢复细节；用像素对齐损失 \( \mathcal{L}_p = \| z_p - z'_p \|_2^2 \)（锚点为冻结像素编码器输出）引导训练。
- **融合与解码**：两流输出在紧凑潜空间沿通道拼接，通过融合层映射回目标维度，送入像素解码器重建。
- **渐进式三阶段训练**：
  - 阶段 I：冻结编码器与解码器，利用像素对齐损失和重建损失训练双流模块。
  - 阶段 II：解冻像素解码器，仅用重建损失联合优化。
  - 阶段 III：解冻编码器，引入语义对齐损失 \( \mathcal{L}_s = \| f_v - f'_v \|_2^2 \)（以冻结编码器特征为锚）和 GAN 损失，同时降低编码器学习率，防止灾难性遗忘。

##### (2) 统一多模态模型中的条件信号构建
- 在输入序列中插入可学习查询嵌入作为目标图像的隐式占位符，经 MLLM 因果注意力融合上下文后，生成条件表示。

##### (3) 动态令牌路由（DTR）
- 从 MLLM 的 \( L+1 \) 层中均匀采样 \( K \) 层隐藏状态。
- 以最深层特征为查询，经轻量网络 \( g(\cdot) \) 和 softmax（带温度系数 \( \tau \)）为每个令牌生成层路由权重 \( w_i \)。
- 利用逐层可学习缩放参数 \( \alpha \) 加权求和，再投影得到融合特征：\( \hat{H}_i = W_p \sum_{k=1}^{K} w_i^{(k)} \cdot \alpha^{(k)} \cdot H_i^{(k)} \)。
- 优势：令牌能根据自身语义（如边缘、纹理、文本指令）自适应选择特征层组合，并提供可解释性。

##### (4) 自对齐生成范式
- 直接使用优化后的统一分词器作为 DiT 的内部对齐教师，无需外部特征网络。
- 对齐损失：\( \mathcal{L}_{\text{align}} = -\frac{1}{N} \sum_{i=1}^{N} \cos(\phi_a(f_{\text{dit},i}), f'_v,i) \)（最大化余弦相似度）。

##### (5) 整体训练策略
- 阶段 I（连接器预训练）：冻结 MLLM 和 DiT，训练连接器、DTR、查询等，仅用流匹配损失 \( \mathcal{L}_{fm} \)。
- 阶段 II（联合预训练）：解冻 DiT，加入自对齐损失 \( \mathcal{L}_{fm} + \lambda_a \mathcal{L}_{\text{align}} \)，混合生成与编辑数据。
- 阶段 III（监督微调）：用高质量指令数据进一步提升。

### 3. 实验设计
- **数据集**：
  - 重建评估：ImageNet 50k 验证集（256×256）。
  - 生成训练：27M 重标注数据、5M CC12M 子集、4M JourneyDB 合成图像。
  - 编辑训练：GPT-Image-Edit 数据集。
  - 微调：BLIP3o-60K、ShareGPT-4o-Image。
- **Benchmark 与任务**：
  - 图像重建：rFID、PSNR、SSIM。
  - 视觉理解：MME-P、MMBench、MMMU、MM-Vet、SEED-Bench、MMVP。
  - 文本到图像生成：GenEval、WISE。
  - 图像编辑：ImgEdit-Bench。
- **对比方法**：
  - 生成专用分词器（LlamaGen、VAR、SD-VAE、DC-AE、VA-VAE 等）和统一分词器（VILA-U、TokenFlow、DualToken、EMU2 等）。
  - 纯理解模型（LLaVA-OV、InternVL3、Qwen2.5-VL 等）和统一理解生成模型（Chameleon、Show-O、Janus-Pro、Harmon、BAGEL、BLIP3-o 等）。
  - 图像编辑模型（MagicBrush、InstructP2P、AnyEdit、UltraEdit、OmniGen、OmniGen2 等）以及闭源模型 GPT-4o。

### 4. 资源与算力
- 论文未明确提及使用的 GPU 型号、数量及训练时长。仅给出模型变体 SPAR‑1B（InternVL3‑1B + SANA‑0.6B）和 SPAR‑3B（InternVL3‑2B + SANA‑1.6B）的参数量信息，无具体算力细节。

### 5. 实验数量与充分性
- **实验组数**：
  - 核心定量对比：重建（表 1）、理解（表 2）、生成（表 3）、编辑（表 4）共 4 组大规模对比。
  - 消融实验：统一分词器组件消融（表 5，3 项变体）、统一模型组件消融（表 6，2 项变体）。
  - 定性结果：多张生成/编辑示例图。
- **充分性与公平性**：实验覆盖多个主流基准，对比方法涵盖同期最优的统一模型与专用模型，消融设计清晰验证了语义流、像素流、动态令牌路由和自对齐损失的独立贡献。各指标均采用标准化评测，比较公正客观。

### 6. 论文的主要结论与发现
- SPAR 在统一架构中首次将语义理解与像素生成显式解耦并重新整合，实现了高质量重建、生成和编辑，且未牺牲预训练的理解能力。
- 非对称双流设计有效避免灾难性遗忘，使编码器既保持强判别力又获得可观的像素表征。
- 自对齐范式成功消除了对外部语义教师的依赖，而动态令牌路由通过自适应多层融合进一步提升了生成质量。
- 在多项基准上达到或超越专用模型与同期统一模型的水平，尤其在 GenEval（0.91）和 ImgEdit（4.01）上表现突出。

### 7. 优点
- **技术解耦巧妙**：将语义保持与像素重建分配给非对称双流，平衡了理解与生成的目标冲突。
- **内部闭环对齐**：利用统一分词器自身作为扩散模型教师，避免外部模型带来的分布偏差，且简化了训练流程。
- **动态令牌路由**：赋予每个令牌按需选择特征层的能力，既提升性能又提供可解释性，优于静态使用单一层特征的常规方案。
- **训练策略稳健**：渐进式三阶段训练配合语义锚定与低学习率，确保编码器能力不被重构目标侵蚀。
- **实验全面**：在重建、理解、生成、编辑四大维度与大量 SOTA 方法对比，并辅以严谨的消融研究。

### 8. 不足与局限
- **规模局限**：实验模型仅覆盖 1B 和 3B 参数级别，未验证该框架在更大规模模型（如 7B 以上）上的有效性及扩展性。
- **算力信息缺失**：未公开训练所用的硬件配置与时长，难以评估实际部署成本。
- **编辑任务偏向**：编辑性能仅在单个基准 ImgEdit 上测试，且对比的闭源模型 GPT‑4o 指标差距仍存，真实场景泛化性待进一步考察。
- **依赖冻结 LLM**：整个训练过程中语言模型被冻结，可能限制了模型在深层语义交互上的潜力，也回避了 LLM 微调带来的灾难性遗忘问题。
- **数据组成未详解**：混合生成和编辑数据的具体比例与训练细节未充分披露，影响复现性。

（完）
