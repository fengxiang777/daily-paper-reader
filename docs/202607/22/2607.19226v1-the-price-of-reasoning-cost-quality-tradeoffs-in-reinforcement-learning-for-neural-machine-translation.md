---
title: "The Price of Reasoning: Cost-Quality Tradeoffs in Reinforcement Learning for Neural Machine Translation"
title_zh: 推理的代价：神经机器翻译中强化学习的成本与质量权衡
authors: "Michael Jungo, Aixiu An"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19226v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 研究基于大模型的神经机器翻译强化学习中推理轨迹的成本-质量权衡
tldr: 神经机器翻译中，采用强化学习与可验证奖励（RLVR）训练大语言模型，尤其在法律文件翻译上展现了潜力，但性能提升是否真正源于模型推理能力尚未明确。本研究通过消融实验，系统性移除训练或推理阶段中模型生成的推理轨迹，以考察其作用。结果表明，推理阶段包含推理步骤能显著提升翻译质量，而训练阶段是否包含推理影响甚微；但推理导致输出token数量增多，增加了计算开销。该研究揭示了RLVR在翻译任务中的成本-质量权衡，提示了在追求高质量翻译时需考虑推理带来的计算代价，为高效部署提供了洞见。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19226v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1666, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19226v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 833, \"height\": 791, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19226v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1763, \"height\": 473, \"label\": \"Table\"}]"
motivation: 针对法律文件翻译中强化学习方法的优越性，质疑其增益是来自推理能力还是训练范式本身，以厘清推理的实际作用。
method: 通过系统性省略训练或推理阶段中的推理轨迹，设计消融实验以分析推理步骤对翻译质量的影响。
result: 实验发现，仅在推理时保留推理步骤才能提升翻译质量，而训练时有无推理影响微弱，但推理导致输出token量增加。
conclusion: 研究揭示了推理带来的质量提升与计算开销之间的权衡，为在资源有限的翻译系统中高效应用RLVR提供指导。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已被确立为大语言模型（LLM）后训练的一种可行范式，涵盖神经机器翻译（NMT）等下游任务。最新研究表明，RLVR 因其引发的推理能力，可能成为翻译法律文件的首选训练方法，这引出了一个问题：这究竟是归因于推理，还是更普遍地归因于训练范式。我们通过在训练或推理阶段之一系统地省略模型的推理轨迹，研究了在生成响应中包含该轨迹的重要性。实验表明，包含推理，特别是在推理阶段，对整体翻译质量有积极影响。此外，我们认识到推理会导致输出 token 数量增加，因此我们研究了增加的计算需求与提高的翻译质量之间的成本-质量权衡。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has been established as a viable paradigm for the post-training of Large Language Models (LLMs), including downstream tasks, such as Neural Machine Translation (NMT). With the latest research indicating that RLVR could be the preferred training method for translating legal documents due to the induced reasoning capabilities, it raises the question whether it is really attributed to the reasoning or more generally to the training paradigm. We investigate the importance of including the model's reasoning trace in the generated responses during both training and inference by systematically omitting it from one of the phases. Our experiments show that including the reasoning, specifically during inference, has a positive effect on the overall translation quality. Furthermore, we recognise that the reasoning leads to an increase in output tokens, hence we study the cost-quality tradeoff between the increased computational demands and the improved translation quality.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：基于可验证奖励的强化学习（RLVR）已成为大语言模型（LLM）后训练的有效范式，尤其在法律文本翻译这类高要求任务中，其通过引发模型的“推理”能力，有望在翻译质量上超越传统的监督微调（SFT）。
- **核心问题**：RLVR带来的翻译质量提升，究竟是源于模型**产生的显式推理轨迹（thinking trace）**，还是源于**RLVR训练范式本身**的更广泛的优化作用？
- **研究动机**：模型在生成翻译前进行推理会产生大量的额外 tokens，导致推理延迟和计算成本显著增加。因此，需要在**翻译质量的提升**与**增加的推理成本**之间寻找最优平衡点，明确推理步骤在训练和推理阶段的实际必要性。

### 2. 论文提出的方法论：核心思想、关键技术细节

本研究的核心是设计了一套**系统性消融实验**，通过在不同阶段禁用模型的推理过程，来测量其对翻译质量和推理成本的影响。

- **核心思想**：将“推理”视为一个可以被独立开启或关闭的模块，分别在**训练阶段**（T）和**推理阶段**（I）进行控制，以观察其作用。
- **关键技术细节（推理的开关控制）**：
    - **开启推理**：训练和推理时，要求模型将思考过程置于 `<think></think>` XML标签内，将最终翻译置于 `<translation></translation>` 标签内。
    - **关闭推理**：
        - **训练时**：通过结构奖励，强制要求 `<think></think>` 标签为空。
        - **为提升效率**：训练时通过预填充空标签来实现。
        - **推理时**：直接禁用模型的思考模式。
- **训练方法**：使用**群组相对策略优化（GRPO）** 算法，采用低秩适配（QLoRA）技术微调模型。
- **成本估算**：采用经济学方法，基于GPU硬件折旧、功耗和维护费用，估算每小时运行成本，再乘以任务总执行时间，得出最终推理成本。
  - 核心公式：`C_GPU,hour ≈ C_depreciation + C_power + C_maintenance`

### 3. 实验设计：数据集、基准、对比方法

- **数据集/场景**：使用 **SwiLTra-Bench（瑞士法律翻译基准）** 数据集，专门聚焦于法律领域的翻译任务。
- **基准模型**：使用 **Qwen3.5** 系列的 4B 和 9B 参数模型。
- **评估指标**：主要评估翻译质量，重点关注 `COMET` 分数（因其与人类专家判断相关性最高），同时辅以 `chrF`， `METEOR`， `MetricX` 等指标。成本方面则评估训练与推理的预估美元成本和生成的 token 数量。
- **对比方法/实验配置**：在训练和推理两个维度上排列组合，形成四种配置：
    1.  `[T: ✗, I: ✗]`（训练无推理，推理无推理）：不包含推理的基线模型。
    2.  `[T: ✗, I: ✓]`（训练无推理，推理有推理）：训练时不包含推理，但推理时启用。
    3.  `[T: ✓, I: ✗]`（训练有推理，推理无推理）：训练时包含推理，但推理时禁用。
    4.  `[T: ✓, I: ✓]`（训练有推理，推理有推理）：完全包含推理的设置。

### 4. 资源与算力

- 论文**未明确说明**训练和推理所使用的具体GPU型号、数量及总训练时长。
- 论文提到使用了优化库（`unsloth`用于训练，`vLLM`用于推理）以最大限度地减少训练和推理时间，并给出了基于标准化的`0.79美元/小时`成本估算出的总费用（例如，4B模型训练费用在`$5.46`至`$12.56`之间），但缺乏详细的硬件参数支撑。

### 5. 实验数量与充分性：是否客观、公平

- **实验充分性**：实验设计较为系统。论文主要进行了两组实验：
    1.  **训练样本数量与质量的关系**：分析不同训练样本量（每100个样本保存一次检查点）下各配置的翻译质量变化曲线。
    2.  **最终模型的全方位对比**：对两个不同尺寸的模型（4B和9B）在全部四种配置下的最佳检查点，于完整的测试集上进行最终评估。
- **公平性**：实验对比公平。在相同的基础模型、数据集、训练算法（GRPO + QLoRA）下，唯一变量是“推理”模式在不同阶段的有无。对成本的分析同时涵盖了训练和推理阶段，并考虑了输出 token 数的直接影响，对比维度全面。

### 6. 论文的主要结论与发现

- **质量结论**：
    - 在**推理阶段包含推理**总能提升翻译质量，无论模型在训练时是否使用推理。
    - 最佳的翻译质量由 `[T: ✓, I: ✓]`（训练和推理均包含推理）配置的 Qwen3.5 9B 模型取得（COMET得分 82.50）。
    - 最差的配置是 `[T: ✓, I: ✗]`（训练时启用了推理，但推理时禁用），其质量甚至低于完全不推理的基线模型 `[T: ✗, I: ✗]`。
- **成本-质量权衡结论**：
    - **核心矛盾**：训练时未包含推理，但在推理时启用 `[T: ✗, I: ✓]` 的模型，会产生大量无意义的推理 token，导致推理成本剧增（最高达230%），但翻译质量并非最佳。
    - **最优权衡**：从成本效益角度看，**训练时包含推理 `[T: ✓]` 的关键作用是“驯化”思考过程**，使其产生的推理 token 更紧凑、高效，从而在获得最佳翻译质量的同时，大幅降低推理时的 token 开销（相比训练时无推理的模型，推理 Token 减少可达 70%）。
    - **训练数据量**：对于 SwiLTra-Bench 数据集，大约 **1500 个训练样本**即可让翻译质量达到平台期，之后增加样本带来的边际收益递减而成本高昂。
- **最终建议**：为达到最佳的成本-质量比，应**在训练和推理阶段保持推理模式的一致性**：要么完全不用，要么全程使用。但若选择全程使用，必须在训练阶段就引入推理，以控制推理成本。

### 7. 优点：方法或实验设计上的亮点

- **问题导向清晰**：直接回应了关于 RLVR 增益来源（是“推理”还是“范式”）的关键争议，研究目标明确。
- **实验设计巧妙**：通过简单的排列组合消融实验，将“推理”从 RLVR 训练范式中解耦，清晰地揭示了其在训练和推理两个阶段的不同作用。
- **分析维度全面且务实**：不仅关注质量指标，更引入了经济学视角，将从 token 数量增长到金钱成本的链条量化，将研究从学术探讨引向实际部署的决策层面，是对 AI 应用成本效益分析的一个很好的示范。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖的局限性**：
    - **模型与任务单一**：仅在 Qwen3.5 系列模型和法律翻译这一特定任务上进行实验，结论能否推广到其他模型家族、其他规模及通用领域翻译任务，仍有待验证。
    - **训练数据量研究不完整**：虽研究了样本量的影响，但认为增加样本“不大可能”带来不成比例的提升，这一论断缺乏足够实验证明。其建议的 1500 个样本的结论仅适用于当前特定设置。
- **分析深度的不足**：
    - 对于为何训练/推理阶段推理模式**不匹配会导致质量下降或成本剧增**的内在机制，论文缺乏深入分析，仅停留在现象描述。
    - 没有对模型生成的推理 trace 内容进行质量评估或分析，例如：有效的推理是什么样的？不同配置下产生的“推理”有何本质区别？
- **泛化性与偏差风险**：
    - **成本估算粗略**：采用标准化的“x 美元/小时”和固定公式，而实际情况高度依赖硬件型号、功耗、折旧年限和当地电价，论文结论仅可作为参考，无法直接套用。
    - **基准局限性**：完全依赖 SwiLTra-Bench 数据集，存在数据集偏差的风险，结论可能对该数据集的特性（如语言对、文本类型）过拟合。

（完）
