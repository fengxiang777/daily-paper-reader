---
title: "MMAgent-R$^2$: Learning to Rerank and Reject for Agentic mRAG"
title_zh: MMAgent-R²：学习为代理式 mRAG 进行重排序与拒绝
authors: "Tao Zhang, Ziqi Zhang, Zongyang Ma, Yuxin Yang, Bing Li, Chunfeng Yuan, Kang Rong, Fengyun Rao, Jing Lyu, Weiming Hu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07383v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 面向知识型视觉问答的智能体式多模态检索增强生成系统MMAgent-R^2
tldr: 知识型视觉问答中，多模态检索增强生成依赖全局特征匹配，面临视觉相似实体干扰和固定候选集导致误差扩散问题。本文提出代理式框架MMAgent-R^2，内建视觉重排序与主动拒绝双重验证：重排序直接比对图像捕捉判别细节，主动拒绝在低置信度时抛弃结果并重新检索新候选。经步级验证奖励和GRPO训练联合优化检索、验证与生成，在InfoSeek、E-VQA、MMhops上性能最优，特别在困难检索和多跳推理中增益显著，表明此机制有效阻断错误传播，提升多模态问答的准确性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07383v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 858, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 730, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07383v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1257, \"height\": 649, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07383v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 844, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 611, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 644, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 666, \"height\": 179, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 512, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 583, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 538, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07383v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 956, \"height\": 344, \"label\": \"Table\"}]"
motivation: 现有mRAG依赖全局视觉特征检索，在视觉相似实体多时产生大量干扰项，固定候选集导致检索错误传播。
method: 提出MMAgent-R^2代理式框架，集成视觉重排序直接比较图像捕捉判别细节，与主动拒绝丢弃低置信结果并重新检索，通过GRPO训练和步级验证奖励联合优化检索、验证与生成。
result: 在InfoSeek、E-VQA、MMhops上取得SOTA性能，尤其在困难检索场景和复杂多图像多跳推理中优势显著。
conclusion: 该框架有效抑制了检索误差的传播，显著提升了多模态问答系统在复杂场景下的鲁棒性和准确性。
---

## 摘要
基于知识的视觉问答（KB-VQA）要求模型从大规模百科知识库中检索与查询图像匹配的视觉实体，并回答相关问题。现有的多模态检索增强生成（mRAG）方法依赖全局视觉特征来匹配候选实体，然而当知识库中存在大量视觉相似的实体时，检索器难以区分它们，导致候选集中充斥着视觉相似但事实不符的干扰项。由于后续处理步骤（如噪声过滤）也局限于这一固定候选集，检索失败的误差将不可避免地传播到最终答案。为应对这些挑战，我们提出 MMAgent-R²，一个将视觉重排序和主动拒绝作为内部验证机制的代理式 mRAG 框架。视觉重排序直接比较查询图像与候选图像，捕捉文本描述之外的判别性细节，从而在相似候选中精确识别目标实体；主动拒绝则在找不到可靠匹配时丢弃不可靠结果并检索额外的候选实体，突破固定候选池的限制。我们设计了一种带有步骤级验证奖励的复合奖励函数，并通过 GRPO 训练实现了外部检索、内部验证和答案生成的联合优化。在 InfoSeek、E-VQA 和 MMhops 上的实验表明，我们的方法达到了最先进的性能，在具有挑战性的检索场景和复杂的多图像多跳推理任务中优势尤为显著。

## Abstract
Knowledge-based Visual Question Answering (KB-VQA) requires models to retrieve visual entities matching the query image from large-scale encyclopedic knowledge bases and answer related questions. Existing multimodal Retrieval Augmented Generation (mRAG) methods rely on global visual features to match candidate entities, yet when the knowledge base contains numerous visually similar entities, the retriever struggles to distinguish them, populating the candidate set with visually similar but factually mismatched distractors. Since subsequent processing steps such as noise filtering are also confined to this fixed candidate set, errors from failed retrieval inevitably propagate to the final answer. To address these challenges, we propose MMAgent-R$^2$, an agentic mRAG framework that integrates visual reranking and active rejection as its internal verification mechanism. Visual reranking directly compares query and candidate images, capturing discriminative details beyond textual descriptions to precisely identify the target entity among similar candidates; active rejection discards unreliable results and retrieves additional candidates when no confident match is found, moving beyond the fixed candidate pool. We design a composite reward function with step-level verification rewards and achieve joint optimization of external retrieval, internal verification, and answer generation via GRPO training. Experiments on InfoSeek, E-VQA, and MMhops demonstrate that \ours{} achieves state-of-the-art performance, with particularly notable advantages in challenging retrieval scenarios and complex multi-image multi-hop reasoning tasks.

---

## 论文详细总结（自动生成）

好的，以下是针对论文《MMAgent-R$^2$: Learning to Rerank and Reject for Agentic mRAG》的结构化深度分析总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：论文聚焦于基于知识的视觉问答任务。现有主流方法是多模态检索增强生成，其范式为“先检索，后处理”。
*   **核心痛点**：
    *   **识别瓶颈**：当知识库中存在大量视觉相似的实体时，依赖全局视觉特征的检索器难以区分，会召回大量视觉相似但事实不符的候选（干扰项）。
    *   **误差传播**：后续的处理（如文本重排序）被限制在这个固定的、可能包含错误的候选池内。一旦检索失败，正确答案不在候选集中，误差将不可避免地传递到最终答案。
*   **整体含义**：为解决上述问题，论文提出了一个名为 `MMAgent-R²` 的代理式框架。其核心思想是赋予模型**主动验证与纠错**的能力，让模型不仅能检索，还能通过深入的视觉比对（重排序）和动态调整检索范围（拒绝）来确保所用知识的准确性。

### 2. 方法论

*   **核心思想**：将推理过程建模为一个多轮智能体决策过程，其中视觉语言模型作为智能体，其内部集成了“外部检索”、“内部验证”和“答案生成”三大行动。
*   **关键技术细节**：
    *   **内部验证机制**：这是核心创新，由两部分组成。
        *   **视觉重排序**：模型不再依赖文本描述，而是直接、细致地**比对查询图像与候选图像**的视觉细节，从而在相似候选者中精确选出匹配的实体。这是一个 `<rerank>` 行动。
        *   **主动拒绝**：当模型判定当前批次的候选图像中**没有**可靠匹配时，会主动执行 `<reject>` 行动，触发环境提供下一批（不重叠的）候选图像。这打破了固定候选池的限制。
    *   **行动空间**：定义了代理可执行的操作，包括`搜索图像`、`搜索文本`、`重排序`、`拒绝`和`生成答案`。
    *   **训练方法**：
        *   采用**群体相对策略优化**算法进行强化学习。
        *   设计了一个**复合奖励函数**：`总奖励 = 结果奖励 + 格式奖励 + 验证奖励`。
            *   **结果奖励**：最终答案是否正确。
            *   **格式奖励**：输出是否符合语法结构。
            *   **验证奖励**：这是关键。它为每一次重排序或拒绝的决策提供**步骤级的密集监督信号**。公式鼓励模型在正确实体出现时进行重排序，在正确实体未出现时进行拒绝，惩罚错误的识别或错误的拒绝。
    *   **算法流程**：模型以“*推理-行动-反馈*”的模式循环运作。
        1.  代理接收多模态输入和问题。
        2.  自主决定下一个行动（搜索图像/文本，或进行内部验证）。
        3.  环境执行行动并返回结果（图像批次、文本段落等）。
        4.  重复步骤2-3，直到模型有足够信心生成最终答案或达到最大轮数。

### 3. 实验设计

*   **数据集/场景**：
    *   **InfoSeek**：包含约130万数据，涵盖1.1万个维基百科实体，评估在未见问题和未见实体上的泛化能力。
    *   **Encyclopedic-VQA (E-VQA)**：包含约100万数据，有1.67万个细粒度类别，具有最大的知识库（200万维基百科页面）和最高的检索难度，包含单跳和双跳推理。
    *   **MMhops**：专门为多模态多跳推理设计的基准，包括单图链式推理和多图比较推理，所有样本需要3-4步推理。
*   **Baseline方法对比**：与三类方法进行了全方位比较。
    *   **直接回答**：仅依赖模型自身参数知识的VLM（如GPT-4V， Qwen2.5-VL）。
    *   **多模态RAG**：多种采用“检索-后处理”管线的先进方法（如EchoSight， ReAG， QKVQA等），这些方法主要在文本层面进行后处理。
    *   **代理式mRAG**：同样采用多轮推理调用工具的同期工作（如MMhops-R1），以验证主动视觉验证的必要性。

### 4. 资源与算力

*   论文明确提到了其实现和训练的资源配置：
    *   **基础模型**：Qwen2.5-VL-7B 和Qwen3-VL-8B。
    *   **检索器**：视觉检索使用 EVA-CLIP-8B；文本检索使用 E5。
    *   **训练框架与算法**：基于 `verl` 框架，使用 GRPO 算法。
    *   **训练数据**：从三个数据集的训练集中分别采样了65K、67K和17K共约14.9万条数据进行训练，而非使用全量数据。
    *   **训练超参**：全局批次大小为1024，训练2个周期，每个实例采样8条（`G=8`）回滚轨迹，学习率为 1e-6。
    *   **未明确信息**：论文未提及具体使用了何种型号及数量的GPU，也未提及总训练时长。

### 5. 实验数量与充分性

*   **实验数量**：实验组数非常丰富，主要包括：
    *   **主实验（3个数据集 x 2种基础模型）**：在InfoSeek, E-VQA, MMhops上与十余种SOTA方法进行全面对比。
    *   **实体识别准确率实验**：单独评估模型识别实体的能力，并与检索器的召回率对比。
    *   **消融实验**：围绕核心组件（重排序、拒绝）、候选批次大小、最大拒绝次数、拒绝奖励权重等关键超参数进行了系统的控制变量研究。
    *   **定性分析**：提供了多个具体案例的可视化分析。
*   **充分性与公平性**：
    *   **充分性**：实验设计极为全面，从宏观性能到微观机制，从量化分析到具体案例，覆盖了所有关键维度，充分验证了所提方法的有效性。
    *   **客观公平性**：对比的基线方法覆盖了当前研究的主流范式，且大多数是在相同或类似的检索器（如EVA-CLIP-8B）下报告的。所有评估均使用标准指标，公平性较高。

### 6. 主要结论与发现

*   **整体SOTA性能**：`MMAgent-R²` 在三个知识型VQA基准测试上均取得了最先进的性能。
*   **在困难场景下优势显著**：在检索难度最高（E-VQA，知识库200万）和推理最复杂（MMhops，多图多跳）的任务上，性能提升幅度最大，证明了视觉验证机制的有效性。
*   **机制的有效性得以验证**：
    *   **视觉重排序是主要驱动力**，能精确区分视觉相似实体。消融实验显示，加入重排序使实体识别准确率大幅提升。
    *   **主动拒绝提供额外增益**，通过扩大候选覆盖范围来解决检索器召回失败的问题。两种机制分别从精度和覆盖率两个互补维度提升了性能。
    *   **拒绝机制被合理利用**：训练后的模型会按需拒绝，而非无限制地扩张，平均拒绝次数远低于设定的上限。

### 7. 优点

*   **创新性方法论**：首次在代理式mRAG框架中将“视觉重排序”和“主动拒绝”作为内部验证机制，将后处理从文本层面提升到了视觉层面，是显著的创新。
*   **精巧的训练设计**：提出的**步骤级验证奖励**是一个亮点，它为内在的、非终端的行为（重排/拒绝）提供了细粒度的强化学习信号，使模型学会了何时选择、何时拒绝，解决了稀疏奖励难以训练此类复杂行为的问题。
*   **优异的实验结果**：在多个有挑战性的基准上取得了远超同期工作的性能提升，尤其是在细粒度实体和多跳推理任务上，实验结果极具说服力。
*   **框架的简洁与鲁棒性**：整个流程通过强化学习内化到单一VLM中，无需依赖额外的模块（如独立的评判模型），架构优雅，且能灵活处理多图输入和多跳推理。

### 8. 不足与局限

*   **性能受限于检索器**：模型能正确识别的实体上限仍受制于初代检索器的召回率。如果正确答案完全无法被检索器体系覆盖，后续的验证机制也无能为力。实验数据也显示，模型识别准确率与检索器R@15上界仍有差距。
*   **推理成本高**：代理式的多轮交互以及将多张候选图像注入上下文，会带来显著的额外Token开销和推理延迟。
*   **未与检索器联合优化**：当前的检索器是冻结不动的，验证与检索是分离的。如果能对两者进行联合优化，可能会进一步提升性能上限。
*   **算力成本未知**：论文未披露训练所需的GPU资源和时间，这使得他人难以精确评估和复现其训练成本。

（完）
