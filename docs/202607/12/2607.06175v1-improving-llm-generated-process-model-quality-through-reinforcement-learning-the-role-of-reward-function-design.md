---
title: "Improving LLM-Generated Process Model Quality Through Reinforcement Learning: The Role of Reward Function Design"
title_zh: 通过强化学习提升大语言模型生成的过程模型质量：奖励函数设计的作用
authors: "Alexander Rombach, Chantale Lauer, Nijat Mehdiyev"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06175v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 使用基于多维奖励函数的强化学习提升LLM生成流程模型的质量
tldr: 大型语言模型生成BPMN过程模型时，监督微调受限于训练数据质量。本研究系统探索强化学习中奖励函数设计，通过48种配置训练两类模型，发现等权重奖励全面优于针对性加权，且奖励组成与模型架构存在复杂交互。该方法显著提升句法和语用质量，降低输出变异性，为多维质量优化的结构化生成任务提供通用启示。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06175v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 1090, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1797, \"height\": 1071, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1793, \"height\": 792, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1791, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06175v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1790, \"height\": 629, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06175v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 657, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 378, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 621, \"height\": 499, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 795, \"height\": 696, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1800, \"height\": 650, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06175v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1804, \"height\": 833, \"label\": \"Table\"}]"
motivation: 监督微调使LLM生成的过程模型质量受限于训练数据模式，需通过强化学习突破上限，但多维质量下的奖励设计方法不明。
method: 以Llama 3.1 8B和Qwen 2.5 14B为基座，采用组序列策略优化，在38项自动评估指标衍生的句法、语用、语义奖励组合下进行48种配置实验。
result: 强化学习大幅提升句法和语用质量，降低变异性超六倍；等权重奖励优于针对性加权，且无效惩罚与SFT初始化的作用因模型架构而异。
conclusion: 奖励组成是优化效果的核心决定因素，其影响可比肩是否使用强化学习，该发现适用于所有需多维自动评估的结构化生成任务。
---

## 摘要
大语言模型能够从自然语言描述中生成BPMN过程模型，但监督微调将其输出质量限制在训练数据中的模式范围内。强化学习可利用外部质量度量突破这一上限，然而当质量是多维时，奖励函数应如何设计仍有待探索。我们对基于强化学习的过程模型生成的奖励函数设计进行了系统性研究，使用分组序列策略优化，在两种LLM系列（Llama 3.1 8B，Qwen 2.5 14B）上训练了48种配置，奖励来自包含38个指标的自动化评估框架，涵盖句法、语用和语义质量。得出三个发现。首先，强化学习显著提高了语用和句法质量，同时保持了语义保真度，将输出变异性降低了六倍以上。其次，平均奖励权重始终优于针对性加权：强调特定维度并不能改善该维度，反而可能使模型陷入低质量模式。第三，设计选择与模型架构以非平凡的方式相互作用：无效性惩罚对一个模型至关重要，而对另一个模型则无关紧要；SFT初始化对一个架构不可或缺，但对另一个架构则适得其反。这些结果表明，奖励构成是优化结果主要决定因素，其影响与决定应用强化学习本身一样大。这些发现可推广到任何质量需沿多个自动化维度评估的结构化生成任务。我们在https://github.com/chlauer99/RL_for_process_modeling 上发布了实现和实验代码。

## Abstract
Large language models (LLMs) can generate BPMN process models from natural-language descriptions, yet supervised fine-tuning (SFT) limits their output quality to the patterns present in the training data. Reinforcement learning (RL) can optimize beyond this ceiling using external quality measures, but how the reward function should be designed when quality is multi-dimensional remains unexplored. We present a systematic investigation of reward function design for RL-based process model generation, training two LLM families (Llama~3.1 8B, Qwen~2.5 14B) under 48 configurations using Group Sequence Policy Optimization with rewards derived from an automated evaluation framework comprising 38 metrics across syntactic, pragmatic, and semantic quality. Three findings emerge. First, RL significantly improves pragmatic and syntactic quality while preserving semantic fidelity, reducing output variability by more than sixfold. Second, equal reward weighting consistently outperforms targeted weighting: emphasizing a specific dimension fails to improve it and can collapse the model into a low-quality mode. Third, design choices interact with model architecture in non-trivial ways: the invalidity penalty is essential for one model but irrelevant for the other, and SFT initialization is indispensable for one architecture but counterproductive for another. These results demonstrate that reward composition is a primary determinant of optimization outcomes, with effects as large as the decision to apply RL itself. The findings generalize to any structured generation task where quality is assessed along multiple automated dimensions. We release our implementation and experimental code at https://github.com/chlauer99/RL_for_process_modeling.

---

## 论文详细总结（自动生成）

# 论文核心问题与整体含义

- 本文聚焦于用大语言模型（LLM）从自然语言描述自动生成 BPMN（业务流程模型与标注）过程模型。
- 监督微调（SFT）只能模仿训练数据的模式，无法超越其质量天花板；强化学习（RL）则可以利用外部、多维度的质量度量（句法、语用、语义）进行优化。
- 核心缺口：当过程模型质量呈多维度时，强化学习的奖励函数应如何设计？各质量维度的权重、惩罚项等如何影响最终优化效果？此前尚无系统研究。
- 整体含义：该工作不仅针对 BPMN 生成，更为任何需要沿多个自动化维度评估质量的**结构化生成任务**提供奖励函数设计的一般性启示。

# 方法论

- **核心思想**：将过程模型生成视为由多维度质量指标驱动的强化学习问题，通过精细设计奖励函数的组成（权重、无效性惩罚）来优化 LLM 输出，突破 SFT 的质量天花板。
- **训练管道**：
  - **第一阶段（可选）**：监督微调（SFT），使用 JSON 格式的过程模型作为目标输出，优化 token 级模仿损失。
  - **第二阶段**：组序列策略优化（GSPO）。为每个提示采样 K=4 个候选输出，经奖励函数评分后，利用组内归一化的相对优势进行策略更新，避免对奖励绝对尺度的敏感，并适应序列级输出评估。
- **奖励函数设计**：
  - 基础：由 BEF4LLM 评估框架自动计算三个维度的质量分数：句法质量（16 项规则指标）、语用质量（15 项复杂度指标）和语义质量（7 项相似度指标）。
  - 统一公式：`R(w, p) = w_syn * r_syn + w_pra * r_pra + w_sem * r_sem` （若输出有效），否则给予固定惩罚 `p`。
  - 六种奖励配置：
    - **R_avg**：均等权重（3:3:3），惩罚 p = -1，聚合标量路径。
    - **R₁**：均等权重，惩罚 p = -1，分解后加权路径（与 R_avg 数学等价，但实现不同，用于检查实现路径影响）。
    - **R₂**：均等权重，惩罚 p = 0（无负惩罚）。
    - **R₃、R₄、R₅**：针对性加权 3:1:1，分别强调句法、语用、语义，惩罚 p = -1。
- **其他技术细节**：均采用 LoRA 低秩适配（SFT 阶段 rank=8，GSPO 阶段 rank=16）以保持参数效率；引入语法约束解码作为独立的实验因子（仅限推理时，不改变策略）。
- **评估与统计**：所有模型在同一 105 样本的 held-out 集上评估，仅有效输出参与质量计分；采用配对置换检验 + Bonferroni 校正进行显著性分析。

# 实验设计

- **数据集**：
  - 训练集：1,552 个文本–过程模型对（多源混合，含合成扩充）。
  - 评估集：105 个手动验证的文本–BPMN 模型对，独立于训练集。
- **场景与任务**：英文和德文的文本到 BPMN 模型生成，要求输出为紧凑 JSON 后再译成 BPMN XML。
- **对比方法**：
  - **基线**：仅经过 SFT 的模型（SFT-only），不进行 RL。
  - **RL 配置矩阵**：对两个模型家族（Llama 3.1 8B Instruct、Qwen 2.5 14B Instruct）分别评估：
    - 两种基本初始化：SFT 初始化 vs. 未经 BPM 适配的指令微调基座（untrained）。
    - 六种奖励函数（R_avg, R₁–R₅）。
    - 两种推理时语法约束（启用/禁用）。
    - 共 2×6×2 = 24 种配置 / 模型，合计 **48 种 RL 训练的配置**。
- **基准指标**：BEF4LLM 提供的句法、语用、语义三项得分（均在 [0,1] 区间），以及有效输出数量。

# 资源与算力

- 论文未明确说明使用的 GPU 型号、数量、训练时长或显存消耗。
- 仅提到使用 **bfloat16 精度**、**LLaMA-Factory**（SFT）和 **Unsloth**（GSPO）框架、**paged AdamW 8-bit** 优化器，并给出了具体的批次大小、学习率等超参数。
- 因此，无法从文中获知确切的算力开销。

# 实验数量与充分性

- **实验总量较大**：共计 48 组 RL 实验，外加 2 组 SFT-only 基线，评估覆盖多维质量，统计检验严格（Bonferroni 校正）。
- **设计公平且系统**：
  - 训练数据、评估集对所有配置保持一致。
  - 对奖励函数设计进行两个正交维度的消融：维度权重和无效性惩罚，同时控制实现路径（R_avg 与 R₁）。
  - 对比了两种模型架构、两种初始化策略，以及推理时语法约束，能揭示交互效应。
- **充分性评估**：实验数量足够支撑主要结论的稳健性；统计显著性检验控制了多重比较，有效样本量（105）虽不大，但效应量较大的发现仍足够显著。略微不足的是加权比固定为 3:1:1，且未测试更多权重梯度，但这些在设计上仍属可控且目的明确。

# 主要结论与发现

1. **GSPO 带动的多维质量提升**（RQ1）：
   - 在两个模型上，RL 均显著且大幅提高了**语用质量**（Δ>0.11）和**句法质量**（Llama 提升明显，Qwen 提升较小）。
   - **语义质量**：基本保持不变或仅轻微正向提升，未观察到牺牲语义质量换取其他维度的情形（在均等权重下）。
   - 最一致的效果是**输出变异性大幅下降**：语用质量标准差下降超六倍，生成模型的质量更加稳定可靠。

2. **奖励函数组成的关键作用**（RQ2）：
   - **均等权重（R₁）全面优于所有针对性加权方案（R₃–R₅）**。强调某一维度的奖励不仅未能提升该维度，反而常常使其下降，最严重时导致策略坍塌（句法质量暴跌 0.264）。
   - **无效性惩罚的作用依赖模型架构**：对 SFT 后有效性已很高的 Llama 几乎无影响；对 Qwen，去掉惩罚（R₂）会导致模型坍缩为单一模板（句法得分低且极窄，语用极高），惩罚实质上起到了隐式多样性正则化器的作用。

3. **初始化策略与架构的交互**（RQ3）：
   - 对 Llama，SFT 初始化**不可或缺**；直接从指令微调基座应用 GSPO 会导致句法质量崩溃（仅 0.664）。
   - 对 Qwen，SFT 初始化对句法和语用质量**基本冗余**，且与较低的语义质量相关；未经 SFT 的基座在 RL 后语义质量反而更高（0.601 vs. 0.567）。
   - 总体表明，SFT → RL 的管道并非万能，应视基座模型的任务先验能力而定。

4. **普遍含义**：奖励函数的构成方式对多维度结构化生成的影响与是否应用 RL 同等重要。等权重是稳健默认起始点；惩罚机制、初始化策略应在具体架构上经实证检验而非盲目套用。

# 优点

- **系统性与完整性**：首次对多维奖励函数设计进行了系统的实证研究，涵盖权重、惩罚、实现路径、初始化和语法约束多因子。
- **统计严谨**：使用非参数置换检验 + Bonferroni 校正，确保结论可靠。
- **可解释性强**：依赖可验证的自动化质量指标，奖励信号透明、可追踪，不易出现奖励黑客。
- **架构多样性**：同时在 Llama 和 Qwen 两种不同家族、不同规模的模型上验证，揭示交互效应，提高结论的泛化性。
- **实践指导价值**：明确给出了奖励设计、无效性惩罚和 SFT 初始化的工程建议，可直接指导类似的结构化生成任务。

# 不足与局限

- **评估集较小**（105 样本），且仅为单次生成评估，难以捕捉配置内部的方差，对小效应量的检验效力有限。
- **加权比固定**：只使用了 3:1:1 一种加权方案，更温和或动态的权重未探索，不能完全否定所有加权策略的可能性。
- **模型规模与家族混杂**：两个模型同时存在规模和预训练分布的差异，无法解耦参数规模与架构本身的影响。
- **RL 训练较保守**：GSPO 仅训练 1 epoch，虽为防过拟合，但也可能限制了更充分的优化；未测试多 epoch 或 KL 正则化策略。
- **语义质量的瓶颈**：基于图编辑距离、标签相似度的语义评估可能低估了“结构不同但语义等价”的有效生成，且其优化信号较弱，论文未能提出针对性改善措施。
- **算力与实现细节不透明**：未提供 GPU 型号、训练时长等信息，难以评估复现所需的资源门槛。

（完）
