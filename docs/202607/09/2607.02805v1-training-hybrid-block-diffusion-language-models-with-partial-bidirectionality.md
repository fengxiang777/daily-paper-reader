---
title: Training Hybrid Block Diffusion Language Models with Partial Bidirectionality
title_zh: 训练具有部分双向性的混合块扩散语言模型
authors: "Pranshu Chaturvedi, Parth Shroff, Tarun Suresh, Hangoo Kang, Kaiyue Wen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02805v1"
tags: ["query:rl-mm-llm-ag"]
score: 10.0
evidence: 通过结合注意力变体和块并行生成解决高吞吐长上下文生成
tldr: 长上下文生成受限于内存带宽，块扩散与线性注意力结合时反向扫描导致缓存复用困难。提出BDLM Mamba-H，通过将反向Mamba扫描限制在去噪块内实现跨块精确缓存。87M参数下验证困惑度达最优，长上下文推理吞吐量大幅领先：65K时19.7倍于DiffuMamba-H，262K时3.7倍于BDLM attention。证实Mamba混合是高效长上下文扩散架构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02805v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 801, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02805v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02805v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 708, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02805v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1448, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1117, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1452, \"height\": 786, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1451, \"height\": 256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1447, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02805v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1451, \"height\": 225, \"label\": \"Table\"}]"
motivation: 早期混合扩散如DiffuMamba因全序列反向扫描无法实现块级前缀缓存，制约吞吐量提升。
method: BDLM Mamba-H限制反向Mamba扫描于当前去噪块，确保跨块缓存精确复用，融合Mamba与注意力。
result: 87M模型C4困惑度最优，350M保持竞争力；长上下文吞吐量在65K/262K token时分别提升19.7倍和3.7倍。
conclusion: BDLM Mamba-H有效突破缓存瓶颈，大幅提升长上下文推理速度，展示了Mamba混合在扩散模型中的潜力。
---

## 摘要
高吞吐量长上下文生成是大语言模型的核心挑战之一。生成通常受内存带宽限制而非计算限制：每个解码步骤必须从内存中流式传输累积的键值（KV）缓存，因此带宽需求随上下文长度增长，而每次仅生成一个标记。因此出现了两种并行的方法：使用高效的注意力变体以及线性时间混合器（如Mamba）来减少内存访问，或通过一次生成多个标记来增加并行计算。然而，当将这两种思想结合时，会出现技术挑战。早期的混合扩散模型如DiffuMamba使用双向Mamba混合，包括相对于因果生成的反向扫描。这种反向扫描需要扫描整个序列，因此其状态并非仅前缀，即使逐块执行扩散，也无法精确地作为缓存重用。我们提出了一种BDLM Mamba-注意力混合模型，通过将反向Mamba扫描限制在活动去噪块内，从而实现了跨块的精确缓存，以应对这一挑战。在87M参数的DCLM扫描中，BDLM Mamba-H与BDLM注意力和全序列基线相比，取得了最佳的C4-en验证困惑度。在3.5亿参数规模下，它仍与BDLM注意力具有竞争力。对于长上下文推理，BDLM Mamba-H在65K标记时达到了全序列DiffuMamba-H吞吐量的19.7倍，在262K标记时达到了BDLM注意力吞吐量的3.7倍，表明Mamba混合模型是一种潜在的长上下文扩散架构。

## Abstract
High-throughput long-context generation is one of the central challenges for large language models. Generation is typically memory-bandwidth-bound rather than compute-bound: each decoding step must stream the accumulated key/value (KV) cache from memory, so bandwidth demand grows with context length while only one token is emitted. Two parallel approaches have therefore emerged: reducing memory access with efficient attention variants and linear-time mixers such as Mamba, or increasing parallel computation by generating blocks of tokens at once. However, technical challenges arise when combining these two ideas. Earlier hybrid diffusion models such as DiffuMamba use bidirectional Mamba mixing, including a reverse-direction scan relative to causal generation. This reverse scan needs to scan the entire sequence, so its states are not prefix-only and cannot be precisely reused as a cache even when diffusion is performed block by block. We propose a BDLM Mamba--attention hybrid that addresses this challenge by restricting the reverse Mamba scan to the active denoising block, which enables exact caching across blocks. In an 87M-parameter DCLM sweep, BDLM Mamba-H achieves the best C4-en validation perplexity compared to BDLM attention and full-sequence baselines. At 350M parameters, it remains competitive with BDLM attention. For long-context inference, BDLM Mamba-H reaches 19.7x the throughput of full-sequence DiffuMamba-H at 65K tokens and 3.7x the throughput of BDLM attention at 262K, showing that Mamba hybrids are a potential long-context diffusion architecture.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将以 Markdown 形式对这篇论文《Training Hybrid Block Diffusion Language Models with Partial Bidirectionality》进行结构化、深入、客观的总结。

### **1. 论文的核心问题与整体含义（研究动机和背景）**

- **核心问题**：大语言模型（LLM）长上下文生成的核心瓶颈在于内存带宽而非计算能力。传统的自回归解码需反复读取庞大的键值（KV）缓存，导致生成速度随上下文长度增加而急剧下降。
- **现有解决方案及其局限性**：
    - **方案一：高效架构**。采用Mamba等线性时间复杂度的序列混合器替代部分注意力层，以减少KV缓存的大小和访问开销。
    - **方案二：并行生成**。采用块扩散语言模型（BDLM）等技术一次生成多个Token，提高计算并行度。
    - **组合挑战**：直接将两者结合的早期模型（如DiffuMamba）采用全序列双向Mamba扫描。该反向扫描的状态依赖于整个（含未来Token的）序列，无法被精确缓存和复用，这违背了BDLM将已生成块作为固定前缀的缓存机制，阻碍了长上下文吞吐量的进一步提升。

### **2. 论文提出的方法论**

论文的核心思想是设计一种**原生支持缓存**的块扩散 Mamba-注意力混合架构，名为 **BDLM Mamba-H**。其关键技术创新点如下：

- **缓存对齐的训练目标（Cache-aligned training）**：训练时的目标函数与推理时的缓存结构完全一致。对每个块 k，模型会先扫描其干净的前缀 `b⋆(<k)` 生成可供后续复用的状态缓存 `Cℓ(c)`。后续块的去噪损失直接反向传播，训练生成该前缀缓存的前向扫描路径，确保推理时使用的缓存正是被训练优化过的对象。
- **块局部反向扫描（Block Partial-Reverse Scan）**：这是解决缓存冲突的核心方法。
    - **挑战**：DiffuMamba 对整个 `[前缀；当前噪声块]` 序列进行完整的双向扫描。其反向扫描状态依赖于待生成的噪声块，导致前缀状态不是独立的，无法作为缓存复用。
    - **解决**：BDLM Mamba-H 将反向 Mamba 扫描 **严格限制在当前活动的去噪块内部**，而前向扫描则从缓存的纯前缀边界状态 `sℓk` 继续。这样，当前块依然获得了块内的双向上下文信息，而可复用的前缀缓存仅依赖于干净的前缀，与后序的去噪过程解耦。具体公式如下：
        1.  **前缀缓存构建**：`sℓk = Fℓθ,→ (c(k))`，仅对前缀进行前向扫描，生成可复用的状态 `sℓk`。
        2.  **活动块内处理**：
            - **前向延续**： `y→,ℓk,t = Fℓθ,→ (˜b(k)t; sℓk)`，从缓存状态 `sℓk` 继续扫描当前噪声块。
            - **块内反向**： `y←,ℓk,t = rev (Fℓθ,← (rev (˜b(k)t)))`，仅对当前噪声块进行反向扫描。
        3.  **输出合并**： `yℓk,t = y→,ℓk,t + y←,ℓk,t`，融合双向信息。
    - **时间步调制分离**：为确保前缀缓存不依赖时间步 t，模型架构在实现时，将时间步相关的自适应层归一化（AdaLN）等调制操作，仅应用于活动块的计算路径，而前缀扫描路径则不包含任何时间步调制。

### **3. 实验设计**

- **数据集与评估基准**：
    - **训练数据**：使用来自 **DCLM** 数据集的50亿个Token进行训练。
    - **验证基准**：主要使用 **C4-en** 和 **Paloma-C4** 数据集，通过扩散模型的负证据下界（NELBO）计算困惑度（PPL）和每字节比特数（BPB）进行语言建模质量评估。
    - **下游任务基准**：使用 **MCQA**（多项选择问答）的伪对数似然准确率进行评测，包括 **HellaSwag, PIQA, ARC-E, ARC-C, BoolQ, WinoGrande** 六项任务。
- **对比方法**：在87M参数规模下进行了完整的对比，共四个模型：
    - **全序列注意力**（Full-sequence attention）：标准Transformer基线。
    - **全序列 DiffuMamba-H**（Full-sequence DiffuMamba-H）：全序列双向Mamba-注意力混合去噪器。
    - **BDLM 注意力**（BDLM attention）：标准BDLM架构。
    - **块部分反向 BDLM Mamba-H**（Partially Reverse BDLM Mamba-H）：本文提出的方法。

### **4. 资源与算力**

- **训练**：在 **8张NVIDIA A100-80GB** GPU上训练。87M和350M参数模型的全局批次大小分别为32和64。
- **推理**：在 **1张NVIDIA A100-80GB** GPU上评估，使用BF16精度，批次大小为1，对700M随机初始化的模型进行吞吐量测试。
- 论文未明确给出单次训练的总时间（如GPU小时），但提到了具体的优化器步数（87M模型为19,074步；350M模型为33,668步）和训练Token数。

### **5. 实验数量与充分性**

- **实验数量**：实验设计相对充分，覆盖了多个维度。
    - **87M参数模型扫参**：对4种不同架构，每种在5个学习率下进行训练，共计**20个模型**，并提供了完整的验证结果表格。
    - **模型缩放实验**：将表现最好的两个BDLM架构（BDLM Attention 和 BDLM Mamba-H）扩展到**350M参数**，验证其可扩展性。
    - **吞吐量基准测试**：对训练吞吐量和**11个不同长度**下的推理吞吐量进行了详细对比。
    - **消融研究**：补充了对时间步条件化（timestep conditioning）的分析，比较了有无时间步注入对模型性能的影响。
- **充分性与公平性**：实验对比公平，所有模型在相同的数据量、序列长度和评估协议下进行训练和验证。87M规模下的完整学习率扫描避免了因超参数选择不当导致的偏差。模型缩放实验使用了有理论依据的 `Complete(d)P` 规则进行学习率迁移，保证了对比的合理性。

### **6. 论文的主要结论与发现**

- **验证质量具竞争力**：在87M参数规模下，BDLM Mamba-H 在 C4-en 数据集上取得了最佳的NELBO困惑度（61.59），优于全序列基线（87.64）和 BDLM 注意力基线（76.46）。在350M规模下，其困惑度与 BDLM 注意力基本持平。
- **长上下文吞吐量大幅领先**：
    - 相比全序列去噪模型，在生成65K Token长度时，BDLM Mamba-H 的推理吞吐量是 DiffuMamba-H 的 **19.7倍**。
    - 相比同类的BDLM Attention架构，在生成16K Token后吞吐量开始领先，并在262K Token长度时达到其 **3.7倍** 的吞吐量，优势随序列变长而愈发明显。
- **训练与推理权衡**：BDLM Mamba-H 虽然推理速度快，但其训练吞吐量低于全序列模型，体现了训练成本与推理收益之间的权衡。

### **7. 优点**

- **方法创新性强**：提出的“块局部反向扫描”巧妙地解决了在BDLM框架下复用Mamba状态缓存的难题，是方法论上的一大亮点。
- **缓存对齐设计**：训练和推理在缓存使用上严格对齐，使得梯度能够直接优化用于推理的缓存状态，设计思想深刻且符合工程直觉。
- **实验扎实**：涵盖了从87M到350M的参数规模，从5亿Token到175亿Token的训练数据量，在多个验证集和下游任务上进行了评估，结论可信度高。
- **收益显著**：在长上下文推理吞吐量上取得了数量级的提升，证实了该方法在解决核心问题（内存带宽瓶颈）上的有效性。

### **8. 不足与局限**

- **规模受限**：实验的最大模型参数量为350M，训练数据量最大为175亿Token，远未达到当前主流LLM的规模，其结论在更大规模下的泛化能力有待验证。
- **训练效率问题**：论文坦诚指出，BDLM Mamba-H 在训练阶段比全序列模型慢25.5%，引入了额外的训练开销。
- **优化器状态迁移的适用性**：350M模型的缩放实验使用了为其他架构设计的 `Complete(d)P` 学习率迁移规则，作者承认这些规则并非为扩散语言模型“显式推导”，其最优性存疑。
- **架构和任务单一**：
    - 只测试了单一的Mamba-Attention混合架构，未探索其他线性注意力或状态空间模型的组合。
    - 下游任务评估仅局限于MCQA形式的伪对数似然评测，缺少生成质量、长文本理解等更全面的评估。

（完）
