---
title: Progress- and Reliability-Oriented Group Policy Optimization for Agentic Reinforcement Learning
title_zh: 面向主体强化学习的进度与可靠性导向的群组策略优化
authors: "Mingxuan Fan, Peiyang Liu"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04242v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 面向智能体强化学习的进度与可靠性导向的群体策略优化
tldr: 为解决步级别组RL中分组策略导致的优势估计偏差，本文提出无需学习评价器的ProGPO方法，通过保持精确前缀动作比较和融合多深度状态势估计来实现上下文一致的步级别学习。在ALFWorld和WebShop任务上，ProGPO性能优于现有基线，且计算开销相当。该方法为基于LLM的智能体RL训练提供了更可靠的步级别优化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04242v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1421, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1425, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1450, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04242v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1435, \"height\": 530, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04242v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 733, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1061, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04242v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 298, \"label\": \"Table\"}]"
motivation: 步级别组RL中分组策略影响优势估计：宽键分组导致历史不一致比较，历史一致性分组则同伴比较信号稀疏。
method: 提出ProGPO方法，无需学习评价器，通过保持精确前缀动作比较和基于语义扩展与逆方差融合的状态势估计来实现可靠的步级别学习。
result: 在ALFWorld和WebShop任务上，用Qwen2.5-1.5B-Instruct实验，ProGPO性能优于智能体RL基线，计算开销相当，并在3B模型上展示可扩展性。
conclusion: ProGPO为基于组的智能体RL提供了可靠且高效的步级别优化策略，在不增加计算负担下提升了性能，具有良好扩展前景。
---

## 摘要
基于群组的强化学习（RL）已成为提升大语言模型智能体在长时序交互任务中表现的有效范式。为获得比轨迹级优化更细粒度的策略更新，近期研究转向了步骤级群组RL，其中在一个 rollout 批次内对中间步骤进行分组和比较。然而，步骤级优势估计对分组方式敏感：按宽泛的状态键分组能提高覆盖度，但可能比较不同历史下采取的动作；而强制历史一致性虽能产生更公平的比较，却要以群组碎片化和缺失同行比较信号为代价。本文中，我们提出ProGPO（进度与可靠性导向的群组策略优化），一种无需学习型评论家、用于上下文一致的步骤级学习方法。ProGPO保持严格前缀动作比较，并利用基于 rollout 状态势能的转换信用来补充稀疏的同行比较。为可靠地估计这些势能，ProGPO将语义扩展与跨历史深度的逆方差融合相结合。我们在ALFWorld和WebShop两个具有挑战性的智能体任务上，使用Qwen2.5-1.5B-Instruct对ProGPO进行评估。结果表明，在类似的计算开销下，ProGPO较匹配的智能体RL基线有所提升，而额外的Qwen2.5-3B-Instruct实验进一步测试了所提方法的可扩展性。

## Abstract
Group-based reinforcement learning (RL) has become an effective paradigm for improving large language model agents on long-horizon interactive tasks. To obtain finer-grained policy updates than trajectory-level optimization, recent work has moved toward step-level group-based RL, where intermediate steps are grouped and compared within a rollout batch. However, step-level advantage estimation is sensitive to how groups are formed: grouping by broad state keys improves coverage but may compare actions taken under different histories, while enforcing historical consistency yields fairer comparisons at the cost of fragmented groups and missing peer-comparison signal. In this paper, we propose ProGPO (Progress- and Reliability-Oriented Group Policy Optimization), a learned-critic-free method for context-consistent step-level learning. ProGPO keeps exact-prefix action comparison, and complements sparse peer comparisons with transition credit derived from rollout-based state potentials. To estimate these potentials reliably, ProGPO combines semantic expansion with inverse-variance fusion across history depths. We evaluate ProGPO on two challenging agentic tasks, ALFWorld and WebShop, with Qwen2.5-1.5B-Instruct. Results show that ProGPO improves over matched agentic RL baselines under comparable computational overhead, and additional Qwen2.5-3B-Instruct experiments further test the scalability of the proposed method.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将以Markdown形式，对所提供的论文《面向主体强化学习的进度与可靠性导向的群组策略优化》进行结构化、深入、客观的总结。

### 1. 核心问题与研究动机

*   **核心问题**：在多步交互式智能体任务（如网页购物、具身家务）中，现有的步骤级群组强化学习方法（Step-level Group RL）面临一个两难困境。
    *   **宽泛分组**：若仅根据当前观测进行分组，能保证群组规模，但会将不同历史背景下采取的动作进行比较，导致优势估计产生偏差（不公平）。
    *   **历史一致性分组**：若强制要求只有共享相同历史前缀（上下文一致）的步骤才能比较，虽然保证了比较的公平性，但会导致群组严重碎片化，产生大量“独例”（singleton）或低对比度群组，使得许多步骤无法获得有效的同行比较学习信号。

*   **研究动机**：为了解决上述困境，研究旨在设计一种无需训练参数化评论家（Critic）的新方法，在保持上下文一致性（公平比较）的同时，为那些因缺乏同行比较而“信号稀疏”的步骤提供额外的、可靠的学习信号（进度信用），从而提升智能体的学习效率和最终性能。

### 2. 提出的方法论：ProGPO

ProGPO（进度与可靠性导向的群组策略优化）的核心思想是**将“动作比较”与“进度估计”解耦**：动作比较严格保持历史前缀一致以确保公平，进度估计则从整个批次的 rollout 统计中挖掘状态转换的潜在价值。其关键组成部分如下：

*   **前缀一致性步骤比较**：
    *   对于动作优势，ProGPO严格根据步骤的历史观测前缀（不同深度`k`）形成组$G^{(k)}$。
    *   仅当组内样本数≥2且回报方差非零时，才计算组内相对优势$A^{(k)}_t$（公式4）。
    *   最终，层次化地聚合所有有效深度下的优势，得到层次化优势$A^{hier}_t$（公式5）。

*   **状态转换评分**：
    *   这是ProGPO的核心创新，旨在为缺乏同行比较的步骤提供“进度信用”。它通过 **非参数化状态势能** 实现，即直接从当前批次rollout结果中估算状态的期望价值$\hat{V}(s)$，而无需训练Critic网络。
    *   **多分辨率融合**：ProGPO并非选择单一历史深度，而是将不同历史深度的前缀组视为一个价值估计器家族。对每个深度`k`，依据其样本数$n_k$和回报方差$\sigma^2_k$计算可靠性权重（类似精度），并加权融合多个深度的势能，得到$\hat{V}^{mr}(s_t)$（公式7、8）。这确保了采样充分、方差小的估计更具影响力。
    *   **语义相似度扩展**：当某个状态的同组样本过少时，ProGPO利用策略网络隐藏状态作为语义表征，检索前$K^{sem}$个语义相似的状态，共同计算加权势能$\hat{V}^{sem}(\bar{s})$（公式10），以稳定价值估计。此扩展**仅用于状态势能估计，动作比较仍保持严格前缀一致**。
    *   **转换信用**：基于融合后的状态势能，计算从一个状态到下一个状态的差值$A^{trans}_t = \hat{V}^{mr}(s_{t+1}) - \hat{V}^{mr}(s_t)$（公式9）。正值表示动作使智能体迈向更高潜力状态，即为正向的“进度信号”。

*   **互补信用组合**：
    *   最终的ProGPO优势函数$A^{ProGPO}_t$由层次化动作优势$A^{hier}_t$和转换信用$A^{trans}_t$组合而成（公式12）。一个门控机制$\eta_t$用于调节：当步骤已有有效的同行比较信号时，可降低转换信用的权重，避免信号冗余或冲突。
    *   策略优化目标是一个标准的带裁剪的替代目标，并加入KL散度惩罚项（公式13）。

### 3. 实验设计

*   **数据集/任务场景**：
    *   **ALFWorld**: 一个具身家务任务基准，包含拾取、清洁、加热等六类子任务，单次尝试（Episode）长达50步。
    *   **WebShop**: 一个模拟在线购物任务基准，要求智能体根据指令搜索和购买产品，单次尝试长达30步。

*   **基准模型**：主要实验使用`Qwen2.5-1.5B-Instruct`模型，可扩展性实验使用了`Qwen2.5-3B-Instruct`模型。

*   **对比方法**：
    *   **轨迹级方法**：GRPO。
    *   **步骤级方法**：GiGPO（基于当前状态分组）和 HGPO（引入层次化历史匹配分组）。
    *   **其他参考方法**：表1中还引用了Prompt-based方法（如GPT-4o, ReAct）、RL-based方法（PPO, RLOO）的历史报告数据，作为宏观参照，但主要结论基于本地重跑结果。

### 4. 资源与算力
*   **文中并未明确提及具体使用的GPU型号、数量以及训练总时长**。仅说明了训练配置，如批次大小（16）、组大小（8）、学习率（$1 \times 10^{-6}$），并使用`vLLM`进行rollout生成。算力开销是评估该方法实用性的一个缺失信息。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了多组实验，相对充分。
    *   **主要结果**：在2个数据集上，对4种RL方法（GRPO, GiGPO, HGPO, ProGPO）进行了本地重跑和对比，每个结果报告了3个随机种子的标准差。
    *   **可扩展性验证**：在更大参数量的模型（3B）上进行了训练趋势对比。
    *   **机制诊断**：通过图表和表格详细分析了信号稀疏性、转换信用的覆盖范围、多分辨率融合的使用频率等内部机制。
    *   **消融实验**：在ALFWorld任务上，针对状态势能估计的各个组件（语义扩展、可靠性融合）进行了消融研究。

*   **客观性与公平性**：
    *   **公平性较好**：主要的性能提升结论（ProGPO vs. GiGPO/HGPO）建立在“匹配的本地重跑”之上，控制了模型和训练配置等关键变量，比较公平。
    *   **充分性有局限**：消融实验（Table 3）仅在ALFWorld的最终训练步（step 160）上进行，且未报告标准差，其可靠性不如主实验。同时，对于多个新引入的超参数（$K^{sem}$, $\lambda^{tr}$, $\lambda^{fb}$等），缺少灵敏度分析。

### 6. 主要结论与发现

*   **性能提升**：ProGPO在ALFWorld和WebShop任务上均取得了优于现有多步骤级群组RL基线（GiGPO, HGPO）的成功率，在1.5B和3B模型规模下均表现出优势。
*   **信号互补机制有效**：实验诊断证明，纯粹上下文一致的同行比较会导致大量步骤（ALFWorld达44.5%）无学习信号，而ProGPO引入的转换信用有效补充了这一缺口，与同行比较信号覆盖了不同的训练阶段。
*   **组件均有贡献**：消融实验表明，语义扩展和基于逆方差的多分辨率融合对于稳定状态势能估计、提升转换信用质量都至关重要，二者结合效果最佳。

### 7. 优点

*   **巧妙的设计**：将动作比较的公平性与进度估计的信号密度需求成功解耦，思路清晰。在保持前缀一致分组优势（公平性）的同时，从现有数据中挖掘额外学习信号，无需修改环境或增加交互成本。
*   **无需学习型Critic**：通过非参数化的rollout统计和逆方差融合来估计状态势能，避免了训练一个额外价值网络带来的复杂性、不稳定性和资源开销，方法更简洁。
*   **实验扎实**：对比了多个强基线，报告了标准差，并在不同规模模型上验证，同时提供了深入的机制诊断和消融分析，体系较为完整。
*   **问题洞察深刻**：图1和图2对分组方式导致信号稀疏问题的可视化非常直观，精准指出了现有步级别方法的痛点。

### 8. 不足与局限

*   **实验覆盖范围有限**：
    *   **任务**：仅在ALFWorld和WebShop上测试，这两个环境的状态空间和交互复杂性可能无法代表更开放的智能体任务（如真实网页浏览、多模态交互）。
    *   **模型**：仅测试了Qwen2.5系列模型的两个较小规模版本（1.5B和3B），泛化到更大、更强的基础模型（如7B, 70B）上的效果未知。
*   **状态势能估计的固有风险**：
    *   **语义误匹配**：基于策略隐藏状态的语义扩展可能将表面相似但本质任务不同的状态错误关联，导致势能估计偏差。
    *   **非因果推断**：转换信用$\hat{V}(s_{t+1}) - \hat{V}(s_t)$是一个批次统计量，并非可靠的因果效应估计，可能混杂了环境随机性和表示误差。
*   **超参数与算力描述缺失**：未进行超参数灵敏度分析，也未报告具体的算力开销，这使得方法的鲁棒性和实用性评估不够完整。
*   **残留信号缺失**：论文承认，即使在ProGPO下，仍有相当比例的步骤信号为零（ALFWorld达33.1%），表明该方法并未完全解决信号稀疏问题。

（完）
