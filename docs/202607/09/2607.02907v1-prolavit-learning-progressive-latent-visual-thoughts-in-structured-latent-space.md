---
title: "ProLaViT: Learning Progressive Latent Visual Thoughts in Structured Latent Space"
title_zh: ProLaViT：在结构化潜在空间中学习渐进式潜在视觉思维
authors: "Peiming Li, Yifan Wang, Xiaotian Zhang, Zhiyuan Hu, Shiyu Li, Zheng Wei, Yang Tang"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02907v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 多模态大模型通过渐进式潜在视觉思维在连续潜空间进行结构化视觉推导
tldr: 多模态大语言模型在复杂视觉推理任务中仍需多步骤感知与逻辑推导，现有隐式思维方法或依赖外部专家、或缺少严谨的认知逻辑，且显式视觉生成计算开销大。ProLaViT提出通过渐进式隐式视觉思维进行结构化推导，利用模型自身的视觉编码器进行自蒸馏监督，并构建程序化合成管线以内涵算法精度；设计粗到细因果链与辩证推理链两种推理范式，辅以距离加权多样性损失保持特征语义区分。实验证明，ProLaViT在视觉中心基准测试中性能优于基线，并实现高精度和高效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02907v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1127, \"height\": 852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02907v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1242, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02907v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1191, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02907v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1264, \"height\": 487, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02907v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 635, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1271, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1270, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1024, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02907v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 880, \"height\": 618, \"label\": \"Table\"}]"
motivation: 复杂视觉推理要求多步感知与逻辑推导，而现有MLLM的显式视觉生成计算成本高，隐式方法又常缺少严谨认知逻辑或依赖外部模型。
method: ProLaViT利用内源自蒸馏在连续隐空间中学习结构化视觉思维，构建程序化合成管线，并设计从粗到细因果链与辩证推理链，辅以距离加权多样性损失防止特征退化。
result: ProLaViT在多项视觉中心基准上超越现有方法，取得了最优的准确率和可解释性，推理效率高。
conclusion: ProLaViT通过渐进式隐式视觉思维显著提升MLLM的复杂视觉推理能力，为隐式推理提供高效且可解释的新途径。
---

## 摘要
多模态大语言模型（MLLMs）取得了显著进展，但在需要多步感知和逻辑推导的复杂视觉推理任务上仍面临挑战。显式视觉生成会带来高昂的计算成本，而现有的潜在方法往往依赖外部专家或缺乏严谨的认知逻辑。在本文中，我们提出了 ProLaViT（渐进式潜在视觉思维）框架，它使 MLLMs 能够在连续的潜在空间中进行结构化的视觉推导。与依赖异构外部模型的工作不同，ProLaViT 利用内生的自我蒸馏机制，使用模型自身的视觉编码器来监督潜在思维。为此，我们构建了一个可扩展的程序化合成流水线，使模型无需推理时工具即可内化算法精度。我们设计了两种推理范式：（1）面向空间任务的粗到细因果链，将注意力从全局上下文引导至局部目标；（2）面向逻辑任务的辩证推理链，融入反事实思维进行验证。此外，我们提出了距离加权多样性损失以施加拓扑感知约束，通过强制语义区分性来防止特征退化。大量实验表明，ProLaViT 在视觉中心基准测试上优于基线方法，以高效率实现了卓越的准确性和可解释性。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable progress but still struggle with complex visual reasoning tasks requiring multi-step perception and logical deduction. While explicit visual generation incurs prohibitive computational costs, existing latent approaches often rely on external experts or lack rigorous cognitive logic. In this paper, we introduce ProLaViT (Progressive Latent Visual Thought), a framework empowering MLLMs to perform structured visual derivation in the continuous latent space. Unlike works dependent on heterogeneous external models, ProLaViT leverages an endogenous self-distillation mechanism, utilizing the model's own visual encoder to supervise latent thoughts. To facilitate this, we construct a scalable programmatic synthesis pipeline enabling the model to internalize algorithmic precision without inference time tools. We design two reasoning paradigms: (1) Coarse-to-Fine Causal Chain for spatial tasks, guiding attention from global context to local targets. (2) Dialectical Reasoning Chain for logical tasks, incorporating counter-factual thinking for verification. Furthermore, we propose a Distance-Weighted Diversity Loss to impose topology-aware constraints, preventing feature degeneration by enforcing semantic distinctiveness. Extensive experiments demonstrate that ProLaViT outperforms baselines on vision-centric benchmarks, achieving superior accuracy and interpretability with high efficiency.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义 (研究动机和背景)
多模态大模型（MLLMs）在复杂视觉推理任务（如多步空间感知、几何分析或逻辑推导）中仍存在局限。具体表现为两大瓶颈：
- **模态鸿沟**：纯文本的思维链（Textual CoT）推理过于抽象，难以捕捉精细的空间细节和几何关系，导致幻觉（如将“电视下方的柜子”错误识别为“木质地板”）。
- **效率与精度的两难**：显式生成中间图像（像素级）的方法计算成本过高；而现有隐式（潜在空间）推理方法往往采用“一步式潜在预测”，缺乏结构化推理，容易出现精度丧失和**潜在空间坍缩**（后续推理步骤的表征变得不可区分），导致推理退化为冗余。

ProLaViT的提出旨在解决这一挑战，其核心思想是：**在结构化潜在空间中执行渐进式视觉推导**，以低成本、高精度和强可解释性弥合模态鸿沟，使模型能够像人类一样进行“定位→聚焦→隔离”的逐步视觉思考。

### 2. 论文提出的方法论 (核心思想、关键技术细节、公式或算法流程)
ProLaViT的核心是在大模型的连续嵌入空间中，将复杂视觉任务分解为一个结构化的潜在思维序列，而非单步预测或文本推理。其方法论包含三大关键组件：

- **渐进式潜在视觉思维模块**
  设计两种针对不同任务的推理范式：
  - **粗到细因果链**：用于空间任务，遵循`定位(Locate)→聚焦(Focus)→隔离(Isolate)`的因果链。这在技术上进阶实例化为四个操作：网格视图（全局感知）、边界框（区域定位）、裁剪（细节聚焦）、分割（目标隔离）。每一步都由一组可学习的特殊tokens（`<|vit_pad_latent|>`）在LLM的嵌入空间中进行表征。
  - **辩证推理链**：用于逻辑任务（如拼图），遵循`假设(Hypothesize)→批判(Critique)→验证(Verify)`的试错循环，引入反事实思维。具体操作如边缘增强、方向引导、反事实模拟和方案验证。

- **内生自蒸馏机制**
  解决中间步骤缺乏真实标签的问题，其特点是**不依赖任何外部视觉专家模型**（如SAM、DINO）：
  - **程序化合成管线**：通过代码自动生成与每个推理步骤相对应的辅助图像（如根据坐标裁剪、分割后的图像），确保训练信号的算法精度。
  - **知识蒸馏**：将MLLM自身的、冻结的（frozen）视觉编码器作为**教师模型**。在训练时，LLM生成的潜在思维tokens通过一个交叉注意力层，去对齐教师模型从辅助图像中提取的特征。训练目标是最小化两者间的均方误差 \( \mathcal{L}_{\text{distill}} \)。

- **距离加权多样性损失**
  为防止推理链中后期步骤的表征趋同（潜在坍缩），引入一个损失函数 `L_{div}`，惩罚不同步骤tokens之间的余弦相似度。其创新之处在于**惩罚权重与步骤间的因果距离成正比**：
  - `w_{ij} = \epsilon + (1 - \epsilon) \cdot (d(i, j) / d_{max})^\alpha`，其中 `d(i,j)` 是步骤 `i` 和 `j` 的因果距离。这确保了相邻步骤（如网格视图和边界框）可以保留适度的相似性以维持上下文，而距离远的步骤（如网格视图和最终分割）则被强制要求表征更具差异化。

最终训练目标为：`L = L_{LM} + \lambda_{distill} L_{distill} + \lambda_{div} L_{div}`。训练采用四阶段课程学习策略，逐步引入锚点初始化、蒸馏损失和多样性损失，最后与通用VQA数据联合训练以防灾难性遗忘。

### 3. 实验设计 (数据集/场景，Benchmark，对比方法)
- **基准模型**：Qwen2.5-VL-7B-Instruct。
- **评测基准**：覆盖多个感知密集型视觉推理基准，以全面评估空间与逻辑能力：
  - MMVP， VisPuzzle， VStar， ChartQA， BLINK， CV-Bench， 以及专门用于评估拼图逻辑的 BLINK-Jigsaw。
- **对比方法**：
  - **文本空间推理**：标准监督微调（SFT）、文本思维链监督微调（CoT SFT）。
  - **潜在空间推理**：链式视觉思维（CoVT，依赖外部专家）、潜在视觉推理（LVR）、一步式潜在预测（自身消融变体）。
  - **ProLaViT自身变体**：无多样性损失、仅使用无距离权重的边缘多样性损失、完整距离加权多样性损失模型。

### 4. 资源与算力
论文在“实现细节”中明确说明了资源使用：
- **GPU型号与数量**：使用 `2 × NVIDIA H20 GPU`。
- **训练时长/步数**：采用两轮课程学习训练流程，总计约16,000步（第一轮6,000步，第二轮10,000步）。文中未提供具体的总训练小时数，但明确指出了训练的步数和阶段。
- **并行框架**：使用 DeepSpeed ZeRO-3 并行策略。基础模型（LLM和视觉编码器）在训练期间被冻结，仅更新LoRA适配器和一个轻量级投影层，显著降低了内存和计算开销。

### 5. 实验数量与充分性
论文设计了较为全面系统的实验，覆盖多个维度，具有很好的充分性、客观性和公平性：
- **主实验**：1个，在7项数据集上对比了1个基座模型与6种先进方法，展示了全面的性能提升。
- **消融实验**：至少5组，系统性地验证了核心设计：
  1.  **推理结构**：对比“一步式潜在预测”与“渐进式分解”，验证多步推导的必要性。
  2.  **多样性约束**：对比“无损失”、“边缘多样性损失”、“距离加权多样性损失”，验证后者对防止潜在坍缩、保留层级结构的关键作用。
  3.  **知识蒸馏来源**：对比“内生视觉编码器”与两个外部专家模型（DINOv2, SDXL-VAE），消除了依赖外部工具的领域鸿沟风险。
  4.  **推理步骤顺序**：通过随机打乱步骤顺序，验证了规范的因果顺序对性能的关键影响。
  5.  **推理模态**：将合成的视觉轨迹转换为文本轨迹进行训练，以量化潜在空间推理相比文本推理的净增益。
- **定性分析**：包含1组，通过可视化不同损失函数下的潜在tokens余弦相似度矩阵，直观展示了距离加权多样性损失如何成功构建了非冗余的渐进式表征。
- **效率分析**：1组，定量对比了ProLaViT与显式工具调用SFT在推理时延和TFLOPs上的差异，证明了其高效性。

### 6. 论文的主要结论与发现
- ProLaViT在多个感知密集型视觉推理基准上取得了卓越性能，平均准确率（75.11%）显著超过包括CoT SFT和依赖外部专家的方法（CoVT）在内的所有基线。
- **结构化渐进式推导优于单步预测**，尤其是在复杂任务（如VisPuzzle, BLINK-Jigsaw）上，验证了分解认知负荷的有效性。
- **内生自我蒸馏相较外部专家更鲁棒**。在图表问答（ChartQA）等高领域差异任务上，依赖外部专家的方法（如CoVT）性能会崩塌（24.99%），而ProLaViT能保持高性能（78.99%）。
- **距离加权多样性损失**是防止潜在空间坍缩、保持推理链拓扑结构清晰和逻辑进展性的关键，保证了每一步都贡献独特的感知信息。
- 方法在实现高性能的同时，保持了较高的推理效率，其推理时延和计算量远低于显式工具调用方法。

### 7. 优点（方法或实验设计亮点）
- **方法创新性**：开创性地将“渐进式认知过程”（粗到细、试错辩证）建模为结构化的潜在空间推理链，解决了隐式推理的结构性缺失问题。
- **机制优雅且独立**：“内生自蒸馏”是一个重大亮点，完全摆脱了对SAM、DINO等外部异构模型的依赖，消除了领域鸿沟和流程复杂性，使框架高度自洽和鲁棒。
- **问题洞察深刻**：敏锐地诊断出“潜在坍缩”这一隐式推理的关键失败模式，并提出了巧妙的“距离加权多样性损失”来解决，其设计符合直觉且效果优异。
- **实验全面扎实**：实验设计非常严谨，不仅覆盖了多个基准和基线，消融研究还系统性地分离并验证了每个核心组件（结构、距离权重、内生源）的独立贡献。定性可视化与定量分析的结合增强了说服力。
- **实用性强**：在保持高准确率的同时，通过操作于潜在空间而非像素空间，实现了显著的效率优势，使得该方法具备向实时应用拓展的潜力。

### 8. 不足与局限（实验覆盖、偏差风险、应用限制等）
- **任务与领域泛化性**：论文主要聚焦于静态图像的“空间和逻辑推理”（如视觉搜索、拼图、图表）。其提出的“结构化潜在推导”范式在更广阔领域的有效性尚未验证，如**时序视频推理、复杂几何变换、具身智能任务**。
- **推理范式的可扩展性**：文中的两种推理范式（粗到细和辩证链）是为特定任务设计的。对于其他类型的更开放或更复杂的推理任务，如何设计或自动发现新的结构化潜在思维链，仍是一个开放式问题。
- **对基础模型性能的依赖**：虽然内生蒸馏避免了外部专家，但其性能上限仍依赖于其自身视觉编码器的表征能力。如果基座模型的视觉编码器本身存在不足，可能会限制潜在思维的上限。
- **可解释性的局限**：虽然潜在空间中的逐步推导比黑盒预测更具解释性，但与生成可视化的中间图像或清晰的文本推理（尽管后者可能不准确）相比，其可解释性仍然相对抽象和隐含，难以直接用于人机交互验证。

（完）
