---
title: Distilled Reinforcement Learning for LLM Post-training
title_zh: 用于大语言模型后训练的蒸馏强化学习
authors: "Chen Wang, Zhaochun Li, Jionghao Bai, Yining Zhang, Hexuan Deng, Ge Lan, Yue Wang"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17247v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 提出蒸馏强化学习，将教师监督融入RL目标用于LLM后训练
tldr: "当前大模型后训练依赖强化学习或在线蒸馏，前者因粗粒度反馈难以分配贡献，后者因KL散度匹配易失效且难以迁移新知识。本文提出蒸馏强化学习，融合教师细粒度指导与RL目标，通过反向重要性采样、负样本重置和序列级归一化，选择性地转移新知识。该方法成功将教师未掌握的能力传递给子模型，在家族内和跨家族设置下均大幅超越标准RL及在线蒸馏，提升pass@1和pass@k指标。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17247v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1407, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1446, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 589, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1452, \"height\": 721, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 589, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17247v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 468, \"height\": 134, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 654, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1443, \"height\": 549, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1273, \"height\": 1550, \"label\": \"Table\"}]"
motivation: 大模型后训练中，强化学习信用分配困难且难以获取新知识，在线蒸馏难以有效利用差异较大的教师。
method: 提出蒸馏强化学习，通过带裁剪的反向重要性采样、负样本重置和序列级几何归一化，将教师监督融入RL目标。
result: "在家族内和跨家族蒸馏实验中，蒸馏强化学习在pass@1和pass@k上均显著优于标准RL和在线蒸馏。"
conclusion: 蒸馏强化学习实现了细粒度知识转移，突破了传统蒸馏的相似性限制，为LLM后训练提供了新范式。
---

## 摘要
大语言模型（LLM）的后训练对于提升推理、适应和对齐能力至关重要。现有方法主要遵循两种范式：强化学习（RL）和在线策略蒸馏（OPD）。然而，RL依赖于粗粒度的结果监督，导致信用分配困难且获取新知识的能力有限。同时，OPD通过KL散度无条件地匹配教师模型的logits，这造成了一个两难困境：相似的教师模型提供的新知识很少，而差异较大的教师模型往往产生无效的指导，这在很大程度上将OPD限制在同系列模型蒸馏内。我们提出了蒸馏强化学习（Distilled RL），它将教师监督集成到RL目标中，以提供细粒度的指导，选择性地迁移新知识并避免无条件模仿。蒸馏RL包含三个组件：带剪裁的反向重要性采样、负样本重置和序列级几何归一化。通过一个简洁且可解释的案例研究，我们证明蒸馏RL能够有效地将教师模型中先前无法获得的知识迁移到学生模型中。在同系列和跨系列蒸馏设定中的大量实验表明，蒸馏RL在pass@1和pass@k指标上都显著优于标准RL和OPD。我们的代码可在https://github.com/597358816/Distilled-RL获取。

## Abstract
Large language model (LLM) post-training is essential for improving reasoning, adaptation, and alignment. Existing methods mainly follow two paradigms: reinforcement learning (RL) and on-policy distillation (OPD). However, RL relies on coarse-grained outcome supervision, resulting in difficult credit assignment and limited capability to acquire new knowledge. OPD, meanwhile, unconditionally matches teacher logits through KL divergence, which creates a dilemma: similar teachers provide little new knowledge, while substantially different teachers often yield ineffective guidance, largely restricting OPD to within-family distillation. We propose Distilled Reinforcement Learning (Distilled RL), which integrates teacher supervision into the RL objective to provide fine-grained guidance, selectively transfer new knowledge and avoid unconditional imitation. Distilled RL contains three components: reverse importance sampling with clipping, negative sample reset, and sequence-level geometric normalization. Through a concise and interpretable case study, we demonstrate that Distilled RL can effectively transfer previously unavailable knowledge from a teacher model to a student model. Extensive experiments across both within-family and cross-family distillation settings show that Distilled RL substantially outperforms standard RL and OPD in terms of both pass@1 and pass@k. Our code is available at https://github.com/597358816/Distilled-RL.

---

## 论文详细总结（自动生成）

好的，以下是基于论文《Distilled Reinforcement Learning for LLM Post-training》的结构化中文总结。

### 1. 论文的核心问题与整体含义

论文专注于大语言模型后训练阶段，旨在提升模型的推理、适应和对齐能力。它指出了现有两种主流范式的根本性缺陷：

-   **强化学习的局限**：使用粗粒度的序列级结果（如答案对错）作为奖励信号，导致严重的信用分配问题，即模型难以识别长文本中哪些中间步骤是关键。此外，RL主要强化模型已有的行为，难以引入模型当前能力之外的新知识。
-   **在线蒸馏的困境**：通过KL散度无条件地强迫学生模型在每一步都模仿教师模型。这产生了一个两难：相似的教师模型能教的新知识有限；而与学生差异巨大的教师模型（如不同模型家族），其指导常常因为分布不匹配而变得无效且嘈杂，这极大地限制了蒸馏的应用范围。

因此，论文的核心问题是：**如何在LLM后训练中，有效地将教师模型的细粒度知识集成到强化学习框架中，既解决RL的信用分配和新知识获取难题，又避免在线蒸馏无条件模仿的局限性，特别是在跨模型家族的场景下。**

### 2. 论文提出的方法论

论文提出了蒸馏强化学习，一个统一的后训练框架。其核心思想不是将教师监督作为一个独立的模仿损失，而是直接将其融入RL的梯度更新中，作为一个选择性的、细粒度的信号重分配机制。

具体而言，蒸馏RL通过对学生模型生成的每个词元（token）计算一个“教师/学生”重要性比率，来重新分配RL的学习信号。该方法包含三个关键组件：

-   **带裁剪的反向重要性采样**：
    -   计算一个比率 `ρ`，表示教师模型相对于旧学生模型生成某个词元的偏好程度，而非RL中常用的从旧策略向新策略校正。
    -   算法会裁剪这个比率，防止因教师和学生概率差异过大导致梯度不稳定。

-   **负样本重置**：
    -   该方法会根据最终回复的优势（即该回复比平均回复好还是差）来选择性应用教师指导。
    -   **对于获得正优势（好）的回复**：保留教师偏好权重，让教师喜欢的词元得到加强，不喜欢的被抑制，实现细粒度指导和新知识迁移。
    -   **对于获得负优势（差）的回复**：将重要性权重重置为1，相当于退化为标准RL更新。**这避免了在错误路径上因模仿教师而受到更大惩罚，导致学生远离教师模型**。此设计确保知识蒸馏只发生在成功的轨迹上。

-   **序列级几何归一化**：
    -   由于回复是由学生模型采样，而非教师，教师为许多学生生成的词元分配的概率会较低，导致整个序列的重要性比率乘积（`∏ρ`）远小于1，过度压制了正样本。
    -   该归一化将序列内每个词元的权重除以其几何平均数，**保证了教师引导只在同一个回复内进行信号重分配（突出教师更偏好的词元），而不会放大或减弱该回复整体的优化强度**。

最终的优化目标 `J(θ)` 将以上组件整合进类似PPO/GRPO的梯度更新公式中。

### 3. 实验设计

实验旨在全面验证蒸馏RL在多个维度的有效性。

-   **数据集与场景**：
    -   **训练集**：使用DAPO-17K数据集进行训练，这是一个常用于推理任务的数据集。
    -   **评估基准**：覆盖了广泛的数学推理和知识密集型任务。
        -   **数学推理**：AIME24/25/26、CMIMC25、HMMT25、AMC23、GSM8K、MATH500、Minerva Math、Olympiad等。
        -   **知识密集型泛化**：MMLU-Pro和SuperGPQA（评估非数学推理领域的泛化能力）。
    -   **蒸馏场景**：设置了两种关键的实验场景：
        -   **同家族蒸馏**：教师模型是Qwen3-8B-GRPO，学生模型是Qwen3-4B。
        -   **跨家族蒸馏**：教师模型是Qwen3-8B-GRPO，学生模型是DeepSeek-R1-Distill-Qwen-1.5B，这是一个更具挑战性的设定。

-   **对比方法**：
    -   **基础模型**：未经后训练的学生模型。
    -   **RL**：使用标准GRPO进行强化学习。
    -   **OPD**：使用反向KL散度的在线策略蒸馏。
    -   **OPD+RL**：将OPD和RL的损失直接相加（1:1混合）。

-   **实验指标**：主要使用**pass@1**（单次采样的平均通过率）和**pass@k**（k次采样的通过率，如k=16）来衡量模型的性能。同时，实验也绘制了奖励、策略熵和回复长度等训练动态曲线。

### 4. 资源与算力

论文正文和附录中未明确提及训练所使用的GPU型号、数量、总训练时长等具体算力信息。仅提到了训练框架为EasyR1和VeRL，详细的训练配置如表5所示，但未包含算力开销。

### 5. 实验数量与充分性

实验设计较为充分、客观且公平，具体体现在：

-   **多模型、多基准测试**：在3个不同规模的学生模型（1.5B、1.7B、4B）上，对超过10个数学和知识密集型基准进行了全面评估。
-   **关键场景的针对性实验**：专门设计了同家族和跨家族蒸馏来验证方法的鲁棒性，这直接回应了论文提出的OPD的核心困境。
-   **丰富的消融研究**：对提出的两个核心组件（负样本重置和序列级几何归一化）进行了消融实验，清晰展示了每个组件的贡献和必要性。
-   **动态分析**：不仅仅报告最终分数，还通过绘制训练过程中的奖励、熵、回复长度等曲线，直观对比了各方法的优化轨迹，支撑了其理论分析（如OPD过早收敛）。
-   **可解释性案例研究**：设计了通过控制教师模型温度来改变熵的巧妙实验，直观证明了蒸馏RL能够捕捉并传递任务奖励之外的信号（如分布特性），增强了方法的内在可解释性。

### 6. 论文的主要结论与发现

-   蒸馏RL在同家族和跨家族蒸馏的所有设置下，其pass@1和pass@k指标**均一致地、显著地优于**标准RL、OPD及OPD+RL。
-   在极具挑战的**跨家族蒸馏**场景下，蒸馏RL的优势尤为突出，而传统的OPD和OPD+RL方法经常出现性能衰退或不稳定。
-   蒸馏RL实现了更优的训练动态：**奖励持续稳定提升，避免了OPD的过早饱和，也保持了适度的策略熵和回复长度**。
-   可解释性实验证明，蒸馏RL不仅优化任务奖励，**还能从教师模型迁移分布层面的知识（如熵的控制）**。

### 7. 优点

-   **方法论创新**：巧妙地将教师信号集成进RL梯度，而非作为独立的损失项，实现了RL与知识蒸馏的深度有机融合。
-   **设计精巧，直击痛点**：“负样本重置”解决了蒸馏信号在错误路径上的误导问题；“序列级几何归一化”解决了因学生采样导致信号过度抑制的问题，每个组件都针对性强。
-   **突破性强**：成功证明了在跨模型家族进行有效蒸馏的可行性，大幅拓宽了知识蒸馏的应用边界。
-   **实验验证扎实**：包含多维度评估、丰富的消融和可解释性研究，论证链条完整，具有很强的说服力。

### 8. 不足与局限

-   **算力细节缺失**：论文未报告GPU型号、数量和训练时长等关键算力信息，这使得方法的大规模部署成本和效率难以评估。
-   **教师模型能力假设**：方法隐含假设对于学生能解决的问题，教师也能提供有效指导。随着学生能力提升并可能在某些方面超越教师，这种基于教师偏好的重加权可能会引入次优指导，这是一个潜在的应用风险。
-   **基准局限性**：评估主要集中在数学和知识问答基准，其在更复杂的生成任务（如长文本创作、代码生成）上的泛化能力还有待验证。

（完）
