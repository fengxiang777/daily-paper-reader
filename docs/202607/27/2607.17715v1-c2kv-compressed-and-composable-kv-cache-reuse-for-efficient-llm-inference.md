---
title: "C$^2$KV: Compressed and Composable KV Cache Reuse for Efficient LLM Inference"
title_zh: "C$^2$KV: 压缩与可组合的KV缓存重用实现高效LLM推理"
authors: "Chuheng Du, Junyi Chen, Hanlin Tang, Kan Liu, Tao Lan, Lin Qu, Chaoyue Niu, Shengzhong Liu, Guihai Chen, Fan Wu"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17715v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 面向长上下文大模型推理的压缩与可组合KV缓存复用框架
tldr: 长上下文LLM推理（如检索增强生成）应用广泛，但KV缓存重用面临存储和访问瓶颈，简单结合压缩与复用会导致严重精度下降。本文提出C^2KV框架，通过联合优化KV缓存提取和推理时拼接，学习位置无关、可组合的压缩表征，配合轻量提取器和结构化注意力，实现不修改基模型的灵活复用。压缩-拼接协同训练确保对齐效果。实验表明，C^2KV显著降低存储和传输成本，在长上下文场景下推理速度提升最高17倍，且保持生成质量。该方法为大规模语言模型高效长上下文服务提供了关键技术支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17715v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1572, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 749, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1734, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 868, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17715v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1549, \"height\": 407, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17715v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 848, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 448, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1666, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17715v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 1386, \"label\": \"Table\"}]"
motivation: 现有KV缓存重用方法主要关注计算节省，却忽视了长上下文推理中KV缓存存储和访问的巨大开销，且直接压缩结合非前缀复用会导致严重精度损失。
method: 提出C^2KV框架，联合优化KV提取和推理时拼接，学习位置无关的可组合压缩KV表征，使用轻量提取器和结构化注意力，无需修改基模型，并采用压缩-拼接协同训练。
result: 在多个长上下文基准和模型上，C^2KV大幅降低KV缓存存储和传输开销，实现高达17倍推理速度提升，同时保持生成质量。
conclusion: C^2KV有效解决了KV缓存存储瓶颈，通过压缩和可组合复用实现了高效长上下文LLM推理，为资源受限部署提供了可行方案。
---

## 摘要
长上下文推理是现代大语言模型（LLM）应用（如检索增强生成和多文档推理）的核心。为了缓解日益增长的推理成本，近期工作探索了键值（KV）缓存重用，以减少冗余的预填充计算。然而，现有的重用方法主要关注计算节省，忽视了长上下文LLM服务中的一个关键瓶颈：存储和访问大型KV缓存的成本。尽管KV压缩看似是自然的补充，但天真地将压缩与非前缀KV重用结合往往会导致严重的准确率下降。本文提出C$^2$KV，一个面向非前缀KV重用的统一框架，联合优化KV提取和推理时的拼接。C$^2$KV学习一个可组合且压缩的KV缓存流形，该流形被明确设计为位置无关的。我们方法引入一个轻量级的Sidecar提取器，带有可学习的压缩令牌和结构化的注意力流，实现了模块化的KV表示，可以灵活重用和拼接，而无需修改冻结的基础模型。我们进一步采用压缩-拼接协同训练策略，使提取时的表示与其下游重用行为对齐。在多个长上下文基准测试和模型家族上的大量实验表明，C$^2$KV显著降低了KV缓存存储和传输成本，在长上下文下实现高达17倍的推理加速，同时保持生成质量。

## Abstract
Long-context inference is central to modern large language model (LLM) applications such as retrieval-augmented generation and multi-document reasoning. To mitigate the growing inference cost, recent work has explored key-value (KV) cache reuse to reduce redundant prefill computation. However, existing reuse methods primarily focus on computation savings and overlook a critical bottleneck in long-context LLM serving: the cost of storing and accessing large KV caches. While KV compression appears to be a natural complement, naively combining compression with non-prefix KV reuse often leads to severe accuracy degradation. In this work, we propose C$^2$KV, a unified framework for non-prefix KV reuse that jointly optimizes KV extraction and inference-time concatenation. C$^2$KV learns a composable and compressed KV cache manifold that is explicitly designed to be position-agnostic. Our approach introduces a lightweight sidecar Extractor with learnable compression tokens and a structured attention flow, enabling modular KV representations that can be flexibly reused and concatenated without modifying the frozen base model. We further employ a compression-concatenation co-training strategy to align extraction-time representations with their downstream reuse behavior. Extensive experiments across multiple long-context benchmarks and model families demonstrate that C$^2$KV significantly reduces KV cache storage and transfer costs, achieving up to 17$\times$ inference speedup under long contexts, while preserving generation quality.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

*   **研究背景与动机**：长上下文推理（如检索增强生成RAG、工具调用）已成为现代大语言模型（LLM）应用的核心，但其推理成本高昂。现有推理系统面临两大瓶颈：
    1.  **预填充阶段**（Prefill）的计算冗余：不同请求中经常包含相同的文档内容，但需重复计算键值（KV）缓存。
    2.  **长上下文服务的内存带宽瓶颈**：随着上下文长度增加，KV缓存的存储和从内存向显存传输的成本，已超越计算本身，成为制约推理性能的主要因素。例如，论文指出加载96K tokens的KV缓存耗时（0.41秒）远大于其混合/预处理时间（0.08秒）。
*   **现有方法的局限性**：
    *   **传统前缀复用**（Prefix Caching）要求复用内容必须出现在请求的相同位置，灵活性差。
    *   **训练无关的非前缀复用方法**（如CacheBlend）允许文档在不同位置组合，但为纠正位置偏差需进行选择性重计算，这引入了KV偏差（$\Delta$KV），导致精度和计算量之间的不可兼得。
    *   **基于训练的方法**（如Block-Attention）通过修改注意力结构实现复用，但可能导致模型原有能力的灾难性遗忘且对基座模型升级不友好。
    *   **天真地结合压缩与复用**会严重损害精度，原因是传统的KV缓存压缩方法产生的表示与其原始上下文紧耦合，**不具备可组合性**，即无法在拼接后保持原有的语义。
*   **核心问题**：如何在长上下文LLM推理中，既能通过非前缀复用减少预填充计算，又能通过有效压缩降低KV缓存的存储和传输开销，同时保持生成质量。

### 论文提出的方法论

论文提出了C$^2$KV框架，其核心思想是联合优化文档KV缓存的提取和推理时的拼接，学习一种**位置无关、可组合且压缩的KV缓存流形**，使得提取出的文档单元KV块可以像插件一样灵活地在任意位置拼接复用。

*   **核心架构：C2Extractor (轻量级Sidecar提取器)**
    *   设计为一个外挂的、可训练的模块，完全冻结基座LLM的所有参数，避免对基座模型能力的破坏。
    *   **引入C2Tokens**：在文档token间引入一组可学习的特殊token作为压缩记忆槽。给定压缩率$k$，一个C2Token负责总结其对应的一个长度为$k$的文档token块。
    *   **新增QKV投影头**：为每一层Transformer专门为C2Tokens添加独立的、可训练的QKV投影矩阵，而基座模型的原始token仍使用冻结的QKV投影。
    *   **残差均值池化变体** (C2KV-Residual)：一个可选的增强版本。在首层，将C2Token对应文档token块的隐藏状态均值，直接加到该C2Token的隐藏状态上，以显式注入局部语义，提升压缩下的稳健性。

*   **关键技术：结构化信息流 (Structured Information Flow, SIF)**
    *   通过精心设计的注意力掩码，**严格分离了原始文档token和C2Token的信息通路**，这是实现KV可组合性的关键。
    *   **原始Token不变性**：原始文档token之间保持标准的因果注意力，但严格禁止它们关注任何C2Token。这确保了冻结基座模型产生的KV表示完全不受影响。
    *   **块内语义提取**：每个C2Token只能因果地关注它所负责的同一块内的原始文档token，实现局部信息压缩，防止全局信息泄露。
    *   **跨块因果累积**：允许后一个C2Token关注前一个C2Token，使压缩表示能逐步累积全文档的语境信息。

*   **训练策略：压缩-拼接协同训练 (Compression-Concatenation Co-training)**
    *   训练目标仅是一个标准的自回归语言模型损失（SFT Loss），根据在不同文档拼接组合下生成的答案来计算。
    *   **隐式学习可组合性**：训练时，不同的文档虽然被独立提取，但监督信号只在多文档拼接后才施加。梯度通过拼接后的效果回传，迫使C2Extractor学习将文档语义投影到一个在任意组合下都能有效工作的“可组合KV流形”中。
    *   仅更新C2Tokens嵌入和C2提取器的QKV头，对基座模型无梯度传播。

### 实验设计

*   **训练数据**：从HotpotQA、2WikiMultiHopQA、LongMagpie三个公开的多文档问答数据集的训练集中，共采样12万个样本进行监督微调，训练仅一个epoch。
*   **评估基准与场景**：
    *   **长文档任务**：使用LongBench基准测试，包含多文档QA（HotpotQA, 2WikiMQA, MuSiQue等）和长文摘要（MultiNews, SAMSum, QMSum, GovReport）任务。指标为F1（QA）和ROUGE-L（摘要）。
    *   **长上下文检索**：使用RULER基准测试，评估在不同上下文长度（4k-64k tokens）和答案深度下的检索能力。
    *   **推理效率场景**：重点评估**首token生成时间（TTFT）**（测量从请求到达到解码准备就绪的时间）和**解码阶段每token生成时间（TBT）**（测量内存带宽压力）。
*   **基座模型**：Qwen3-4B-Instruct、Llama-3.1-8B-Instruct、Qwen2.5-7B-Instruct，并扩展实验至Qwen3-14B。
*   **对比基线**：
    *   **Full KV Recompute (FR)**：完整重计算，精度上界。
    *   **Naive Reuse (Reuse)**：直接在不同位置复用原始KV，精度下界。
    *   **训练无关的非前缀复用方法**：EPIC（选择性重计算）、CacheBlend。
    *   **基于训练的非前缀复用方法**：Block-Attention。
    *   **天真压缩+复用结合**：SnapKV（一种通用KV压缩方法）分别结合上述各类复用方法。

### 资源与算力

*   **训练配置**：全局批大小32，梯度累积步数4，总共12万样本，训练1个epoch。训练精度为BF16。
*   **硬件与时长**：论文未明确提及所使用的GPU型号、数量及具体训练时长。这是一个关键信息的缺失。

### 实验数量与充分性

*   **实验组数**：论文涵盖了丰富的实验组合，大致包括：
    1.  **3种基座模型**（4B, 7B, 8B）上的TTFT-准确率权衡对比。
    2.  **7个不同的LongBench任务**的详细准确率对比。
    3.  **多种解码上下文长度**（16k-128k）下的解码效率（TBT）测试。
    4.  **1个更大模型**（14B）上的泛化实验。
    5.  **多压缩率设定**（4x, 8x, 10x, 16x）下的固定和动态训练效果对比。
    6.  **RULER长上下文基准**下的性能对比。
    7.  **消融实验**，验证了结构化信息流（SIF）各项约束、位置重对齐、残差连接、动态训练等设计选择的效果。
    8.  **动机验证实验**，量化了天真地将SnapKV压缩与多种复用方法结合导致的严重精度下降。
*   **充分性与客观性评估**：实验设计**非常全面和充分**，覆盖了多种模型规模、多种任务类型、多个评估维度（精度、TTFT、TBT）。消融实验和动机实验有力地支撑了其核心论点，对比基线选择全面且具有代表性，评估指标公开标准，体现了较强的客观性和公平性。

### 论文的主要结论与发现

1.  **跨越了精度-效率鸿沟**：C$^2$KV在TTFT-准确率权衡图上达到了上左侧的最优区域，即在使用4倍压缩比、几乎无计算开销（仅加载和位置重排列）的情况下，其精度与昂贵的全重计算（FR）基线相当，并显著优于其他所有非前缀复用方法。
2.  **高效的解码阶段**：C$^2$KV通过压缩KV缓存的体积，显著压平了解码时延随上下文长度的增长曲线。在128k长上下文下，其解码速度远快于需处理完整KV缓存的方法，最高实现了**17倍的端到端推理加速**。
3.  **压缩率可扩展且适应性强**：C$^2$KV即使在高达16倍的压缩率下仍能保持稳健的性能，并且可以通过动态压缩比训练，使一个模型适应多档推理时的压缩预算。
4.  **设计的必要性验证**：
    *   天真地将通用压缩（SnapKV）与复用结合是无效的，会严重破坏精度，这证明了C$^2$KV联合优化设计的必要性。
    *   消融实验证实了结构化信息流中的位置隔离和局部提取是保证可组合性的根本，位置重对齐操作对提升复用后的精度至关重要。

### 优点

*   **问题视角深刻**：准确识别并验证了长上下文服务中KV缓存存储和内存带宽是新的主要瓶颈，而非单纯的计算。
*   **方法论创新性强**：首次提出并实现了“可组合KV缓存流形”的概念，将近似计算（KV压缩）和模块化复用（非前缀拼接）这两个孤立领域成功统一。
*   **系统与算法协同设计**：区分了提取与推理过程，并采用“外挂式”可训练模块，既保证了基座模型能力的无损，又实现了高效的在线推理，工程落地价值高。
*   **实验论证极为扎实**：从动机验证、到多维度指标对比、再到详尽的消融实验和模型扩展实验，逻辑链条完整，证据充分。

### 不足与局限

*   **算力开销未知**：论文未报告关键训练资源消耗（如GPU型号、数量、训练时间），这影响了对该方法实施成本的准确评估。
*   **应用范围限定**：当前框架假设需要复用的文档块是预先已知并可离线处理的。尚不支持对未知或流式到来的文本进行在线的、增量的提取与复用。
*   **压缩策略的粒度**：当前方法对所有文档、所有层采用统一的压缩比。缺乏内容感知或层间差异化的压缩策略，可能限制了某些场景下的效率最优解。
*   **训练依赖**：虽然仅外挂模块，但仍需要一定规模的训练数据和微调，对于“零训练”部署场景来说存在门槛。

（完）
