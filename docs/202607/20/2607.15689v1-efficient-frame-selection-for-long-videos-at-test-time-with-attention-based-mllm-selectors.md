---
title: Efficient Frame Selection for Long Videos at Test Time with Attention-Based MLLM Selectors
title_zh: 基于注意力的多模态大语言模型选择器在测试时对长视频的高效帧选择
authors: "Yilin Wang, Xiangxi Zheng, Dongxing Mao, Linjie Li, Zhengyuan Yang, Ping Yu, Rui Yan, Yuan Yao, Alex Jinpeng Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15689v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 基于注意力的帧选择方法，高效处理长视频输入以用于多模态大模型
tldr: 为了理解长视频，多模态大语言模型必须从数千帧中选取紧凑的帧集，但选择哪些帧又依赖于对视频的整体理解，这构成了一个循环依赖问题。本文发现，在多模态大模型验证阶段选定的注意力层中，交叉注意力能够在不进行自回归生成的情况下，直接提供与查询相关的帧级证据。基于这一发现，我们提出了DAFS，一种免训练的帧选择器，它利用轻量级多模态大模型（参数低至2B）作为选择器，通过查询条件聚合，将选定层的注意力转为帧的相关性分数，从而实现无需解码的跨帧比较与选择。同时，我们将候选池大小与每帧token预算的联合分配建模为离散优化，用动态规划求解；实验表明，在32帧预算下，DAFS在Video-MME上较均匀采样提升6.4点，超过训练选择器，泛化性强。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15689v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 797, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15689v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1645, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15689v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1621, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15689v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1659, \"height\": 523, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15689v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 746, \"height\": 360, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15689v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 794, \"height\": 442, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15689v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 808, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1652, \"height\": 1103, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 803, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 819, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 800, \"height\": 252, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 805, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15689v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 806, \"height\": 364, \"label\": \"Table\"}]"
motivation: 解决长视频理解中帧选择与内容理解的循环依赖，避免为不同任务或模型重训练帧选择器，实现高效、通用的选帧方法。
method: 利用验证层交叉注意力无需自回归生成即可提供帧级证据，构建轻量MLLM选择器DAFS，通过查询条件聚合注意力分数，并用动态规划联合优化候选池大小和每帧token预算。
result: 在Video-MME基准上，32帧预算下DAFS较均匀采样提升最高6.4点，性能超过匹配预算的训练选择器，并泛化至多种选择器与回答器主干及任务。
conclusion: DAFS是一种高效免训练的帧选择器，利用多模态大模型的交叉注意力实现视频帧的智能选取，无需重训练且泛化性强。
---

## 摘要
使用多模态大语言模型（MLLMs）理解长视频需要从数千个候选帧中选择一个紧凑的帧集合，然而，识别正确的帧似乎需要首先理解视频。我们通过一个简单的观察解决了这种循环依赖：在验证阶段选定的提取层中，MLLMs的跨模态注意力已经提供了与查询相关的帧证据，而无需进行自回归生成。我们利用这一特性构建了DAFS（基于动态注意力的预算感知帧选择），一种无需训练的帧选择器。一个轻量级的MLLM选择器，即使参数仅为20亿，也能通过查询条件下的聚合，将选定层的注意力转换为相关性分数，从而提取帧级别的证据。这实现了无需自回归解码的跨帧比较。为了处理选择器自身的上下文约束，我们将候选池大小和每帧令牌预算的联合分配问题，形式化为一个可通过动态规划求解的离散优化问题。在32帧的预算下，我们的选择器在Video-MME上比均匀采样最多提高了6.4个点，并在匹配的帧预算下优于先前基于训练的选择器，同时无需重新训练即可泛化到不同的选择器和回答器骨干模型，以及不同的任务。

## Abstract
Understanding long videos with multimodal large language models (MLLMs) requires selecting a compact set of frames from thousands of candidates, yet identifying the right frames seemingly requires understanding the video first. We resolve this circular dependency with a simple observation: cross-modal attention at validation-selected extraction layers in MLLMs already provides query-relevant frame evidence without requiring autoregressive generation. We exploit this property to build DAFS (Dynamic Attention-based Budget-aware Frame Selection), a training-free frame selector. A lightweight MLLM selector, even with only 2B parameters, can extract frame-level evidence by converting selected-layer attention into relevance scores through query-conditioned aggregation. This enables cross-frame comparison without autoregressive decoding. To handle the selector's own context constraint, we formulate the joint allocation of candidate pool size and per-frame token budget as a discrete optimization problem solved by dynamic programming. Under a 32-frame budget, our selector improves over uniform sampling by up to 6.4 points on Video-MME and outperforms prior training-based selectors under matched frame budgets, while generalizing across selector and answerer backbones, and across tasks, without retraining.

---

## 论文详细总结（自动生成）

# 论文结构化总结：基于注意力的多模态大语言模型选择器在测试时对长视频的高效帧选择

## 1. 论文的核心问题与整体含义

- **核心问题**：多模态大语言模型在处理长视频时，受限于有限上下文窗口和视觉令牌预算，无法处理所有帧。因此，理解长视频的任务本质上简化为一个“选择少量信息帧”的问题。
- **循环依赖困境**：高效地选择与查询答案相关的帧，似乎又需要先对整个视频内容有所理解，这形成了一个先有鸡还是先有蛋的循环依赖。
- **研究动机与背景**：现有方法存在不足。均匀采样内容盲区、基于CLIP的相似度打分缺乏跨帧上下文和深层查询条件、基于MLLM的自回归选择器虽语义深度强但推理成本高且需要额外训练。
- **整体含义**：本文旨在提出一种**无需训练、计算高效且查询感知**的帧选择框架，通过利用现成MLLM内部已经存在的“免费”信号（跨模态注意力），打破上述循环依赖，实现普适且高性能的长视频帧选择。

## 2. 论文提出的方法论

- **核心思想**：**利用轻量级MLLM选择器中，验证层（validation-selected layer）的跨模态注意力模式，直接作为帧级相关性的证据，无需进行自回归生成。**
- **关键技术细节与流程**:
    1.  **候选池构建与预算感知采样**: 以1 FPS提取候选帧，根据视频时长 \(d\) 和全局视觉令牌预算 \(B\)，动态决定采样子集帧数 \(F(d)\) 和每帧压缩后的令牌数 \(P(d)\)，确保 \(F(d) \cdot P(d) \le B\)。
    2.  **空间令牌压缩**: 对每帧的视觉令牌网格进行非重叠的平均池化，将其压缩至 \(P(d)\) 个令牌，以满足选择器的上下文限制。
    3.  **查询感知的帧评分**:
        -   **提取注意力**: 从预选的特定层 \(\ell^*\) 提取文本到视觉的注意力矩阵 \(A^{(\ell^*)}\)。
        -   **原始分计算**: 对每帧 \(j\)，将其包含的所有视觉令牌的注意力权重求和，得到每个文本令牌对每帧的原始相关性分数 \(S_{t,j}\)。
        -   **关键令牌聚合**: 定义一个“重要文本令牌集合” \(T_{imp}\)（如选择题选项、实体词），仅对这些令牌的分数进行最大池化，得到每帧的最终显著性分数 \(\phi_j\)。此步骤旨在削减常见无信息词（如“the”）带来的注意力偏差，聚焦于语义核心。
        -   **公式表示**: \(\phi_j = \max_{t \in T_{imp}} \sum_{p=1}^{P} A^{(\ell^*)}_{t, (j,p)}\)
    4.  **帧选择与回答**: 根据 \(\phi_j\) 对帧排序，选出前 \(K\) 个帧，以其原始分辨率输入给最终的回答器MLLM生成答案。
- **动态规划校准**:
    -   **问题建模**: 将针对不同时长 \(d_i\) 的 \((F_i, P_i)\) 配置选择，建模为一个在全局令牌预算 \(B\) 约束下，最大化校准集上问答准确率总和的离散序列优化问题。
    -   **约束**: \(F_i \cdot P_i \le B\)，且 \(F_i\) 随 \(d_i\) 单调非减，\(P_i\) 单调非增，以平衡长视频的时间覆盖和短视频的空间细节。
    -   **求解与应用**: 该问题具有最优子结构，通过**动态规划**求解全局最优分配的查找表。推理时，根据输入视频时长查表即可在 \(O(1)\) 时间内获得 \(F(d)\) 和 \(P(d)\)。

## 3. 实验设计

- **数据集/场景**:
    -   **长视频QA基准**: Video-MME (无字幕)、MLVU (含Needle-QA子任务评估时序定位)、LongVideoBench (LVB)。
    -   **开放式生成基准**: MMBench-Video。
    -   **校准集**: 用于验证层选择（Charades-STA, NExT-GQA）和DP策略校准（CG-Bench, NExT-QA等），均与评测集无重叠。
- **比较方法**: 进行了全面对比，涵盖以下类别：
    -   **通用采样**: 均匀采样。
    -   **基于CLIP/SigLIP的相似度选择**: Top-K CLIP, Top-K SigLIP2, BOLT, Q-Frame, AKS。
    -   **基于MLLM的训练选择器**: K-Frames, A.I.R., GIFT, MSJoE, ViaRL。
    -   **闭源/开源MLLM原生方法**: GPT-4o, Gemini-1.5-Pro, Keye-VL-1.5等。
- **评测设置**:
    -   统一使用Qwen2.5-VL-7B作为回答器，在32帧预算下进行公平对比。
    -   评估了不同选择器（InternVL3.5, Qwen2.5-VL, Qwen3-VL, Qwen3.5）和不同回答器（LLaVA-Video, InternVL3.5-8B, Qwen2.5-VL-72B）下的泛化能力。
    -   评估了在“预览-检索-回答”工具链中，替代CLIP检索器的能力。

## 4. 资源与算力

-   文中明确提到所有评估均在**NVIDIA A100 GPU**上进行。
-   在推理成本分析（表3）中，报告了**单张A100 GPU**上每个查询的端到端耗时。
-   关于选择器的校准过程，**未提及**具体的GPU数量或训练时长，这符合其“无需训练”的特点。校准仅在推理前运行少量前向传播以确定配置，开销极小。

## 5. 实验数量与充分性

- **实验数量**:
    -   **主实验**: 在3个长视频基准（Video-MME, LVB, MLVU）上与10余种基线方法比较。
    -   **泛化实验**: 涵盖更换4种选择器、3种回答器、1个开放式生成任务（MMBench-Video）和1个工具链场景（GPT-4o）。
    -   **消融实验**: 包括选择器模型规模和提取层的消融、DP分配策略的消融、重要令牌聚合函数的消融。
    -   **分析实验**: 跨模态注意力模式分析（图层相似度、时序证据召回率）、推理成本分析、时序定位专项评估（MLVU Needle-QA）。
- **充分性与客观性评估**:
    -   **充分**: 实验设计系统且全面，覆盖了主要基准、主流基线和多个分析维度。消融研究清晰地证明了各组件（尤其是注意力层选择和DP策略）的有效性。
    -   **客观公平**: 对比严格控制，使用相同的回答器和帧预算。验证协议清晰，确保了校准数据与测试数据严格隔离，防止信息泄露。对训练型和免训练型方法进行了公平的成本和精度比较。

## 6. 论文的主要结论与发现

-   MLLM中间层的跨模态注意力是**帧级相关性的强信号**，可直接用于查询感知的帧选择，无需自回归解码或模型训练。
-   **DAFS框架**在多个长视频基准上，以匹配的帧预算**一致优于**均匀采样、CLIP类方法和现有的基于训练的选择器。
-   方法具备极强的**泛化能力**，其选出的帧可有效迁移至不同强大回答器，并适用于不同任务格式。
-   **动态规划策略**有效平衡了时间覆盖与空间细节，在不同时长视频上均优于静态分配方案。
-   该选择器**计算成本极低**，其选择阶段的耗时远低于基于训练的自回归选择器，实现了精度与效率的兼得。

## 7. 优点

-   **巧妙的信号复用**: 发现了MLLM内部固有的、可解释的注意力模式，并将其巧妙转化为无需训练的帧评分机制，思想新颖。
-   **高效的免训练方案**: 完全无需对选择器进行微调，仅需少量推理校准，大幅降低了使用门槛和计算开销。
-   **预算感知与联合优化**: 将传统上独立的帧采样和令牌压缩问题联合建模，并通过动态规划寻找全局最优，系统性强。
-   **优异的性能与泛化性**: 在性能提升、不同模型和任务间的迁移能力、以及计算效率方面均表现出色，论证扎实。
-   **实验设计严谨**: 验证集严格分离，对比基线广泛且公平，消融分析逻辑清晰，结论说服力强。

## 8. 不足与局限

-   **依赖于特定层的先验**: 最佳提取层 \(\ell^*\) 需要通过一个小规模校准步骤（如时序定位任务）预先确定，这可能为该框架引入了额外的、虽小却必不可少的人工设定。
-   **关键令牌聚合的规则化**: “重要单词集合”的构成依赖于启发式规则（选选项词或用LLM提取实体），在复杂长问句或非实体中心的问题上，其聚合效果可能并非最优。
-   **对视频底层特征的依赖**: 方法效果建立在MLLM的视觉编码器和注意力机制能准确将语义与视觉令牌关联的基础上，对某些类型视频（如高度抽象、快速蒙太奇）的鲁棒性未作探讨。
-   **DP策略的适用性**: 动态规划策略是在特定回答器和选择器组合上校准的，虽然文中展示了良好泛化性，但理论上更换强异构的模型组合时，最优分配策略可能发生变化，文中未对此进行详细探讨。

（完）
