---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: "Traj-VLN: 通过自回归轨迹生成学习像素空间交互"
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: LLM代理用于视觉语言导航任务，结合自回归轨迹生成
tldr: 针对VLM难以处理VLN-CE中涉及深度的空间交互问题，本文提出Traj-VLN，通过自回归生成像素空间轨迹来学习导航交互。模型在2D图像上预测像素坐标序列，形成从观察底部中心出发的轨迹，避免引入3D几何信息。实验表明像素轨迹监督显著优于离散动作学习，旗舰模型在有限资源和数据下取得最优性能。该工作为VLN提供了一种简洁高效的新范式，证明像素空间轨迹生成的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 633, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1845, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 385, \"label\": \"Table\"}]"
motivation: VLM难以理解VLN-CE中涉及深度方向的空间交互，且引入3D几何信息与VLM预训练数据不符。
method: 提出Traj-VLN，在2D像素空间自回归生成轨迹，学习导航交互，避免依赖深度信息。
result: 像素轨迹监督显著优于离散动作学习，模型以有限资源和数据达到当前最优，验证像素轨迹有效性。
conclusion: 像素空间轨迹生成为VLN提供高效范式，无需深度信息，在有限资源下实现强泛化，推动VLM在具身导航的应用。
---

## 摘要
受益于大规模预训练数据中嵌入的强大先验和新兴的常识推理能力，大语言模型在许多研究领域展现出了前所未有的泛化能力。近期，通过视觉-语言模型将视觉嵌入投射到语言空间以实现仿真到现实和跨场景泛化，已成为连续环境视觉-语言导航领域的主流范式。视觉-语言导航要求具身智能体根据自然语言指令在未见环境中导航。我们强调，视觉-语言导航任务可分解为一系列子任务，每个子任务对应于与指令所描述的环境进行3D空间交互的过程，例如“走到沙发尽头并左转”。然而，此类涉及沿深度感知方向移入图像的空间交互，对于主要在与RGB图像对话上训练的视觉-语言模型而言是令人困惑的。我们并未引入深度或3D几何信息——这些在视觉-语言模型预训练中很少遇到——而是提出了一种替代方法：通过自回归轨迹生成，直接在2D像素空间中微调视觉-语言模型以学习导航交互。给定语言指令和历史观测，我们的模型顺序预测一系列像素坐标，从当前观测的底部中心开始绘制一条轨迹。尽管先前工作已证明像素目标监督优于离散动作的学习，我们的实验进一步验证了像素空间轨迹的监督显著提升了视觉-语言导航的性能。此外，我们证明，我们的一流模型在相对有限的计算资源和训练数据下达到了最先进的水平。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

### 总结：Traj-VLN: 通过自回归轨迹生成学习像素空间交互

#### 1. 论文的核心问题与整体含义

*   **核心问题**：现有基于视觉-语言模型（VLM）的视觉-语言导航（VLN）方法面临一个根本性矛盾。VLN任务要求智能体理解3D空间交互（如“走向沙发并左转”），但主流VLM主要在与2D RGB图像的对话数据上预训练，缺乏对深度和3D几何布局的内禀理解。引入额外的深度或3D信息不仅增加了模型复杂度，也与VLM的预训练数据分布不匹配，可能导致灾难性遗忘。
*   **整体含义**：该论文旨在解决这一瓶颈，提出了一种全新的范式：**避免让VLM进行显式的3D推理，而是引导它在熟悉的2D像素空间中学习“交互过程”**。论文核心思想是，通过自回归地生成一条2D像素轨迹，隐式地编码了3D空间交互逻辑，从而将VLN任务转化为一个与VLM预训练任务（像素级理解和序列生成）高度兼容的形式。

#### 2. 论文提出的方法论

*   **核心思想**：将导航“动作”的监督信号，从低层次的离散动作（如前移0.25米，左转15度）或单一的像素目标，替换为**一条连续的像素空间轨迹**。通过预测“从近到远”的轨迹，模型学习了指令所描述的空间交互过程。
*   **关键技术细节与流程**：
    *   **模型架构**：基于Qwen3-VL-8B-Instruct构建，冻结视觉编码器，全量微调大语言模型（LLM）和投影层。
    *   **两阶段对话机制**：
        *   **第一阶段**：VLM根据当前观察和历史图像，输出高层决策：`STOP`（停止）、`←`/`→`（旋转15度）或 `↓`（前进）。
        *   **第二阶段（触发时）**：若第一阶段输出 `↓`，智能体向下低头30度捕捉俯视图像。该图像作为额外输入，引导VLM进入第二阶段，**自回归地生成一系列文本格式的归一化像素坐标** `[u1, v1] [u2, v2] ... [uk, vk]`，在俯视图像上绘制一条从底部中心开始的轨迹。
    *   **监督信号生成（数据构建）**：将未来时刻的智能体3D位姿投影到当前俯视图像上，得到一条2D像素轨迹，并过滤掉遮挡和重复的像素点，作为模型的监督目标。
    *   **导航执行**：生成的2D像素轨迹结合对应的深度图，通过相机内参和外参被反投影为3D空间中的路径点，智能体依次执行。

#### 3. 实验设计

*   **数据集与Benchmark**：
    *   **训练数据**：从标准数据集 **R2R-CE**、**RxR-CE** 以及 **ScaleVLN** 的子集中提取样本，以4步为间隔采样，共构建了 **175万** 个训练样本。
    *   **评估Benchmark**：在 **R2R-CE Val-Unseen** 和 **RxR Val-Unseen** 这两个主流的、具有挑战性的VLN-CE基准上进行评估。
*   **对比方法**：与两类方法进行了全面比较：
    *   **非VLM的传统方法**：Sim2Real, AO-Planner, NavMorph。
    *   **基于VLM的SOTA方法**：NaVid, NaVid-4D（引入深度信息）, NaVILA, StreamVLN, JanusVLN（解耦语义与空间）, InternVLA-N1（像素目标监督）, Goal2Pixel（像素目标监督）。
*   **核心消融/对比实验**：为公平比较，基于相同的Qwen2.5-VL-7B骨干、相同的175万数据和相同的LoRA微调配置，训练了两个模型：一个采用像素目标监督，另一个采用本文的像素轨迹监督。

#### 4. 资源与算力

*   **旗舰模型训练**：使用 **8 块 Ascend 910C NPU**，训练一个Epoch共 **16998 步**，耗时约 **91 小时**（总计728 NPU小时）。冻结视觉编码器节省了约62.5%的训练时间。
*   **对比实验训练**（LoRA微调）：使用 **4 块 Nvidia 4090 GPU**，训练时长约 **171 小时**。

#### 5. 实验数量与充分性

论文设计了多层次、较充分的实验来验证其主张：
1.  **主要结果表 (Table I)**：旗舰模型与大量SOTA方法在两大基准上的性能对比，证明了其整体先进水平。
2.  **核心对比表 (Table II)**：在严格控制变量（相同模型、数据、训练设置）的条件下，直接对比“像素轨迹监督”与“像素目标监督”，有力证明了轨迹监督的优越性。
3.  **参数分析图 (Fig. 4)**：探究了执行轨迹点数量（执行1个到全部点）对SR（成功率）和OS（Oracle成功率）的影响，并进一步证实了在各种设定下轨迹监督都优于目标监督。
4.  **思维链验证（Traj-CoT）**：通过将生成的轨迹“最终点”作为“像素目标”进行评估，证明自回归生成轨迹本身起到了思维链机制的作用，提升了目标预测的准确性。
这些实验设计逻辑清晰，层层递进，不仅展示了性能，还解释了性能提升的原因，具有较好的客观性和公平性。

#### 6. 论文的主要结论与发现

*   **像素空间轨迹监督极其有效**：相比直接使用像素目标或离散动作作为监督信号，让VLM学习自回归地生成一条完整的2D交互轨迹，能**显著提升VLN性能**。
*   **状态达到了最优水平**：在使用相对有限的计算资源和训练数据的情况下，Traj-VLN在R2R-CE和RxR-CE的Val-Unseen集合上取得了当前最优水平（SOTA）的成功率（SR），超越了引入深度、3D先验或使用更多数据的其他方法。
*   **轨迹生成可作为思维链**：自回归地生成轨迹过程不仅能规划路径，还能作为模型进行空间推理的“思维链（CoT）”，从而帮助模型更准确地定位最终目标。

#### 7. 优点

*   **方法论创新**：巧妙地将3D空间交互问题转化为2D像素空间的序列生成问题，高度契合VLM的预训练特性，有效规避了VLM的3D感知弱点和引入多模态数据带来的分布不匹配风险。
*   **设计极简且高效**：无需额外设计深度编码器或对齐3D几何先验的复杂模块，仅通过改变监督信号和推理流程就实现了性能跃升，计算资源利用率高。
*   **实验论证有力**：通过严格的变量控制和多维度的分析（如Traj-CoT验证、执行步数分析），清晰、严谨地证明了方法各部分的有效性。
*   **性能出色**：在资源受限的条件下超越了许多资源消耗更大的竞争对手，具有较好的实际应用潜力。

#### 8. 不足与局限

*   **导航精度指标**：旗舰模型在R2R-CE上的SR（58.4）很高，但在更综合的SPL指标（51.7）上并非绝对最高，暗示其路径规划可能不是最优，或执行过程不够平滑。
*   **对俯视图的依赖**：轨迹生成依赖于获取一张俯视图像，这需要机器人执行低头的动作。在实际应用中，这可能增加交互的复杂度和时间成本，并可能在某些低矮或极度杂乱的环境中受限。
*   **初步的定性展示**：论文展示了通用多模态大模型无法绘制合理轨迹的问题，但未对Traj-VLN本身的失败案例进行深入的定性分析，不易判断模型的边界和典型失效模式。
*   **部分细节缺失**：对于某些复杂情况下的鲁棒性（如投影轨迹被遮挡的处理、动态障碍物的影响），论文中的讨论较为有限。

（完）
