---
title: "Better Harnesses, Smaller Models: Building 90% Cheaper Agents via Automated Harness Adaptation"
title_zh: "更优的套索，更小的模型：通过自动化套索适配构建成本降低90%的智能体"
authors: "Chenyang Yang, Xinran Zhao, Tongshuang Wu, Christian Kästner"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08938v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 自动化框架适配以构建更便宜的大语言模型智能体
tldr: "面对前沿大语言模型(LLM)代理的高推理成本，直接替换为小语言模型(SLM)虽能降低成本但性能往往不佳。本文提出通过元代理自动发现并优化适配组件（harness），包括定制指令、工具和编排循环，将任务难度从模型转移到harness中。在七个商业导向的代理任务和三个SLM系列上实验，优化后的harness在16/21任务-SLM组合中显著提升性能，其中7个组合完全弥合了性能差距，最佳SLM代理以4%的成本恢复89.7%的LLM性能。这一方法表明，自动化harness适配能大幅扩展SLM在常规商业任务中的有效部署范围。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08938v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1769, \"height\": 705, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08938v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1604, \"height\": 944, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08938v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08938v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 378, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08938v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 902, \"height\": 346, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08938v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1874, \"height\": 1014, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08938v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1487, \"height\": 1035, \"label\": \"Table\"}]"
motivation: 大模型代理推理成本高，小语言模型直接替换性能不足，需高效降低部署成本同时保持性能。
method: 提出元代理自动优化harness，通过分析失败轨迹，为SLM适配定制指令、工具和编排循环，将任务难度转移至harness。
result: "在7个任务和3个SLM系列上，优化harness在16/21对中显著提升性能，7对弥合差距，最佳SLM以4%成本恢复89.7%LLM性能。"
conclusion: 自动化harness适配可显著提升SLM代理在常规商业任务中的表现，大幅降低成本，拓展了实际部署范围。
---

## 摘要
前沿的大语言模型（LLM）智能体正在自动化许多商业任务，但其高昂的推理成本使得大规模部署难以为继。小语言模型（SLM）提供了一种更廉价的替代方案，但当它们被直接用于为前沿LLM设计的套索时，性能通常会有所欠缺。我们表明，对于许多常规商业任务，当搭配使用可由元智能体自动发现的适配套索时，SLM智能体能够以降低90%的成本达到LLM的性能。关键的见解在于，任务难度在很大程度上是跨实例共享的，并且可以通过量身定制的指令、工具和编排循环从模型转移到套索中。为了系统性地研究这一点，我们创建了一个框架，将智能体的失败模式映射到套索适配策略，并构建了一个套索优化器，该优化器能够从失败轨迹中自动发现有效的适配方案。在七项面向商业的智能体任务和三个SLM家族中，我们发现优化后的套索在21个任务-SLM对中的16个上显著提升了性能，其中7个对消除了SLM与LLM之间的性能差距，最佳SLM智能体以4%的成本恢复了89.7%的LLM性能。我们的分析进一步表明，适配对于工作流程重复性更高的任务以及具有足够基础能力的SLM最为有效。这些结果表明，套索适配能够扩展SLM智能体在常规商业任务中的实际部署范围。

## Abstract
Frontier LLM agents are automating many business tasks, but their high inference cost makes large-scale deployment unsustainable. Small language models (SLMs) offer a cheaper alternative, yet they typically fall short when swapped into a harness designed for a frontier LLM. We show that for many routine business tasks, SLM agents can match LLM performance at 90% lower cost, when paired with an adapted harness that can be automatically discovered by a meta agent. The key insight is that much of the task difficulty is shared across instances and can be lifted from the model into the harness via tailored instructions, tools, and orchestration loops. To study this systematically, we create a framework that maps agent failure modes to harness adaptation strategies, and build a harness optimizer that automatically discovers effective adaptations from failure trajectories. Across seven business-oriented agentic tasks and three SLM families, we found optimized harnesses significantly improve performance on 16 of 21 task-SLM pairs, with seven pairs closing the SLM-LLM performance gap and the best SLM agent recovering 89.7% of LLM performance at 4% of the cost. Our analysis further shows that adaptation works best for tasks with more repetitive workflows and for SLMs with sufficient base capabilities. Together, these results suggest that harness adaptation can expand the practical deployment range of SLM agents in routine business tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **研究动机与背景**：当前由前沿大语言模型驱动的智能体在商业任务自动化中表现优异，但其高昂的推理成本（API调用费用）使得大规模、可持续的商业部署面临巨大挑战。小语言模型因成本低、延迟小、隐私风险小成为一种廉价替代方案。
*   **核心问题**：直接将SLM替换到为LLM设计的通用智能体套索（即提示词、工具集、编排逻辑的组合）中，往往会导致性能大幅下降，无法满足业务要求。论文探讨的核心问题是：**能否以及如何通过自动化的方式，为SLM智能体找到一套专门的适配套索，使其在显著降低成本（约90%）的同时，恢复或接近前沿LLM智能体的性能？**
*   **整体含义**：这项工作提出了一种将任务通用难度从模型能力转移到基础设施设计中的范式，为在常规、重复性高的商业任务中实现智能体廉价且可靠的规模化部署提供了一条有前景的路径。

### 2. 论文提出的方法论
*   **核心思想**：
    *   **任务难度可转移**：对于许多常规任务，其核心难度是跨实例共享的（例如固定的工作流、已知的业务规则、受限的工具使用模式），而非每次都需要模型创造性地解决。这种“可转移的难度”可以通过精心设计的套索来吸收，从而降低对模型本身的能力要求。
    *   **自动化适配**：通过一个由前沿LLM驱动的元智能体，自动诊断SLM的失败轨迹，并根据诊断结果针对性地修改套索组件（指令、工具、编排逻辑），将任务难度从SLM转移到套索中。

*   **关键技术细节与算法流程**：
    *   **失败模式与适配策略框架（如图2所示）**：
        *   **失败模式**：将智能体失败按所需能力分类为工具使用、指令遵循、知识、长上下文和规划/推理失败。
        *   **适配策略**：将套索修改归纳为三大类：
            1.  **上下文适配**：添加或管理提示词、示例、外部文档等。
            2.  **工具适配**：创建封装复杂序列的高级工具、过滤无关工具、调整工具模式。
            3.  **智能体循环适配**：增加确定性检查的钩子（hooks）、将单智能体拆分为多智能体的编排。
        *   **映射关系**：建立了从失败模式到适配策略的映射，例如，“知识失败”可通过“上下文适配”中的“添加外部知识”来缓解。

    *   **自动化套索优化器（元智能体）的工作流程（如图3所示）**：
        1.  **采样评估**：从一个待选套索池中选取一个套索，在SLM上执行一批任务实例，收集完整的运行轨迹和评估得分。
        2.  **诊断与提议**：元智能体检查失败的轨迹，结合当前套索代码、搜索历史记忆和设计空间文档（即可用的API），诊断失败原因，并通过对套索代码进行编辑来提出新的适配方案（如修改系统提示词、新增钩子文件）。
        3.  **验证与更新**：对新提议的套索进行语法和基本运行校验，通过后在相同的训练实例上评估。若有提升，则在验证集上进行完整评估；若在验证集上表现优于池中候选，则将其加入套索池。
        4.  **循环迭代**：在设定的预算（如20美元）内，使用类似遗传算法的策略从帕累托前沿采样套索进行迭代优化。

### 3. 实验设计
*   **数据集与场景**：论文从公开基准中精选并构建了**7个面向商业的智能体任务**，旨在模拟现实世界中的常规业务流程，包括：考勤审计、预算审批、库存警报、异常检测、Playwright测试生成、网站管理与代码重构。每个任务都配备了明确的、可自动执行的评估指标（如：与标准答案比对、执行测试套件）。
*   **对比的Benchmark与方法**：
    *   **基线模型**：使用通用套索的前沿大模型 (`gemini-3.1-pro-preview`)。
    *   **对比对象**：为三个当前主流的开源SLM系列各设置两种配置：
        1.  **通用套索 + SLM**：直接将SLM放入为LLM设计的原始套索中。
        2.  **优化套索 + SLM**：使用本文提出的元智能体为特定任务和SLM自动优化后的套索。
    *   **SLM模型**：实验选取了代表当前先进水平的三个开源SLM：`qwen3-coder-30b-a3b`， `ministral3-8b`， 和`gemma-4-26b-a4b`。
    *   **评估指标**：主要评估三种配置在**测试准确度（Accuracy）**、**单次推理成本（Cost）**和**端到端延迟（Latency）**上的表现。

### 4. 资源与算力
*   文中**未明确提及**用于训练SLM的GPU型号、数量或训练时长，因为该方法的核心是对已冻结的SLM模型进行“套索”层面的优化，而非微调模型本身。
*   论文明确指出了**元智能体优化的资源消耗**：每一次针对单个任务-SLM对的优化运行设定了**20美元**的API调用预算。总计进行了21个任务-SLM对（7个任务 × 3个模型）的优化，总优化预算约为 **1260美元**。

### 5. 实验数量与充分性
*   **实验组数**：共进行了 **21组**（7项任务 × 3种SLM）核心对比实验。此外，为了分析任务多样性和模型能力的影响，还设计了控制变量实验（通过改变任务模板数量来模拟不同多样性）和跨模型能力的相关性分析。
*   **实验充分性与公平性**：
    *   **充分性**：实验覆盖了多种业务领域和不同能力等级的SLM，规模较为可观。通过对任务多样性和模型能力两个关键变量的深入分析，不仅回答了“是否有效”，还揭示了“何时更有效”、“为什么有效”的规律。
    *   **公平性**：对比基线设定公平。每种配置都在相同的任务实例和评估指标下进行，每个任务-模型对进行了多次重复运行以减少随机性，并使用了统一的训练/验证/测试集划分（20%/20%/60%）。
    *   **客观性**：存在一定局限。针对某个任务-SLM对的优化套索可能对于同一任务下的其他SLM不适用，这表明适配是高度定制化的，大规模跨模型、跨任务的泛化性评估尚缺。此外，分析基于特定优化器实现，其发现可能不完全适用于其他优化算法。

### 6. 论文的主要结论与发现
*   **性能与成本**：优化套索能显著提升SLM智能体的性能。在**21个任务-SLM对中的16个**上取得显著提升，其中**7个对**完全消除了与LLM的性能差距。表现最好的`gemma-4-26b-a4b`智能体平均能以**4%** 的成本恢复了LLM **89.7%** 的性能，并降低了25%的延迟。一次性的优化成本平均运行13次即可回收。
*   **任务多样性影响**：**任务工作流越单一、重复性越高，套索适配的效果越好**。高度多样化的任务（如代码重构）由于实例间差异大，难以设计统一的套索来覆盖所有情况。
*   **模型能力影响**：**模型自身的基础能力越强，套索适配的效益越高**。这表明套索可以转移大量任务难度，但无法完全替代缺失的核心能力（如复杂推理、代码生成）。
*   **适配策略**：最常见的成功适配策略是**解决指令遵循和知识性失败**，主要通过**添加上下文（86%）**、**创建新工具（43%）** 和**管理工具集（29%）** 来实现。

### 7. 优点
*   **极具实际应用价值**：直接回应了行业普遍面临的LLM成本高昂的痛点，提供了一套自动化且低成本的解决方案，经济和实践意义显著。
*   **系统的理论框架**：不仅提供了自动化工具，更构建了从“失败模式”到“适配策略”的系统性映射框架，为理解和解释“为何适配有效”提供了理论支撑。
*   **自动化方法创新**：提出的元智能体优化器，将人类从繁琐的试错式手动调优中解放出来，体现了“用AI优化AI”的思想，并能自动适应不同SLM的独特失败模式。
*   **关键洞察深刻**：核心洞察——常规业务的任务难度可被转移至基础设施（套索）——是此方法论的基石，为低资源场景下的AI部署提供了新思路。

### 8. 不足与局限
*   **任务多样性有限**：实验任务虽然覆盖了多个领域，但都局限于“常规业务”且有明确、可自动评估目标的场景，结论可能不适用于更开放、更需要创造性和复杂推理的任务。
*   **模型与优化器的强耦合**：优化过程高度依赖于元智能体的分析诊断能力。若底层元智能体模型（实验中使用的是`gemini-3.1-pro-preview`）的能力不足或更新换代，优化效果可能不稳定或需要重新验证。
*   **可泛化性风险**：
    *   **模型间泛化**：为特定SLM优化的套索可能对其他SLM无效，导致迁移到新模型时需重新优化，增加了隐性成本。
    *   **环境间泛化**：优化过程是在纯净的模拟环境中进行的，其产出的套索在真实、嘈杂且任务分布可能随时间变化的生产环境中是否同样鲁棒，是未知数。
*   **元智能体策略存在盲区**：实验中未观察到成功创建子智能体的案例，这可能是当前SLM在多智能体管理方面能力缺失所致，但也暴露出优化器自身搜索能力或设计空间指导的局限。

（完）
