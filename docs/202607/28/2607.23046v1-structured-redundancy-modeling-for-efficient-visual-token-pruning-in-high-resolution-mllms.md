---
title: Structured Redundancy Modeling for Efficient Visual Token Pruning in High-Resolution MLLMs
title_zh: 针对高分辨率多模态大语言模型中高效视觉令牌剪枝的结构化冗余建模
authors: "Jouwon Song, Woohyeong Kim, Kyeongbo Kong"
date: 2026-07-25
pdf: "https://arxiv.org/pdf/2607.23046v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 高分辨率多模态LLM的高效视觉token剪枝
tldr: 高分辨率多模态大模型通常产生数千视觉令牌，导致严重延迟瓶颈。现有令牌剪枝方法依赖迭代子集优化，选择开销随令牌数量增加而剧增，难以转化为实际加速。本文提出单次前向剪枝器（SFPruner），将冗余控制直接嵌入评分空间，通过语义引导岭杠杆率和排序方向掩码，在单一前向传播中实现冗余感知的重要性选择。在Qwen2.5-VL上，512令牌的选择时间从112.4毫秒大幅降至2.5毫秒，有效将理论令牌削减转化为实际推理加速，同时保持极具竞争力的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23046v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1179, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23046v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1035, \"height\": 593, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23046v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1195, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1191, \"height\": 592, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 787, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23046v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 212, \"label\": \"Table\"}]"
motivation: 高分辨率多模态大模型产生大量视觉令牌，现有迭代剪枝方法的选择开销过大，难以实现实际推理加速。
method: 提出单次前向剪枝器（SFPruner），通过语义引导岭杠杆率和排序方向掩码，在单次前向传播中实现冗余感知的令牌重要性选择。
result: 在Qwen2.5-VL上，512个令牌的选择时间从112.4毫秒降至2.5毫秒，有效将理论削减转化为实际加速，且性能极具竞争力。
conclusion: SFPruner以极低的选择开销实现高效令牌剪枝，为高分辨率多模态大模型的实时推理提供了结构化的冗余控制方案。
---

## 摘要
近期的高分辨率多模态大语言模型（MLLMs）每次输入会生成数千个视觉令牌，导致视觉令牌爆炸，从而引入严重的延迟瓶颈。虽然令牌剪枝缓解了这一问题，但最先进的子集优化方法通常依赖迭代子集构建来联合捕捉视觉多样性和指令相关性。随着视觉令牌数量增加，这种顺序依赖性带来了显著的选择开销，严重限制了理论 FLOPs 减少转化为实际 wall-clock 加速。为了解决这一局限，我们提出了单次前向剪枝器（SFPruner），这是对视觉令牌剪枝的结构化重新表述，将冗余控制直接嵌入到评分空间中，从而无需迭代组合优化。我们的非迭代框架通过两个互补机制在单次前向传递中实现了冗余感知的重要性选择。首先，为了在协方差级别减弱冗余，我们引入了语义引导的岭杠杆方案。通过整合指令相关性和视觉显著性，该机制抑制了主导的协方差方向，并减轻了表征偏差。其次，基于排名的定向掩蔽通过非对称相似性竞争解决了残余重叠问题，其中评分较高的令牌通过并行张量运算显式地抑制冗余的较低评分替代令牌。大量评估表明，我们的方法保持了稳定的选择成本，在 Qwen2.5-VL 的 512 个令牌设置下，将令牌选择过程从 112.4 毫秒减少到仅 2.5 毫秒，减少了高达 110 毫秒。这种结构效率成功地将理论上的令牌减少转化为切实的推理加速，同时在激进压缩下保持了与最先进技术高度竞争的性能。

## Abstract
Recent high-resolution Multimodal Large Language Models (MLLMs) generate thousands of visual tokens per input, leading to a visual token explosion that introduces severe latency bottlenecks. While token pruning mitigates this issue, state-of-the-art subset-optimization methods typically rely on iterative subset construction to jointly capture visual diversity and instruction relevance. As visual token counts scale, this sequential dependency introduces significant selection overhead, severely limiting the translation of theoretical FLOPs reductions into actual wall-clock speedups. To address this limitation, we propose Single-Forward Pruner (SFPruner), a structural reformulation of visual token pruning that embeds redundancy control directly into the scoring space, bypassing the need for iterative combinatorial optimization. Our non-iterative framework achieves redundancy-aware importance selection in a single forward pass through two complementary mechanisms. First, to attenuate redundancy at the covariance level, we introduce a semantics-guided ridge leverage scheme. By integrating instruction relevance and visual saliency, this mechanism suppresses dominant covariance directions and mitigates representation bias. Second, ranking-based directional masking resolves residual overlap through asymmetric similarity competition, where higher-scoring tokens explicitly suppress redundant lower-scoring alternatives via parallel tensor operations. Extensive evaluations demonstrate that our approach maintains stable selection costs, reducing the token selection process by up to 110 ms, from 112.4 ms to just 2.5 ms at 512 tokens in Qwen2.5-VL. This structural efficiency successfully translates theoretical token reductions into tangible inference speedups while preserving highly competitive performance against state-of-the-art techniques under aggressive compression.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：高分辨率多模态大语言模型（MLLMs）在处理图像或视频时会产生大量视觉令牌（如数千个），导致自注意力计算复杂度二次增长，造成严重的延迟和内存瓶颈，限制了实际部署。现有令牌剪枝方法大致分为两类：
  - **启发式方法**（如基于注意力或相似性的 Top-K 筛选）：速度快但无法有效消除空间冗余，在激进压缩下性能退化严重。
  - **子集优化方法**（如基于行列式点过程 DPP、多样性最大化、最大加权独立集 MWIS 等）：能够显式建模重要性和多样性以保持精度，但依赖迭代子集构建，选择开销随令牌数量线性增长，导致理论 FLOPs 降低无法转化为实际的推理加速。
- **整体含义**：需要在保持冗余感知选择能力的同时，消除迭代依赖，使令牌剪枝的选择过程与令牌数量无关，从而将理论节省转化为真切的 wall-clock 加速。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将冗余控制直接嵌入到评分过程中，在单次前向传递中完成冗余感知的重要性选择，无需迭代组合优化。
- **框架名称**：Single-Forward Pruner（SFPruner）。
- **三个关键步骤**：
  1. **语义引导（Semantic Guidance）**：建立视觉令牌的基础重要性得分。
     - 文本相关性：计算视觉令牌与文本指令嵌入的余弦相似度，经温度缩放和 Softmax 归一化。
     - 视觉显著性：从视觉编码器的自注意力中提取全局令牌（如 CLS）到各视觉令牌的注意力权重，若没有标准 CLS 令牌（如 Qwen2.5-VL）则聚合所有空间令牌的注意力作为代理。
     - 融合：通过线性加权结合两者，再经 Min-Max 归一化得到语义引导分数 \(\tilde{S}_{\text{guide}}\)。
  2. **协方差级别的冗余衰减：语义引导的岭杠杆得分（SG-RLS）**：
     - 计算视觉令牌的特征协方差矩阵 \(C = V^\top V\)。
     - 计算每个令牌的 Ridge Leverage Score：\(\ell_i = v_i (C + \lambda I)^{-1} v_i^\top\)，利用正则化协方差的逆来抑制主导特征方向，降低冗余令牌的得分，提升结构独特性令牌的得分。
     - 将结构得分与语义引导分数相乘：\(S_{\text{SG-RLS}, i} = \ell_i \cdot \tilde{S}_{\text{guide}, i}\)，类似于软 AND 门，要求令牌既具有结构独特性又具有语义相关性。
     - 当序列长度 \(N\) 大于特征维度 \(D\) 时，利用 Woodbury 矩阵恒等式将 \(N \times N\) 矩阵的求逆转化为 \(D \times D\) 的求逆，避免高分辨率下的巨大计算开销。
  3. **排序引导的定向掩蔽（Ranking-Based Directional Masking）**：
     - 基于 SG-RLS 得分对令牌进行排序，构造掩码矩阵 \(M_{ij} = \mathbb{I}(S_{\text{SG-RLS}, j} > S_{\text{SG-RLS}, i})\)，即得分更高的令牌可抑制得分更低的令牌，但反之不行。
     - 计算定向抑制惩罚：\(P_i = 1 - \max_j (M_{ij} \cdot C_{\text{sim}, ij})\)，其中 \(C_{\text{sim}}\) 为余弦相似度矩阵。
     - 最终冗余感知得分：\(S_{\text{final}, i} = S_{\text{SG-RLS}, i} \cdot P_i\)，然后通过单次 Top-K 操作选出最终子集。
- **特点**：所有操作（矩阵乘法、元素级比较、行间归约）均为并行张量运算，无迭代循环。

### 3. 实验设计
- **使用模型**：
  - LLaVA-NeXT-7B（处理高分辨率多图块图像）
  - Qwen2.5-VL-7B（单序列密集编码）
  - LLaVA-Video-7B（多帧视频推理）
- **基准测试（Benchmarks）**：
  - 图像理解：VQAv2、GQA、TextVQA、MME、ScienceQA（SQA）、POPE、MMB、MMB-CN、MMVet、AI2D、HallusionBench（HBench）、MMStar 等。
  - 视频理解：Video-MME、MLVU、LongVideoBench、MMVBench。
- **对比方法**：
  - 启发式：PruMerge/PruMerge+、VisionZip、VisPruner、SparseVLM 等。
  - 优化式：DivPrune、CDPruner、D²Pruner、VisionSelector（基于学习的）等。
- **评价指标**：绝对准确率、相对性能保留率（Rel.%）、选中延迟（毫秒）、FLOPs、峰值 GPU 内存、端到端推理时间等。
- **实验设置细节**：
  - 多种保留预算：如 LLaVA-NeXT 的每图块 128、64、32 个令牌（对应总量 640、320、160）。
  - Qwen2.5-VL 的 40% 和 20% 保留。
  - 视频的每帧 512 和 256 令牌。
  - 极端分辨率压力测试：Qwen2.5-VL 输入 9,216 个令牌，保留 30%。
- **延迟测量**：在单块 NVIDIA RTX 4090 GPU 上使用同步 CUDA 计时，包含语义引导、SG-RLS 评分、定向掩蔽和 Top-K 选择的完整流程，但不包括视觉编码和 LLM 前向传播。

### 4. 资源与算力
- 论文明确提到测延迟时使用单块 NVIDIA RTX 4090 GPU。
- 未提及训练过程，因为所提方法是无训练的（training-free），故不涉及大规模训练算力。
- 计算资源方面提及韩国政府的“Advanced GPU Utilization Support Program”和釜山大学的集群服务器，但未给出具体数量和型号。
- 因此，算力使用仅限于推理和延迟测量，无训练开销。

### 5. 实验数量与充分性
- **实验组数**：在三个不同模型家族、多种保留率下进行了全面的主实验，涵盖图像、多图块、单序列、视频等多种模态场景，共涉及十几个基准测试。
- **消融实验**：在 LLaVA-NeXT 上对评分方法（SG、SG-RLS）与选择策略（Top-K、顺序搜索、定向掩蔽）进行了组合消融，验证了协方差衰减与定向掩蔽的互补性。
- **极端压力测试**：专门测试了 9,216 令牌的极端场景，提供了详细的 FLOPs、内存、剪枝时间、预填充延迟和总推理时间的统计。
- **微架构分析**：提供了不同令牌数量下的端到端延迟和峰值内存对比，并进行了函数级微基准测试以验证延迟声明的准确性。
- 实验设计客观公平，所有对比方法均使用公开代码或与原论文一致的设置，性能保留率以未剪枝基线为参考，延迟测量均在同一硬件和同步条件下进行。整体实验充分，能够支撑核心论点。

### 6. 论文的主要结论与发现
- 通过将冗余控制嵌入评分过程，可以在不牺牲性能的前提下，彻底消除迭代子集构建的选择开销。
- SFPruner 在 Qwen2.5-VL 上，512 令牌保留的选择延迟从 112.4 ms 降至 2.5 ms，且不同保留率下延迟几乎恒定。
- 在多个模型和任务上，SFPruner 在激进压缩（如保留 5.5% 或 20% 令牌）下仍能保持高度竞争性能（相对性能保留 94.9%–99.0%），并且成功将 FLOPs 减少转化为实际推理加速。
- SG-RLS 与定向掩蔽分别处理全局和局部冗余，两者结合可在极小计算开销下实现接近顺序子集优化的准确性。

### 7. 优点：方法或实验设计上的亮点
- **结构化创新**：首次将冗余建模分解为协方差级和成对级，并完全通过并行张量运算实现，避免迭代，是方法论上的范式转换。
- **效率显著**：选择延迟极低且与令牌数无关，极大地释放了高分辨率 MLLM 的加速潜力。
- **通用性强**：成功应用于三种主流 MLLM 架构，涵盖图像、多图块、视频，且无需额外训练。
- **实验扎实**：多模型、多基准、多保留率下全面对比，包含极端的压力测试和详尽的消融实验，延迟测量透明可信。
- **数学技巧**：利用 Woodbury 矩阵恒等式和 Cholesky 分解优化协方差求逆，兼顾速度和数值稳定，避免 \(O(N^3)\) 开销。

### 8. 不足与局限
- **可能的信息损失**：虽然结构冗余被抑制，但在某些需要精细局部细节的任务（如 OCR、细粒度定位）中，协方差衰减和相似性抑制是否会丢弃关键细节，文中未专门分析。
- **超参数敏感性**：融合参数 \(\alpha\)、温度 \(\tau\)、正则化 \(\lambda\) 等可能对性能有影响，但文中仅在补充材料中提及详细设置，正文未深入讨论其鲁棒性。
- **缺乏与基于学习的方法对比**：仅对比了无需训练的方法和有限的训练型方法（VisionSelector），未与需要训练的剪枝策略（如轻量级选择器网络）全面比较，可能低估了其他范式的潜力。
- **视频视频长依赖**：视频实验只测试了 512/256 tokens/帧，未展示更长帧数或更极端压缩下的行为。
- **硬件依赖**：延迟受益于 GPU 的高度并行计算，在边缘设备或 CPU 上，矩阵求逆和密集张量操作的实际加速效果可能受限，但论文未讨论。
- **文本模态依存**：对于没有明显文本指令的自由形式对话或零样本场景，语义引导中的文本相关性可能失效，只能依赖视觉显著性，可能影响性能。

（完）
