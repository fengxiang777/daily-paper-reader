---
title: "Mobius Learning: Cyclic Depth Folding in Transformers"
title_zh: 莫比乌斯学习：Transformer中的循环深度折叠
authors: Tongtian Zhu
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17843v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 循环深度折叠的Transformer训练架构
tldr: 传统Transformer中浅层与深层块的角色常与深度位置绑定，限制了架构灵活性。Mobius Learning提出循环深度折叠训练架构，不同数据流以循环移位顺序经过共享块组，使同一块组交替担任浅层和深层角色，实现深度角色叠加。在基于GPT-2 small（124M）、2.5B FineWeb tokens及Muon优化器的实验中，Mobius Learning在多次块序列传递下验证损失低于固定顺序循环Transformer。这一反直觉发现打破了块角色的固定观念，开辟了循环深度折叠的新设计空间，尤其利于内存受限的分布式训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17843v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1509, \"height\": 550, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17843v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17843v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1501, \"height\": 822, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17843v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1155, \"height\": 371, \"label\": \"Table\"}]"
motivation: 挑战Transformer中块角色与深度绑定的传统假设，寻求更灵活的深度利用方式。
method: 训练时不同数据流按循环移位顺序通过同一块组，迫使块在浅层和深层角色间叠加优化。
result: 在2.5B令牌的GPT-2训练中，Mobius Learning在多次循环传递下验证损失反超固定顺序循环模型。
conclusion: 循环深度折叠可替代固定深度顺序，提升性能并特别适合内存受限的分布式训练。
---

## 摘要
基于Transformer的语言模型沿有序深度轴组织计算，浅层和深层模块通常发展出不同的表示角色。我们挑战了这些角色必须与模块在有序序列中的位置绑定的传统观点。我们引入莫比乌斯学习，一种基于循环深度折叠的训练架构，其中不同的数据流遵循循环移位的模块顺序。因此，相同的模块组对于某些数据流在模块序列的早期应用，而对于其他数据流则在后期应用，从而在浅层和深层角色中同时优化，我们将此现象称为深度角色叠加。令人惊讶的是，在使用Muon在2.5B FineWeb令牌上训练的修改版GPT-2小型（124M）模型的四工作节点实验中，莫比乌斯学习在更大的Transformer模块序列传递次数下，实现了比固定顺序循环Transformer更低的验证损失。这一反直觉的结果表明，模块组不必局限于模块序列中固定的浅层或深层角色，并开启了基于循环深度折叠的新设计空间。至关重要的是，这种结构使莫比乌斯学习特别适合内存受限的分布式训练：原始训练数据保持本地，而每个工作节点存储一个模块组而非完整的Transformer模块堆栈。

## Abstract
Transformer-based language models organize computation along an ordered depth axis, where shallow and deep blocks often develop distinct representational roles. We challenge the conventional view that these roles must remain tied to a block's position in the ordered sequence. We introduce Mobius Learning, a training architecture based on cyclic depth folding, in which different data streams follow cyclically shifted block orders. The same block group is therefore applied early in the block sequence for some data streams and late for others, so it is optimized in both shallow and deep roles, a phenomenon we call depth-role superposition. Surprisingly, in four-worker experiments with a modded GPT-2 small (124M) model trained on 2.5B FineWeb tokens using Muon, Mobius Learning achieves lower validation loss than a fixed-order looped Transformer at larger numbers of Transformer block-sequence passes. This counterintuitive result shows that a block group need not remain confined to one fixed shallow or deep role within the block sequence and opens a new design space based on cyclic depth folding. Crucially, this structure makes Mobius Learning particularly well suited to memory-constrained distributed training: raw training data remain local, while each worker stores one block group rather than the complete Transformer block stack.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在传统 Transformer 架构中，浅层和深层 block 通常发展出不同的表示角色，这些角色是否必须与 block 在序列中的固定位置绑定？
- **研究动机**：现有方法（如参数共享、循环 Transformer）均维持固定的 block 顺序，未挑战深度角色的位置依赖性。论文旨在探索相同 block 组能否同时担任浅层和深层角色。
- **整体含义**：论文提出“莫比乌斯学习”，通过循环深度折叠打破 block 角色与深度的强绑定，证明 block 组无需局限于单一深度角色，为灵活深度利用和内存受限的分布式训练开辟了新设计空间。

### 2. 论文提出的方法论
- **核心思想**：将 Transformer 划分为多个 block 组，每个 worker 持有一个 block 组和本地数据。不同 worker 的数据以循环移位的顺序遍历相同的 block 组，使得同一 block 在不同数据流中的深度位置各异，从而在浅层和深层角色中联合优化，形成“深度角色叠加”。
- **关键技术细节**：
  - **模型划分**：设共有 \( P \) 个 block 组，记为 \( F_0, F_1, ..., F_{P-1} \)。每个 worker \( i \) 持有 \( F_i \)。
  - **循环路径**：对于源于 worker \( s \) 的数据，其前向传播顺序为 \( F_s \rightarrow F_{s+1} \rightarrow ... \rightarrow F_{P-1} \rightarrow F_0 \rightarrow ... \)，循环 \( L \) 遍（总应用次数 \( K = LP \)）。形式化地，状态更新为 \( h_{s,t+1} = F_{\langle s+t \rangle_P}(h_{s,t}) \)，其中 \(\langle a \rangle_P\) 表示模运算。
  - **深度角色叠加**：定义 block \( F_i \) 在路径 \( s \) 中的源相对深度 \( d_{s,i} = \langle i - s \rangle_P \)。当 \( s \) 遍历所有 worker 时，\( d_{s,i} \) 覆盖所有可能的深度值（0 到 \( P-1 \)），使得每个 block 均被训练于所有深度角色。
  - **训练目标**：优化所有路径的平均交叉熵损失，每个路径使用独立的输入嵌入和预测映射。
- **算法流程**：前向传播中，隐藏状态按循环顺序在 worker 间传递；反向传播沿同一依赖图计算梯度。每个 worker 仅在本地存储一个 block 组，原始数据不离开 worker。

### 3. 实验设计
- **数据集与场景**：
  - 训练和验证均使用 FineWeb 数据集，总训练 token 量为 2.5B。
  - 实验场景设定为 4 个 worker 的分布式环境，每个 worker 拥有本地数据和单个 block 组。
- **Benchmark 与对比方法**：
  - 基准方法为“有序循环训练”（ordered-loop training），即所有 worker 的数据均沿固定 block 顺序 \( F_0, F_1, F_2, F_3 \) 循环 \( L \) 遍。
  - 对比在相同循环深度 \( L \) 和相同总块应用次数 \( K=4L \) 下进行，确保公平。
- **模型与配置**：使用修改版 GPT-2 small（124M 参数），优化器为 Muon，学习率计划、全局批次大小等超参数在所有运行中固定。

### 4. 资源与算力
- **明确说明**：论文中未提及 GPU 型号、数量、训练时长或具体算力消耗。仅提到使用四工作节点（four workers），但未描述硬件配置。

### 5. 实验数量与充分性
- **实验数量**：主要实验涉及 5 个循环深度设置（\( L = 1, 2, 6, 10, 15 \)），每个设置下对比了莫比乌斯学习和有序循环训练的验证损失，共 10 组对比结果。
- **充分性与公平性**：
  - 实验在 token 预算、应用次数、模型尺度和优化器上均严格匹配，对比公平。
  - 然而，实验仅限于单一模型规模（124M）、单一 worker 数量（4）和单一数据集，缺乏消融实验（如改变 worker 数量、block 组大小）和泛化性验证，整体实验规模较小，结论的普适性有待进一步检验。

### 6. 论文的主要结论与发现
- **主要结论**：
  - 通过莫比乌斯学习，同一 Transformer block 组可被成功训练为同时服务浅层和深层角色，即实现深度角色叠加。
  - 在较大的循环深度（\( L = 6, 10, 15 \)）下，莫比乌斯学习取得了比有序循环训练更低的验证损失，表明深度角色的去固定化不会损害性能，反而能带来增益。
  - 该架构天然适合内存受限的分布式训练，因为每个 worker 只需存储一个 block 组，而非完整模型层数。
- **关键发现**：block 组的角色不应被其位置所固化；循环深度折叠提供了一种有效且内存友好的新训练范式。

### 7. 优点
- **新颖性**：首次提出通过循环移位顺序训练实现深度角色叠加，挑战了 Transformer 的传统深度假设。
- **反直觉结果**：在固定顺序被视为自然选择的背景下，实验显示循环折叠能反超基线的验证损失，具有理论启发价值。
- **应用潜力**：明确示了在分布式训练中的内存优势，为联邦学习、拆分学习等场景提供了新思路。
- **方法严谨性**：公式化定义了源相对深度和循环折叠机制，架构清晰。

### 8. 不足与局限
- **实验覆盖有限**：
  - 仅使用 124M 小型模型和 2.5B tokens 进行实验，未在更大规模模型（如 1B+参数）或长程训练中验证。
  - Worker 数量固定为 4，未探索该参数变化的影响。
  - 缺乏消融实验来隔离深度角色叠加的具体贡献（如对比随机路径或不同块组数量）。
- **算力信息缺失**：未报告任何硬件细节，难以评估方法的实际资源需求和效率。
- **应用范围**：仅在语言模型预训练任务上测试，未涉及微调、多模态或其他任务，通用性待证明。
- **潜在偏差**：实验结果仅基于 FineWeb 数据集，可能受数据特性影响。

（完）
