---
title: "Experience Memory Graph: One-Shot Error Correction for Agents"
title_zh: 经验记忆图：智能体的一次性错误纠正
authors: "Wenjun Wang, Yuchen Fang, Fengrui Liu, Zibo Liang, Kai Zheng"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13884v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 通过经验记忆图匹配解决长周期任务中LLM智能体的错误恢复问题
tldr: 在复杂长程任务中，LLM智能体常因错误累积而难以恢复。本文提出经验记忆图（EMG）框架，将失败恢复建模为图匹配问题，通过从探索轨迹和专家轨迹中提取图编辑路径来指导纠正。实验表明，EMG在ALFWorld和ScienceWorld上成功率和平均奖励均优于现有反射基线，且无需测试时反复试错。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13884v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13884v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1772, \"height\": 939, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13884v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13884v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 335, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13884v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 879, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13884v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 808, \"height\": 818, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13884v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 875, \"height\": 195, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13884v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 548, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13884v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13884v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 910, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13884v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 941, \"height\": 980, \"label\": \"Table\"}]"
motivation: 现有基于提示的自我纠正机制脆弱、成本高且难以泛化，需要一种无需测试时迭代试错的高效错误恢复方法。
method: 将失败探索和成功专家轨迹转化为有向动作决策图，通过图匹配提取共同子结构和编辑路径，构建跨任务记忆图，测试时单次检索指导无循环执行。
result: 在ALFWorld和ScienceWorld上，EMG的成功率和平均奖励一致超越反射基线方法。
conclusion: EMG通过图记忆和单次校正实现了高效、可泛化的智能体错误恢复，避免了测试时的试错开销。
---

## 摘要
大型语言模型（LLM）智能体通过生成状态、动作和观察的序列轨迹，在自主决策方面展现了显著的能力。然而，在复杂的长程任务中，这些智能体频繁遭受复合错误，并难以从失败中恢复。现有的自我纠正机制依赖于基于提示的反思，这本质上是脆弱的，由于迭代试错循环而带来沉重的时间和API成本，并产生特定于任务的记忆，可能难以泛化到新场景。为了解决这个问题，我们提出了经验记忆图（EMG），一个将智能体失败恢复重新表述为图匹配问题的框架。在训练时，我们将失败的探索轨迹和成功的专家轨迹都转换为有向动作决策图。通过匹配这些图，我们提取公共子图（成功的工作流）和明确指示如何纠正失败的图编辑路径（例如，在给定观察下添加、删除或重新标记哪些动作），并将它们存储在一个具有任务内节点和跨任务边的记忆图中。在测试时，EMG检索相关见解并以单次无循环执行的方式引导智能体。在ALFWorld和ScienceWorld上的实验表明，EMG在成功率和平均奖励方面始终优于最先进的反思基线，同时不需要测试时的试错。

## Abstract
Large Language Model (LLM) agents have shown remarkable capabilities in autonomous decision-making by generating sequential trajectories of states, actions, and observations. However, in complex, long-horizon tasks, these agents frequently suffer from compounding errors and struggle to recover from failures. Existing self-correction mechanisms rely on prompt-based reflection, which is inherently brittle, incurs heavy time and API costs due to iterative trial-and-error loops, and produces task-specific memory that may be hard to generalize to new scenarios. To address this, we propose Experience Memory Graph (EMG), a framework that reformulates agent failure recovery as a graph matching problem. At training time, we convert both failed exploration trajectories and successful expert trajectories into directed action decision graphs. By matching these graphs, we extract common subgraphs (successful workflows) and graph edit paths that explicitly indicate how to correct failures (e.g., which actions to add, delete, or relabel under a given observation), and store them in a memory graph with intra-task nodes and cross-task edges. At test time, EMG retrieves relevant insights and guides the agent in a single, loop-free execution. Experiments on ALFWorld and ScienceWorld show that EMG consistently outperforms state-of-the-art reflection baselines in success rate and average reward, while requiring no test-time trial-and-error.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：大型语言模型（LLM）智能体在执行复杂、长周期的任务时，会因错误累积而频繁失败，且难以从失败中自我恢复。现有的自我纠正机制主要依赖**基于提示的反思**，存在三个主要局限：
    *   **对LLM能力依赖性强**：反思效果受提示设计和模型自身推理能力限制，小模型尤其容易陷入错误循环。
    *   **推理成本高**：需要多轮“反思-重放”迭代，导致极高的时间延迟和API调用费用，不适合实时应用。
    *   **跨任务泛化能力弱**：产生的记忆是任务特异性的，难以迁移到新场景。
*   **整体含义**：本文提出“经验记忆图”（EMG）框架，旨在将智能体的错误恢复问题**从根本上进行转换**，从依赖LLM在线反思的范式，变为一个**离线、确定性的图计算与匹配问题**，从而实现高效、一次性且可泛化的错误纠正。

### 2. 论文提出的方法论

核心思想是通过在训练阶段离线构建一个结构化的经验记忆图，在测试时直接检索纠正方案，避免在线试错。

*   **核心思想与流程**：
    1.  **准备阶段**：为每个训练任务收集一个探索轨迹（可能失败）和一个成功专家轨迹，将它们转换为**有向动作决策图**。
    2.  **记忆图构建**：通过图匹配技术，从失败-成功图对中提取**公共子图**（成功工作流）和**图编辑路径**（具体纠正指令），生成**任务内洞察（Intra-task Insight）** 存储为记忆图的**节点**。通过匹配相似任务的成功图对，提取**跨任务洞察（Cross-task Insight）** 存储为记忆图的**边**。此过程完全离线。
    3.  **测试时检索**：新任务到来时，只需在记忆图中检索最相关的节点和边，将获得的纠正性洞察直接注入智能体的提示中，引导其**单次、无循环**地完成任务。

*   **关键技术细节**：
    *   **动作决策图构建**：将轨迹转化为图，节点表示抽象动作（如`(take, apple, table)`），边表示触发该动作的前置观察。通过**节点复用**和**无效动作并行化**的设计，使图结构能更精确地捕捉决策逻辑。
    *   **图匹配与洞察提取**：
        *   采用基于最优传输的**融合格罗莫夫-瓦瑟斯坦（FGW）距离**来求解两个图之间的节点匹配。
        *   基于匹配结果，通过专门的算法（Algorithm 2和Algorithm 3）提取**公共子图**（哪些动作在特定观察下是正确的）和**图编辑路径**（添加、删除或替换哪些节点/边），这些路径构成了确定的、一步到位的错误纠正计划。
    *   **洞察生成**：将提取的图结构信息（公共子图、编辑路径）连同原始查询和图一同交给LLM，总结成自然语言的**任务内洞察**和**跨任务洞察**，存储在记忆图的节点和边上。
    *   **测试时检索公式**：
        *   对于测试查询 \( q_{test} \)，检索与查询嵌入相似度最高的前 \( K \) 个节点。
        *   对于每个检索到的节点，再检索其连接的前 \( T \) 个最相似邻居的跨任务边。
        *   最终检索结果 \( \mathcal{R}(q_{test}) \) 整合了这些节点和边的洞察。

### 3. 实验设计

*   **数据集/场景**：
    *   **ALFWorld**：具身家务任务，评估空间记忆和顺序执行能力。成功率为0或1。
    *   **ScienceWorld**：交互式科学实验模拟，轨迹极长，评估长期规划、记忆保持和复杂科学推理能力。奖励为0到1之间的连续值。
    *   两个数据集均使用ETO论文中的划分，分为训练集、测试集-已见（Seen）和测试集-未见（Unseen）。

*   **基准对比方法**：
    *   **ReAct**：无记忆的基础模型。
    *   **Reflexion**：通过短期记忆进行迭代反思。
    *   **ExpeL**：从历史轨迹中积累规则的经验学习基线。
    *   **CDMem**：基于反思构建短期和长期记忆。
    *   **MemP**：构建并复用程序性记忆。
    *   *注：EMG采用ReAct作为基础模型，在测试时检索记忆。对比的反思方法（Reflexion, ExpeL, CDMem）均运行5次迭代。*

### 4. 资源与算力

*   论文**未明确说明**构建经验记忆图或进行实验时使用的**GPU型号、数量或训练时长**。
*   论文提到使用以下模型进行轨迹收集、记忆构建和测试推理，但未涉及微调，主要成本是API调用和推理：
    *   **DeepSeek V3.2 (chat mode)**: 用于收集ScienceWorld的探索轨迹及构建其经验记忆图。
    *   **Deepseek V4-flash (chat mode)**: 用于收集ALFWorld的探索轨迹及构建其经验记忆图。
    *   **Qwen3:4B-instruct-2507-q8**: 作为测试用的小模型。
    *   **DeepSeek-V4 flash (chat mode)**: 作为测试用的大模型。

### 5. 实验数量与充分性

*   **实验数量**：实验设计较为全面，覆盖了多个维度。
    *   **主要结果（2组）**：在2个数据集（ALFWorld, ScienceWorld）的2个测试集划分（Seen, Unseen）上，使用2种规模的语言模型（Qwen3-4B, DeepSeek-V4-Flash）对比了6种方法（含EMG），报告了成功率（SR）和平均奖励（AR）两个核心指标。
    *   **消融实验（2组）**：在两个数据集和两种模型上，对EMG的两个核心组件（节点记忆、边记忆）进行了消融，以验证各部分的贡献。
    *   **范式对比实验（1组）**：专门设计了实验，对比“一次性记忆”方法与“迭代纠正”方法的成功率和效率。
    *   **时间成本对比实验（1组）**：在ScienceWorld上，对比了EMG与各迭代基线方法的总推理时间。
    *   **超参数敏感性分析（2组）**：在两个数据集上，分析了检索节点数（top-k）对成功率的影响。
*   **充分性与公平性**：
    *   **充分**：实验覆盖了不同难度（短/长周期）、不同测试分布（已见/未见）和不同能力的模型，并进行了组件消融、效率对比和参数分析，结果全面且相互印证。
    *   **客观公平**：所有方法使用统一的数据集划分，迭代方法的迭代次数一致（5次），对比的指标（成功率、奖励、时间）客观。EMG的离线构建成本被明确解释为一次性的，摊销到多个测试任务上，比较逻辑合理。

### 6. 论文的主要结论与发现

*   **性能卓越**：EMG在所有数据集、测试划分和模型规模上，成功率和平均奖励**一致地显著优于**所有基于在线反思迭代的基线方法。
*   **弥补小模型短板**：EMG对小模型的提升效果**比对大模型更为显著**，表明其提供的结构化纠正知识可以弥补小型LLM推理能力的不足。在ScienceWorld上，配备EMG的4B模型性能可媲美甚至超越使用标准基线的大模型。
*   **跨任务泛化有效**：消融实验证实，跨任务洞察（边）对提升小模型的未见过任务性能尤其关键，而任务内洞察（节点）是坚实的基础。
*   **效率优势巨大**：EMG仅需**单次**测试执行，不仅成功率高，而且总推理时间**远低于**需要5次迭代试错的反思方法，显著节省了时间和API成本。
*   **方法鲁棒**：超参数敏感性分析显示，EMG的性能对检索节点数（top-k）变化不敏感，表现出很强的鲁棒性。

### 7. 优点

*   **范式创新**：创造性地将LLM智能体的错误恢复问题，从依赖模型推理的“软”反思，转化为基于图匹配的“硬”计算，从根本上解决了在线反思成本高、不稳定和泛化难的问题。
*   **结构化的知识表征**：设计了“动作决策图”和“经验记忆图”，将非结构化的轨迹数据和纠正逻辑，以图结构进行精确、可解释的存储和复用，特别是节点复用和无效动作并行化的设计，使图编辑路径能产生更有意义的纠正洞察。
*   **一次性与高效性**：通过离线构建、在线检索的方式，在测试时实现了“一次性”错误纠正，无需迭代试错，极大地提升了推理效率。
*   **模型无关的增强能力**：EMG作为记忆增强框架，能与不同规模的LLM结合，尤其能显著增强小型模型的复杂推理能力，缩小了与小模型与大模型间的性能差距。

### 8. 不足与局限

*   **依赖专家轨迹**：EMG的构建强依赖于**高质量的“成功专家轨迹”**。在现实世界的许多开放性任务中，获取这样的专家轨迹可能成本高昂甚至不可能。
*   **离线构建的计算开销**：论文未明确讨论构建阶段的算力成本。尽管是离线一次性投入，但当训练任务数量庞大时，对所有轨迹进行图构建和配对图匹配（FGW求解器复杂度为 \( O(|V|^3 \cdot |L|) \)）的计算开销仍不容忽视。
*   **对语义匹配的挑战**：图匹配依赖于离散标签的相等性（节点标签对应标准化的动作元组，边标签对应标准化的观察文本）。对于语义上等价但表述不同的动作或环境反馈，这种严格的标签匹配可能会失效，导致无法识别出相同的决策模式。
*   **测试任务类型的局限**：实验仅在相对受控的文本交互环境（ALFWorld, ScienceWorld）中验证。其在更开放、更复杂的现实世界多模态任务（如图形界面操作、物理机器人等）中的有效性尚待探索。

（完）
