---
title: "Subagents vs Agent Skills: Executing Reusable Knowledge for Long-Horizon Agentic Tasks"
title_zh: 子代理与代理技能：执行可重用知识以解决长时程代理任务
authors: "Wasu Top Piriyakulkij, Rachel Lawrence, Alicia Curth, Sushrut Karmalkar, Niranjani Prasad"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.09233v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 语言模型智能体在长时程任务中执行可复用技能包，解决上下文累积问题
tldr: 长程任务中，将可重用技能指令直接加载进主上下文会因上下文累积导致推理质量下降。本文探索将技能包作为子代理调用，为每个子任务开启独立上下文窗口。实验表明，当技能包提供清晰输入输出契约和过程知识时，子代理执行优于技能执行，但需额外通信开销。研究揭示可重用知识效益还取决于其组织与调用方式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.09233v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1136, \"height\": 495, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1286, \"height\": 623, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 746, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1445, \"height\": 699, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1421, \"height\": 1082, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1445, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09233v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1427, \"height\": 794, \"label\": \"Figure\"}]"
motivation: 在长程任务中，直接加载技能指令到主上下文会导致推理质量随上下文增长而下降。
method: 将技能包作为子代理调用，为各子任务开启独立上下文窗口，通过额外通信协调。
result: 当技能包具备清晰输入输出契约和过程知识时，子代理执行表现优于技能执行。
conclusion: 可重用知识的效益取决于其内容以及组织和调用方式，子代理是一种有效替代方案。
---

## 摘要
语言模型代理如何有效利用可重用知识库来解决长时程任务？近期工作越来越关注代理技能：以技能包形式表示的可重用能力，即包含指令、脚本和其他资源的多文件捆绑包，帮助代理执行特定任务。代理技能通常通过将技能指令加载到代理上下文中并依赖代理遵循这些指令来执行。然而，随着任务时程变长，这种方法变得越来越脆弱，因为随着上下文窗口中信息积累，推理质量会下降。我们研究了一种替代方法，即技能包作为子代理来调用。子代理执行不是将技能指令加载到主上下文中，而是生成专门用于解决单个子任务的全新上下文窗口。我们表明，当技能包暴露清晰的输入-输出契约且其指令编码了满足这些契约所需的过程性知识时，子代理执行优于代理技能执行。权衡是额外的通信开销，因为需要额外的标记在主代理与其子代理之间协调。我们的结果表明，可重用知识的优势不仅取决于其内容，还取决于其组织和调用方式。

## Abstract
How can language model agents effectively leverage libraries of reusable knowledge to solve long-horizon tasks? Recent work has increasingly focused on agent skills: reusable capabilities represented as skill packages, i.e., multi-file bundles containing instructions, scripts, and other resources that help agents perform specific tasks. Agent skills are typically executed by loading their skill instructions into an agent's context and relying on the agent to follow them. As task horizons grow, however, this approach becomes increasingly brittle, because reasoning quality degrades as more information accumulates in the context window. We investigate an alternative approach in which skill packages are instead invoked as subagents. Rather than loading skill instructions into the main context, subagent execution spawns fresh context windows dedicated to solving individual subtasks. We show that subagent execution outperforms agent-skill execution when skill packages expose clear input-output contracts and their instructions encode the procedural knowledge needed to fulfill those contracts. The tradeoff is additional communication overhead, as extra tokens are required to coordinate between the main agent and its subagents. Our results show that the benefit of reusable knowledge depends not only on its content, but also on how it is organized and invoked.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：语言模型代理（LLM agents）在长时程任务中需要复用领域知识，近期的主流做法是“代理技能”（agent skills）——将可复用能力打包为多文件技能包（skill package），调用时将技能指令（如 `SKILL.md`）加载进主代理上下文，由主代理自行执行。
- **核心问题**：随着任务时程变长，主上下文累积大量工具输入输出和中间推理，导致 LLM 推理质量下降（带宽受限的注意力问题）。即使技能包包含有用知识，直接加载技能指令反而会加剧上下文过载。
- **整体含义**：本文探索将技能包作为**子代理**（subagent）调用，为每个子任务开启独立上下文窗口，只返回最终输出给主代理。结果表明：可重用知识的效益不仅取决于其内容，还取决于其组织与调用方式；当技能包具备清晰输入-输出契约和过程性知识时，子代理执行优于代理技能执行。

## 2. 论文提出的方法论

- **形式化框架**：
  - 工具调用代理在每轮生成响应 `r_t`、工具 `k_t` 和参数 `x_t`，执行器 `E` 返回输出 `o_t`，上下文通过追加交互轨迹更新。
  - **代理技能**：技能包 `s = (d, m, R)`，其中 `d` 是描述（供技能发现）、`m` 是指令文件（`SKILL.md`）、`R` 是资源集合。调用代理技能 `AgentSkill(s)` 不返回执行结果，仅把指令文件 `m` 加载进主上下文，由主代理按指令逐步执行。
  - **子代理**：`Subagent(s)` 不向主代理暴露指令文件，而是由工具执行器实例化一个独立策略 `π(k)(·) = π_LLM(m_k ⊕ ·)`，以任务输入 `x_t` 初始化新上下文，在独立上下文中执行完整工具调用推理过程，只返回最后响应 `r(k)_T`。
- **有效子代理的关键设计**：
  - 借鉴强化学习中的 **options 框架**（`ω = (I_ω, π_ω, β_ω)`），将技能描述结构化为 `d = (q_in, h, q_out)`，其中 `q_in` 是输入契约（可调用条件），`h` 是目的摘要，`q_out` 是输出契约（返回内容约束）。
  - 修改后的技能包为 `s = ((q_in, h, q_out), m, R)`，使 `Subagent(s)` 实现从合法输入集合到输出集合的映射。
  - **核心思想**：通过信息封装（隐藏子代理内部轨迹）降低主上下文峰值长度，但代价是主代理与子代理之间需要通信，总 token 数增加。

## 3. 实验设计

- **数据集 / 基准**：
  - 使用 **SkillsBench** 基准，包含 87 个跨领域长时程代理任务；本文在其 64 个任务子集上报告结果。
  - 使用 **OpenHands** 作为代理执行框架，并对原始 SDK 做了少量修改（延长子代理超时、移除 OpenHands 原生技能、禁用自动技能检索）。
- **技能包构造**：
  - **原始 curated 技能包**：SkillsBench 自带的人类编写技能包，通常描述相关知识，但很少明确指定技能的预期输入和输出。
  - **合成过程性技能包**：先用 GPT-5.3 Codex 在 OpenHands 上运行所有任务三次，收集成功轨迹；再用 Copilot CLI（GPT-5.5 驱动）以人工干预极少的方式，为每个有成功轨迹的任务合成 3-5 个带明确输入输出契约的过程性技能包，共覆盖 64 个任务。
- **对比方法 / 实验组**：
  - **执行模式**：代理技能执行（agent skill） vs. 子代理执行（subagent）。
  - **技能包类型**：无 I/O 契约的原始技能包 vs. 有 I/O 契约的合成技能包。
  - **基础 LLM**：Ministral-8B、Gemma-4-12B、Qwen3.5-9B、Mistral-Large-3.1、gpt-5.4-mini、Kimi-K2.6、gpt-5.3-codex。
  - **上下文压力实验**：通过加入干扰工具（0、100、200、263 个）增加初始上下文长度，观察两种执行模式随上下文增大的鲁棒性。
  - **成本测量**：比较子代理与代理技能在每任务上的峰值上下文长度和总 token 消耗。
  - **附录实验**：技能库组织（扁平、层级树、层级图、两级任务树）与执行模式（全代理技能、全子代理、混合）的对比，在 Qwen3.5-9B 上进行。

## 4. 资源与算力

- **GPU 资源**：实验在 **NVIDIA A100 GPU 节点**上运行。
- **模型部署方式**：
  - 开放权重模型（Qwen3.5 家族 2B/4B/9B、Ministral-3-8B、Gemma-4-12B-it）使用 **vLLM** 在 A100 节点 GPU 上本地服务。
  - 闭源或大模型（GPT-5.x 家族、Mistral-Large-3、Kimi-K2.6）通过托管 API 访问，不需要本地 GPU。
- **训练时长 / GPU 数量**：论文 **未明确说明** GPU 卡数、总计算时长或推理耗时；由于主要是推理评估而非训练，未报告训练相关算力。

## 5. 实验数量与充分性

- **实验规模**：
  - 主实验覆盖 **7 个 LLM × 2 种执行模式 × 2 种技能包类型**，共 28 个主要对比条件，在 64 个任务子集上评估。
  - 干扰工具实验涉及多个干扰数量（0、100、200、263），并给出了部分模型和全部模型（附录图 6）的趋势图。
  - 峰值上下文与 token 成本实验覆盖 7 个模型。
  - 附录中的技能库组织实验在单个模型（Qwen3.5-9B）上比较 4 种库结构 × 3 种执行模式。
- **充分性与客观性**：
  - 实验覆盖多个模型规模和类型，主结果较全面；干扰实验和成本分析增强了说服力。
  - **潜在问题**：合成技能包与原始技能包在内容上不同，因此二者的对比不是严格受控实验；部分实验（如层级库）仅在一个模型上进行，限制了泛化结论；未报告统计显著性检验或多次运行方差；合成技能包过程本身使用了 GPT-5.3/5.5，可能与评估模型存在同源偏差。

## 6. 论文的主要结论与发现

- **执行模式与技能包设计存在交互**：当使用原始 curated 技能包（无 I/O 契约）时，代理技能执行匹配或优于子代理执行；但当使用带明确输入输出契约的过程性技能包时，子代理执行显著优于代理技能执行，且对小模型（带宽受限更严重）优势更大。
- **子代理在上下文增长时更鲁棒**：随着干扰工具数量增加，子代理执行的任务准确率下降更平缓，说明降低峰值上下文长度有助于对抗上下文过载。
- **成本权衡**：子代理执行在强模型上能降低超过 80% 任务的峰值上下文长度，但总 token 消耗显著高于代理技能执行，因为需要重复提供上下文信息用于通信。
- **组织与调用方式很重要**：过程性、契约驱动的技能可以从成功轨迹中合成，这为学习适合子代理执行的技能提供了可行路径；技能库的层级组织（尤其是图结构和混合执行模式）可进一步提升性能。
- **模块化视角**：子代理执行本质上是强制模块化，通过清晰接口封装子任务知识，控制信息流，有助于扩展代理系统。

## 7. 优点

- **形式化清晰**：将代理技能和子代理统一到同一工具调用框架下，并类比强化学习 options，给出了技能包应具备输入输出契约的理论依据。
- **实验设计针对性强**：直接对比同一技能包在两种执行模式下的效果，并区分了技能包设计质量的影响；干扰工具实验模拟真实环境中的工具泛滥，验证了上下文压力的作用。
- **多模型覆盖**：包含从小型开放模型到大型闭源模型，展示了模型规模与带宽限制的关系。
- **成本分析透明**：同时报告了峰值上下文长度和总 token 成本，揭示性能提升的代价。
- **实践意义**：展示了从成功轨迹合成契约驱动技能包的可行性，并初步探索了技能库层级组织和混合执行模式，为实际系统设计提供参考。

## 8. 不足与局限

- **对比不够严格**：合成技能包与原始技能包内容不同，无法完全分离“内容质量”和“执行模式”的影响；合成过程依赖高级模型，可能引入偏差。
- **基准单一**：仅在 SkillsBench 一个基准上评估，未在其他长时程代理任务或真实生产环境中验证。
- **部分实验覆盖有限**：技能库组织实验只在 Qwen3.5-9B 上进行；干扰实验的趋势虽一致，但未在所有模型上完整呈现。
- **弱模型下峰值上下文降低不稳定**：因弱模型在代理技能模式下可能提前终止，导致峰值上下文不可比，削弱了部分结论的普适性。
- **通信开销未优化**：虽然提出“最小契约”作为方向，但未实际探索减少通信 token 的方法；总 token 成本较高，可能限制实际部署效率。
- **缺少统计细节**：未报告多次运行的平均值和方差，也未进行显著性检验；未说明 GPU 卡数和具体推理时间，算力报告不完整。
- **契约设计依赖人工/强模型干预**：输入输出契约的生成仍需要人工或强大模型辅助，自动化程度有限；对于无法清晰定义 I/O 的技能，子代理优势可能不成立。

（完）
