---
title: "AgentGrad: Intervention-guided Prompt Optimization for Multi Agent Systems"
title_zh: AgentGrad：面向多智能体系统的干预引导提示优化
authors: "Jaewon Chu, Jinwoo Seo, Jaewon Cho, Jeehye Na, Yunyang Xiong, Youngdae Kim, Hyunwoo J. Kim"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08572v1"
tags: ["query:rl-mm-llm-ag"]
score: 9.0
evidence: 针对LLM多智能体系统的文本梯度提示优化方法
tldr: 现有多智能体提示优化中的文本梯度方法存在梯度提取与聚合缺陷，难以精准定位失败代理并泛化提示。AgentGrad提出顺序干预与语义文本梯度抽象框架，通过逐代理干预定位目标代理并提取细粒度梯度，再聚类抽象泛化梯度。实验在五个多智能体基准上达到最优，优化时间平均减少2.5倍。该方法显著提升了多智能体系统的提示优化效率与性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08572v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 834, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 583, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1432, \"height\": 1058, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.08572v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 736, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有文本梯度优化在多智能体系统中因缺乏代理级监督与梯度泛化，导致失败定位不准且提示泛化差。
method: 通过顺序干预逐个修改代理以定位解决失败的目标代理，并利用其输出进行语义聚类与抽象，生成泛化梯度。
result: 在五个多智能体基准上达到最先进性能，墙钟优化时间比第二快基线平均减少2.5倍。
conclusion: AgentGrad通过干预引导与语义抽象解决了多智能体提示优化的两大瓶颈，显著提升效率与性能。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）通过使用专用多个智能体实现了强大性能，但其性能取决于每个智能体的提示设计。对于 MAS 提示优化，使用自然语言反馈指导提示更新的文本梯度方法已成为领先范式。在本文中，我们识别了现有文本梯度方法两个阶段的局限性：梯度提取和梯度聚合。在梯度提取中，先前工作选择一个目标提示而不验证修改它是否能解决失败，且在没有对相应智能体中间输出进行智能体级别监督的情况下推导梯度。在梯度聚合中，各个梯度被随机分组并拼接，常常混合不相关的失败模式，产生无法泛化的提示。为解决这些局限性，我们提出 AgentGrad，一个基于顺序干预和语义文本梯度抽象的多智能体系统提示优化框架。对于每个失败，顺序干预逐一修改一个智能体的行为，以识别其修改能解决失败的目标智能体。目标智能体修改后的输出随后作为智能体级别监督，用于提取细粒度梯度。语义文本梯度抽象对语义相似的梯度进行聚类，以避免混合不相关的失败模式，并将每个聚类抽象为一个泛化梯度，捕获共享的纠正模式。实验结果表明，AgentGrad 在五个 MAS 基准上实现了最先进的性能，并且与次快基线相比，平均将实际优化时间减少了 2.5 倍。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) achieve strong performance by employing specialized multiple agents, yet their performance depends on the prompt design of each agent. For MAS prompt optimization, textual gradient methods that guide prompt updates using natural-language feedback have emerged as a leading paradigm. In this paper, we identify limitations in two stages of existing textual gradient approaches: gradient extraction and gradient aggregation. In gradient extraction, previous works select a target prompt without verifying whether modifying it resolves the failure, and derive gradients without agent-level supervision over the corresponding agent's intermediate output. In gradient aggregation, individual gradients are randomly grouped and concatenated, often mixing unrelated failure modes and producing prompts that fail to generalize. To address these limitations, we propose \textbf{AgentGrad}, a prompt optimization framework for multi-agent systems based on sequential intervention and semantic textual gradient abstraction. For each failure, sequential intervention modifies the behavior of one agent at a time to identify the target agent whose modification resolves the failure. The modified output of the target agent then serves as agent-level supervision for extracting a fine-grained gradient. Semantic textual gradient abstraction clusters semantically similar gradients to prevent mixing unrelated failure modes, and abstracts each cluster into a generalized gradient that captures the shared corrective pattern. Experimental results show that AgentGrad achieves state-of-the-art performance across five MAS benchmarks and reduces wall-clock optimization time by $2.5\times$ on average compared to the next-fastest baseline.

---

## 论文详细总结（自动生成）

# AgentGrad 论文详细总结

## 1. 论文的核心问题与整体含义

- **研究背景**：基于大语言模型（LLM）的多智能体系统（MAS）通过多个专业化智能体协作，在推理、规划、信息综合等任务上表现优异。然而，每个智能体的行为由各自的输入提示（prompt）控制，提示设计对系统整体性能影响极大。
- **现有方法局限**：文本梯度方法（textual gradient）是当前 MAS 提示优化的重要范式，通过自然语言反馈迭代修正提示。
- **核心问题**：现有文本梯度方法在以下两个阶段存在系统性缺陷：
  - **梯度提取阶段**：
    - 选择目标提示时不验证“修改该提示是否真能解决系统失败”；
    - 仅使用系统级输出对比，缺乏对智能体自身中间输出的监督。
  - **梯度聚合阶段**：
    - 样本级梯度被随机分组并直接拼接；
    - 容易混合不相关的失败模式，生成的提示泛化性差。
- **整体含义**：论文提出 AgentGrad，通过“顺序干预”精准定位失败责任智能体，并以干预后的输出作为智能体级监督；通过“语义文本梯度抽象”按纠正模式聚类，避免无关信号混合，提升提示更新质量与泛化能力。

## 2. 论文提出的方法论

AgentGrad 的核心思想是：**先通过顺序干预定位失败责任智能体，再用该智能体干预前后的输出差异提取细粒度文本梯度，最后对梯度进行语义聚类抽象，产生泛化更新方向。**

### 2.1 顺序干预与目标提示识别

- 给定当前提示集合 \(P\)，对每个失败样本 \((x_i, y_i)\)，从最后一个智能体开始，**逆序**逐个注入提示（hint）\(H\)，观察哪个智能体的修正能够使系统输出达到最大奖励 \(r_{\max}\)。
- 被识别为目标的智能体记为 \(\pi_n\)，其干预后解决的失败样本组成集合 \(T_n\)。
- 逆序干预的原因：论文观察到失败往往集中在后期智能体，因此减少平均干预次数。
- 无法被任何干预解决的样本在本轮训练中暂时排除，但下一轮会重新评估。

### 2.2 智能体级监督与文本梯度提取

- 对于目标智能体 \(\pi_n\) 和失败样本 \((x_i,y_i)\in T_n\)：
  - 原始输出：\(\hat{y}_{ni} = \pi_n(x_{ni}; p_n)\)
  - 干预后输出：\(\tilde{y}_{ni} = \pi_n(x_{ni}; p_n, H)\)
- 由于两者使用相同输入上下文，差异可隔离干预引起的行为变化。
- 把 \(\tilde{y}_{ni}\) 视为“智能体级伪标签”，用 LLM 提取样本级文本梯度：
  \[
  \delta_{ni} = \text{LLM}_{\nabla}(p_n, x_{ni}, \hat{y}_{ni}, \tilde{y}_{ni})
  \]
- 这一步避免了传统方法需要显式构造系统级损失函数的限制。

### 2.3 语义文本梯度抽象

- 对每个智能体 \(\pi_n\)，收集其所有样本级梯度 \(\Omega_n\)。
- 使用一个聚合 LLM 完成两个耦合操作：
  1. **语义聚类**：将 \(\Omega_n\) 中语义相似的梯度分组，形成语义小批量 \(D_{nj}\)；
  2. **抽象泛化**：对每个聚类提取一个泛化梯度 \(\bar{\delta}_{nj}\)，捕捉共享的纠正模式。
- 聚类大小软下界循环变化（如 \(5 \to 3 \to 1 \to 5\)），以在粗粒度全局模式和细粒度局部修正之间交替。

### 2.4 提示更新与验证

- 按语义小批量规模从大到小更新提示；
- 对每个泛化梯度，LLM 生成候选提示：
  \[
  p_n^{\text{new}} = \text{LLM}_{\text{PromptOptimizer}}(p_n, \bar{\delta}_{nj})
  \]
- 候选提示先在语义小批量上验证，若提升再在验证集上验证，两阶段都通过才接受更新。

### 2.5 算法流程概览（对应 Algorithm 1）

1. 构建失败样本集 \(F\)；
2. 逆序对每个智能体进行干预，划分目标样本集 \(T_n\)；
3. 对每个目标智能体，提取样本级梯度 \(\delta_{ni}\)；
4. 对每个智能体的样本级梯度执行语义聚类与抽象，得到泛化梯度；
5. 按聚类大小降序尝试更新提示，经过小批量和验证集双重筛选，接受有效更新；
6. 在 rollout 预算内重复上述过程，最终返回优化后的提示集合。

## 3. 实验设计

### 3.1 数据集与场景

- 五个 MAS 基准，覆盖多种任务类型：
  - **HotpotQA**：多跳问答；
  - **HoVer**：事实核查/声明验证；
  - **IFBench**：指令跟随；
  - **PUPA**：隐私保护的智能体委派；
  - **MATH**：数学推理。
- 前四个基准的 MAS 结构、数据划分和奖励函数沿用 GEPA 的设置；MATH 沿用 MACM 的设置。

### 3.2 对比方法

- 基线（No PO，不优化提示）；
- MIPROv2；
- TextGrad；
- GEPA；
- **AgentGrad（本方法）**。

### 3.3 骨干模型

- 两种 LLM 骨干：
  - **GPT-5-mini**（闭源）；
  - **Qwen3-8B**（开源）。
- 同一骨干同时用于任务执行和所有优化器组件。

### 3.4 评估指标

- 各基准上的任务性能（奖励/准确率等）；
- 墙钟优化时间；
- 优化轨迹中的 rollout 效率；
- 提示跨基准迁移性；
- 小批量/验证提升比例（用于解释效率与泛化来源）。

## 4. 资源与算力

- 论文没有明确说明 GPU 型号、GPU 数量、显存或具体训练时长等硬件指标。
- 论文仅报告了**墙钟优化时间**（wall-clock optimization time）这一端到端效率指标，例如：
  - MIPROv2 平均 608 分钟；
  - TextGrad 平均 647 分钟；
  - GEPA 平均 337 分钟；
  - AgentGrad 平均 136 分钟。
- 因此，**无法从论文内容推断真实算力消耗或硬件配置**，这是复现实验信息上的一个缺口。

## 5. 实验数量与充分性

### 5.1 主要实验规模

- **主结果**：5 个基准 × 2 种骨干 = 10 组主要对比实验，每组 3 个随机种子，报告均值±标准误。
- **消融实验**：在 HotpotQA 和 PUPA 上系统地逐步添加三个组件（TI, AS, STGA），验证各组件贡献。
- **优化轨迹**：HotpotQA 上展示验证性能随 rollout 数量变化，比较 AgentGrad 与三个基线。
- **时间对比**：5 个基准上比较四种方法的墙钟时间。
- **效率与泛化分析**：图 4 中比较小批量提升比例和验证提升比例，并做组件级消融。
- **迁移性实验**：5 个源基准对应 5 个同域未见目标基准，评估优化后提示的跨数据集迁移。
- **定性分析**：图 5 提供隐私重写任务中梯度聚类与抽象的具体案例。

### 5.2 充分性与公平性评价

- **优点**：
  - 覆盖多个任务域和两种异质骨干，增强了结论普适性；
  - 包含消融、效率、迁移、定性等多维度分析；
  - 多次随机种子减少方差；
  - 对比方法是近期强基线，且共享相同优化器组件与骨干，较为公平。
- **潜在的不足**：
  - 实验仅限 5 个基准，未覆盖更多如多轮对话、代码生成、工具调用等 MAS 场景；
  - 墙钟时间比较依赖具体 API 延迟和环境影响，缺少硬件/并发控制说明；
  - 没有报告统计显著性检验结果。

## 6. 论文的主要结论与发现

- **SOTA 性能**：AgentGrad 在 5 个 MAS 基准、两种骨干模型上均优于 MIPROv2、TextGrad、GEPA 和无优化基线。
  - GPT-5-mini 平均提升 **+11.76** 分；
  - Qwen3-8B 平均提升 **+9.67** 分。
- **效率显著提升**：AgentGrad 比次快基线 GEPA 平均快 **2.5 倍**，比 TextGrad 快约 4.7 倍。
- **消融结论**：目标识别（TI）、智能体级监督（AS）、语义文本梯度抽象（STGA）三者互补，共同提升性能。
- **效率机制**：AgentGrad 的小批量提升比例达 **0.72**，显著高于 TextGrad（0.44）和 GEPA（0.28）；验证提升比例也最高（0.27），说明其更新既更常用又更可靠。
- **迁移性**：优化后的提示在 5 个未见同域基准上均最佳，其中 2WikiMultiHopQA 上优势显著（51.22 vs 44.89）。
- **核心发现**：通过干预定位责任智能体并利用其修正输出作为监督，能同时提升提示优化的“质量”和“效率”，二者并非此消彼长。

## 7. 优点

- **方法创新**：
  - 将失败归因从“系统级错误”细化到“智能体级修正”，有效缓解多智能体信用分配问题；
  - 引入干预产生的伪标签，为文本梯度提供更直接的监督信号；
  - 语义聚类抽象抑制了无关失败模式的混合，提升提示泛化性。
- **高效实用**：
  - 逆序干预减少定位开销；
  - 高小批量提升比例直接带来更高的有效 rollout 利用率，墙钟时间大幅缩短。
- **实验说服力**：
  - 多任务、多骨干、多随机种子；
  - 与多个强基线充分对比；
  - 提供消融和机制分析，而非单纯报告最终分数。
- **可解释性**：图 5 的定性案例清晰展示了梯度聚类与抽象过程，有助于理解方法行为。

## 8. 不足与局限

- **算力信息缺失**：论文未报告 GPU 型号、数量、显存、训练时长等硬件资源，难以评估真实计算成本和复现难度。
- **依赖 hint 构造**：顺序干预需要为每个任务设计合适的 hint，通常依赖 ground truth 或明确约束；在奖励稀疏、难以构造有效提示的场景中，方法适用性可能受限。
- **额外干预成本**：虽然总体上墙钟时间更短，但每轮训练都需要对失败样本进行多次干预 rollout，可能增加单轮的计算与调用次数。
- **聚类可控性**：聚类数目和语义分组由聚合 LLM 自主决定，尽管有软下界约束，仍可能存在不稳定性或难以预料的聚类结果。
- **实验范围有限**：仅覆盖 5 个 MAS 基准，任务类型相对集中（问答、核查、指令跟随、隐私、数学），未涉及更复杂的协作、竞争或工具使用场景。
- **对比方法数量有限**：仅与 MIPROv2、TextGrad、GEPA 比较，可能遗漏其他同期提示优化方法。
- **统计严谨性**：未提供显著性检验或多次运行的方差分析细节，部分比较的差异可能受随机性影响。
- **对外部环境的依赖**：墙钟时间可能受 API 延迟、网络波动等外部因素影响，论文未说明如何控制这些变量。

（完）
